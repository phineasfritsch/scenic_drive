{
  "speed": [
    { "if": "true", "limit_to": "car_average_speed" }
  ],
  "priority": [
    { "if": "!car_access", "multiply_by": "0" },
    { "if": "road_access == PRIVATE || road_access == NO", "multiply_by": "0" },
    { "if": "surface == GRAVEL || surface == DIRT || surface == GROUND || surface == SAND || surface == UNPAVED || surface == COMPACTED || surface == FINE_GRAVEL", "multiply_by": "0" },
    { "if": "road_class == TRACK", "multiply_by": "0" }
  ],
  "distance_influence": 30
}
