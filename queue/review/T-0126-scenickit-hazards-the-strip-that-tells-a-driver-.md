---
id: T-0126
title: ScenicKit Hazards: the strip that tells a driver what the route will do to them
state: review
owner: agent/claude-opus-5
owner_session: d217767a
claimed_at: 2026-09-08T18:07:49Z
lease_expires_at: 2026-09-08T20:07:49Z
worktree: .worktrees/T-0126
branch: task/T-0126
exclusive: []
touches: [Sources/ScenicKit/Hazards/, Tests/ScenicKitTests/, ops/mutate/]
pins_affected: []
reviewer: agent/reviewer-pr80
depends_on: []
verify: [ops/test, ops/check-pins]
acceptance:
  - "python ops/mutate/hazards.py -> `caught by a named test: 40 of 40   (trapped 0, compile-only 0, MISSED 0, skipped 0)`, all 3 EQUIVALENT mutants reported MISSED, `restored to HEAD: 205d66a0, b0da69d0`, exit 0"
  - "python ops/mutate/hazards.py --prove-vacuity empties BOTH HazardStripTests.swift and HazardStripOmissionTests.swift -> `VACUITY PROOF OK: with no tests present, caught=0 (need 0) and MISSED=40 of 40`, exit 0"
  - "the floor red run: delete any one entry from MUTATIONS -> `REFUSING: 39 mutations and 3 equivalent mutants, expected at least 40 and 3.` exit 2; delete any one EQUIVALENT -> `REFUSING: 40 mutations and 2 equivalent mutants, expected at least 40 and 3.` exit 2; empty both -> `REFUSING: 0 mutations and 0 equivalent mutants, expected at least 40 and 3.` exit 2. `BASELINE` is never printed in any of the three"
  - "the suite red run, WHICH NEEDS A SCRATCH COMMIT because the harness refuses a subject that differs from `git show HEAD:`: in a throwaway worktree, delete `func unattributedClosureReachesTheStrip()` with its `@Test` attribute from HazardStripOmissionTests.swift, COMMIT it, then run the harness -> `caught by a named test: 38 of 40   (trapped 0, compile-only 0, MISSED 2, skipped 0)` naming `drop a closure whose source is empty` and `demote an unattributed closure to .unrecognised`, exit 1. `drop a closure whose source is only whitespace` stays CAUGHT, by whitespaceSourcedClosureReachesTheStrip"
  - "the HEAD red run: plant `(facts.surfaceUnknownKm * 10).rounded() / 10` on the surfaceUnknown append in HazardStrip.swift - a line NO anchor quotes, which the run re-checks mechanically and prints as `the planted line is quoted by any anchor: False` - and run the harness -> `REFUSING: the subject on disk is not HEAD`, naming `disk 19d10b5270e771c82d333824a1a71586  HEAD 205d66a05b21e5bff18e210259d02474`, exit 2, with `BASELINE`, `pristine` and `of 40` all absent from the output"
  - "the F1 red run, the sentence this PR body got wrong twice: in a throwaway worktree, delete `func noCellHasNoCeiling()` with its `@Test` attribute from HazardStripOmissionTests.swift, COMMIT it, then run the harness -> `caught by a named test: 38 of 40   (trapped 0, compile-only 0, MISSED 2, skipped 0)` naming BOTH `no-cell silently stops firing above 600 minutes` and `no-cell stops firing above 100_000 minutes, the last fixture's own top value`, exit 1. It costs TWO mutations, not one: they are covered by that single test and by nothing else"
---
## Brief

The hazard strip is the one place the product tells a driver something they cannot see on the map, and it is
the safety half of the plan's risk table: *"Sedan onto dirt/gated/closed road - Med likelihood, injury and
liability blast radius."*

~~CLAUDE.md~~ **the PLAN's invariant table (`:220`)** makes it a product invariant: **every `Route` has
`hazards: [HazardFlag]`**, and P-SAFE-02 requires the flags at three layers - ScenicKit, a golden fixture,
and an XCUITest on `warning.*`. This is the first of the three. **[CORRECTED 2026-09-15, second review,
MINOR 6. CLAUDE.md contains no such line; P-SAFE-02 is a plan row (`:243`) not yet transcribed into
`pins/PINS.yaml`. The brief is where the misattribution entered and was then copied verbatim into
`HazardFlag.swift`, so it is corrected here as well as there.]**

From the plan, the flags a V1 route can carry:

  * `surfaceUnknown(km:)` - the route spends distance on ways with no `surface` tag. Shown only past a
    threshold, because "unknown" is not "unpaved" and flagging every rural lane trains the driver to ignore
    the strip.
  * `gate` / `ford` - positive evidence of a physical obstruction on the way.
  * `closure(source:until:)` - from the 511/Caltrans feed, with its provenance, because a closure the app
    cannot attribute is one the driver cannot verify.
  * `noCell(minutes:)` - from the FCC BDC join. Minutes, not kilometres: the question a driver is actually
    asking is how long they are out of contact.
  * `twilightArrival(at:)` - arriving after civil twilight, which matters most on exactly the roads this
    product sends people down.

### What makes this more than an enum

**Ordering is the product.** A strip is read top-down and the first line is the one that gets read at all.
So the flags sort by consequence - a closure or a ford before a stretch of unknown surface - and that
ordering has to be pinned, not incidental to how the derivation happened to append them.

**Thresholds are safety decisions.** `surfaceUnknown` only appears past 2 km (the plan's number), and
`noCell` only past a duration worth naming. Both must be pinned against literals, not against the constants
they check - this repository has shipped that defect eleven times in one session and four of those were in
tests written to close a previous instance.

**A flag must never be silently dropped.** The derivation takes what the router reported and turns it into
flags; anything it cannot classify has to surface, not vanish. A hazard strip that quietly omits a ford is
worse than no strip, because the driver has learned to trust it.

Do:

1. `Sources/ScenicKit/Hazards/HazardFlag.swift` - a closed enum, one case per hazard above, `Equatable`
   and `Sendable`, with the payloads the UI needs to write a sentence.
2. `Sources/ScenicKit/Hazards/HazardStrip.swift` - derivation from a route's per-edge facts, ordering by
   consequence, and the thresholds.
3. Tests pinning: the order, each threshold at and either side of its boundary, that an unclassifiable input
   surfaces rather than disappears, and that a route with nothing wrong produces an empty strip rather than
   a reassuring flag.
4. `ops/mutate/hazards.py` in the shape of the existing five: built before a compile failure is believed, a
   catch requires a NAMED test, `trapped` reported separately, an `EQUIVALENT` list asserted the other way
   round, and `--prove-vacuity`.

**No `Package.swift` change**: `Sources/ScenicKit/Hazards/` is inside the existing target path.

**Not in scope**: the 511 feed, the FCC join, and the solar arithmetic. This is the type and the derivation;
each source is its own task, and inventing their formats here would be the fabrication this repository
exists to catch.

## Log
- 2026-09-08T18:07:49Z claimed by agent/claude-opus-5; lease until 2026-09-08T20:07:49Z
- 2026-09-08T19:05:00Z GREEN: `swift test --scratch-path .build-T0126` -> 29 tests in 4 suites passed (0.037 s). `bash ops/check-pins --source-only` -> `PINS ok=5 skipped=9 pending=0 expired=0 failed=0`. Line counts: HazardFlag 71, HazardStrip 106, HazardStripTests 152, ops/mutate/hazards.py 234 - all under the 300 cap. **[STRUCK 2026-09-15 (second review, F13/B2). The line counts in this sentence were already stale when it was written - HazardStripTests was 244 and hazards.py 354 - and "all under the 300 cap" was therefore not something this entry had checked. Current, measured with both `wc -l` and `awk END{print NR}`: HazardFlag 80, HazardStrip 116, HazardStripTests 183, HazardStripOmissionTests 162, ops/mutate/hazards.py 487. The four Swift files are under the cap; the harness is not, and P-SRC-02 does not glob it.]** **[AND THAT "Current" IS ITSELF STALE AS OF 2026-09-15, third fixer round, which is the same failure one layer up: a present-tense number in a dated entry stops being true the next time anyone edits the files. Re-measured on this commit, same two commands: HazardFlag 80, HazardStrip 117, HazardStripTests 188, HazardStripOmissionTests 235, ops/mutate/hazards.py 528. `bash ops/lib/check-line-cap` -> `P-SRC-02: 13 Swift files tracked, none over 300 lines`. Read the LAST Log entry for line counts, never this one.]**
- 2026-09-08T19:05:00Z `bash ops/test` FAILS, and NOT because of this task: `FAIL: services/api exists but vitest produced no report`. `services/api/node_modules` is absent in this worktree AND on main, so the Worker tier cannot run for anyone right now. That is already filed as T-0040 (claimed). The Linux Swift tier is green (29/4 above) and this task adds 13 tests to it. I did not raise floor_linux - a floor ratchet is the reviewer's, and it cannot be verified while ops/test cannot produce a total.
- 2026-09-08T19:05:00Z MUTATION HARNESS `ops/mutate/hazards.py`, 15 mutations, all caught BY A NAMED TEST: ~~`caught by a named test: 15   trapped: 0   compile-only: 0   MISSED: 0   of 15`~~ **[STRUCK 2026-09-15 (second review, F-B2). The harness has never been able to print that string in any state: its format is `caught by a named test: %d of %d   (trapped %d, compile-only %d, MISSED %d, skipped %d)`. The population is now 28, not 15, and the true line is `caught by a named test: 28 of 28   (trapped 0, compile-only 0, MISSED 0, skipped 0)`. A quoted output that was never produced is the worst kind of claim in this file, because it reads like evidence.]** Six move a NUMBER or a comparison direction (2.0 -> 0.1, 2.0 -> 50.0, `>` -> `>=` on surface, 5 -> 60, `>=` -> `>` on no-cell, `>` -> `>=` on twilight) because a reviewer found an earlier harness whose every mutation was structural, which is exactly where a suite is blind. Three permute `severityRank` (closure 0 -> 9, surfaceUnknown 6 -> 0, unrecognised 3 -> 8). The rest attack the two product claims: nothing is dropped (delete the `.unrecognised` loop; drop the dedup; report a ford as a gate; accept a sourceless closure) and a clean route stays empty (seed `out` with a reassuring flag).
- 2026-09-08T19:05:00Z EQUIVALENT MUTANT asserted the other way round **[STRUCK 2026-09-15: singular. There are THREE, and `MIN_EQUIVALENT = 3`; the other two - delete the sort, flatten every severityRank to 0 - arrived as survivors in the first review and were banked as equivalent with a proof.]**: swapping the order of the independent `.ford`/`.gate` appends cannot change the output, since the stable sort restores ford(1) before gate(2). The harness reports it MISSED, which is the PASS - a catch there would mean a test had an opinion about how the code is written rather than what it does, and the way an agent satisfies such a demand is by anchoring on source text.
- 2026-09-08T19:05:00Z VACUITY PROOF: `ops/mutate/hazards.py --prove-vacuity` replaces HazardStripTests.swift with an empty suite and requires every mutation to go MISSED. Result ~~`caught by a named test: 0 ... MISSED: 15 of 15`~~ **[UPDATED 2026-09-15: `VACUITY PROOF OK: with no tests present, caught=0 (need 0) and MISSED=28 of 28`. It now empties BOTH test files - HazardStripOmissionTests.swift was split out of HazardStripTests.swift at the 300-line cap - and the set is grepped each run rather than remembered.]**, `VACUITY PROOF OK`. Without this the harness could be measuring the Swift compiler.
- 2026-09-08T19:05:00Z RED (the harness itself, not just the code): deleted the ONE named test that pins the no-cell boundary, `func noCellThreshold()`, and re-ran. `HARNESS EXIT = 1`, with `MISSED: no-cell comparison strict, so exactly 5 minutes stops firing`. Honest detail worth the reviewer's attention: the sibling mutation `5 -> 60 minutes` stayed CAUGHT, because `orderedByConsequence` passes `noCellMinutes: 30` and would stop producing that flag at 60. So deleting one test cost exactly one mutation, the boundary one - which is the correct and non-flattering result, and evidence the harness measures assertions rather than files. Tests restored byte-identical (verified by comparing bytes, not by re-reading the diff).
- 2026-09-08T19:05:00Z THE DEFECT THIS REPOSITORY KEEPS SHIPPING was avoided deliberately and the reviewer should attack exactly this. The order is asserted in `orderedByConsequence` as a WRITTEN-OUT SEQUENCE of seven `flags[i] == ...` comparisons, never as `flags.map(\.severityRank).sorted()` or any expression that reads `severityRank` - deriving the expectation from the property under test would hold for any ordering the code happened to have. Both thresholds are written as literals (`== 2.0`, `== 5`) and then probed on BOTH sides of the boundary (1.9 / 2.0 / 2.1 and 4 / 5 / 6), so the constant and the comparison are each pinned independently. The three `severityRank` mutations and the two comparison-direction mutations all being caught is the mechanical evidence that this held. **[CORRECTED 2026-09-15 - that last sentence is an overclaim and the PR #80 reviewer was right (F8). Those catches are evidence about the ORDER the strip comes out in. They are NOT evidence that `severityRank` is what produces it, and cannot be: see the 2026-09-15 entry below.]**
- 2026-09-08T19:05:00Z ONE CONSTANT IS NOT FROM THE PLAN AND IS LABELLED SO. `surfaceUnknownMinimumKm = 2.0` is the plan's number, quoted at the declaration ("shown only if the route spends >2 km on such edges"). `noCellMinimumMinutes = 5` IS NOT: the plan specifies the FCC BDC join and the flag and leaves the threshold open. Its doc comment says so in as many words and says the 5 is chosen, so it is not mistaken later for a value handed down. Reviewer: this is the number to argue with.
- 2026-09-08T19:05:00Z `HazardFlag` is a CLOSED enum with no `String` escape hatch, and `.unrecognised(String)` is how a tag this version has no case for reaches the screen instead of vanishing. A strip that quietly omits a ford is worse than no strip, because the driver has learned to trust it. `.unrecognised` sorts at rank 3, above the three advisories, because nobody has judged it.
- 2026-09-08T19:05:00Z SCOPE HELD. No 511 feed, no FCC join, no solar arithmetic - `RouteFacts` is a plain value the caller fills, and inventing those formats here would be the fabrication this repository exists to catch. `Sources/ScenicKit/Hazards/` is inside the existing SwiftPM target path, so NO `Package.swift` edit and no `exclusive:` lock was needed. This is layer 1 of 3 for P-SAFE-02; the golden fixture and the `warning.*` XCUITest are separate tasks and the pin stays pending until they exist.
- 2026-09-08T19:05:00Z moved to review/. Reviewer must not be agent/claude-opus-5 (`ops/queue-check`).

### 2026-09-15 — fixer round against the PR #80 review (`.artifacts/rv1/pr80.md`)

- FIVE SURVIVORS CLOSED, and a sixth of the same shape the review did not name. Every one reproduced first against the shipped suite (`.artifacts/fix_probe.py`, pristine bytes from `git show HEAD:`, never the working tree): `SURVIVED RV1/RV5/RV6/RV7/RV14 ... NO TEST OBJECTED`, plus RV14b, the SOURCE half of `RouteFacts ==`, which survives exactly as the `until` half does. After the five new tests, the same script on the same six: `CAUGHT ... by:` the named test written for it, each caught by exactly one test and no other. NO SOURCE CHANGE: the code was right in all six cases, nothing pinned it. `HazardStrip.swift` and `HazardFlag.swift` are byte-identical to the reviewer's md5s (b67469b6 / ece140a4).
- THE BLIND SPOT BEHIND FOUR OF THE FIVE, named so the next harness does not repeat it: every fixture in the suite probed a threshold at its FLOOR and nothing anywhere probed the TOP of a range. A hazard can therefore be removed for being too big - 600 minutes out of contact, 100 km of unsurveyed surface - and nothing moves. The fifth is the same failure in another dress: seven flags is the number of flag KINDS, so `.prefix(7)` looked complete in every fixture that existed. New tests: `nothingIsTruncated` (nine flags in, nine out, the count transcribed from the inputs by hand and ~~the closures and tags fed in the OPPOSITE order to the one expected out~~ **[STRUCK 2026-09-15, fourth review N1. True of the TAGS only. The closures' expected order IS their input order and cannot be anything else, because `closureOrderIsStable` makes feed order product-visible - so the fixture cannot be reversing them. The same false sentence stood verbatim in the shipped test's own comment at `HazardStripOmissionTests.swift:98`; both are corrected this round.]**), `noCellHasNoCeiling`, `surfaceHasNoCeiling`, `whitespaceSourcedClosureReachesTheStrip`, `routeFactsEqualityCoversEveryField` (all ten fields, since a tuple array cannot be synthesised and each line of the operator can be deleted alone).
- NOT ONE EXPECTED VALUE IS READ BACK FROM THE CODE. No new assertion mentions `severityRank`, `noCellMinimumMinutes` or `surfaceUnknownMinimumKm`; every expectation is a literal transcribed by hand. Each new test was then shown to FAIL: `.artifacts/fix_probe.py` reports each of the six going SURVIVED -> CAUGHT, and the acceptance block's red run deletes `func noCellHasNoCeiling()` and re-runs the harness for `caught by a named test: 20 of 21` with `MISSED: no-cell silently stops firing above 600 minutes`, exit 1. Deleting one test costs exactly one mutation, which is the non-flattering and correct result. **[NOT STRUCK, but it is the ancestor of a false claim and the next round must not copy it again. It was arithmetically sound HERE: `git show 2dc53a2:ops/mutate/hazards.py` carries `MIN_MUTATIONS = 21` and exactly ONE no-cell ceiling mutation. The next round added `no-cell stops firing above 100_000 minutes`, a sibling covered by the same single test and by nothing else, and republished this sentence in the PR body with `20 of 21` bumped to `27 of 28` INSTEAD OF RE-RUNNING IT - the fourth review caught it. Measured on the fourth round's commit: `caught by a named test: 38 of 40   (trapped 0, compile-only 0, MISSED 2, skipped 0)`, both no-cell ceiling mutations MISSED, exit 1. It costs TWO. This is now acceptance line 6 so that it is a checked line rather than prose.]**
- THREE SURVIVORS REFUSED, matching the reviewer's own standard. RV2 (delete the sort), RV4 (flatten every `severityRank` to 0) and RV3 (rank-only unstable sort) are NOT banked as fixes. `flags(for:)` appends in groups of non-decreasing rank, and within a group every element shares a rank, so ordering by `(rank, offset)` is ordering by `offset`, which is the order `out` is already in: the sort is the IDENTITY for every possible input. PROVED, not asserted - `.artifacts/equiv_check.py` dumps `flags(for:)` over 15000 input combinations under pristine, under RV2 and under RV4 and the three dumps are byte-identical (md5 70f0d00463bec147c23ea3e2edae8db2). RV2 and RV4 now live in the harness's EQUIVALENT arm, where a CATCH fails the run, because the only test that could close them is one that reads `severityRank` - the signature defect. RV3 is in NEITHER arm: `sorted(by:)` is not contractually stable in Swift, so a rank-only sort is behaviour-preserving by implementation accident rather than by argument, and neither arm can honestly hold it. Said plainly: the product-visible ORDER is pinned; the MECHANISM that produces it is not, and cannot be without a self-referential test.
- THE LOG'S OWN OVERCLAIM IS CORRECTED, not defended - see the bracketed correction on the 19:05 entry above. What the three caught `severityRank` mutations show is that a WRONG ORDER is caught. They show nothing about the order being derived from the rank table, because for every input the append sequence already equals rank order.
- HARNESS, each guard demonstrated RED then GREEN (`.artifacts/atk_fix.py`, which loads both the HEAD copy and the working copy by path and overrides globals IN MEMORY - the tracked file's md5 is unchanged across the whole attack run):
  * MIN_MUTATIONS 13 -> 21 and MIN_EQUIVALENT 1 -> 3, EQUAL to the shipped population. BEFORE: two of fifteen mutations deleted, no refusal, the run proceeds to `caught by a named test: 0 of 13`. AFTER: one of twenty-one deleted -> `REFUSING: 20 mutations and 3 equivalent mutants, expected at least 21 and 3.` EXIT 2; one of three equivalents deleted -> the same refusal. A floor that does not refuse a deletion is a comment, not a floor.
  * the baseline build is RETRIED, as the mutation build already was (F4 / T-0132). BEFORE, one injected transient failure then a healthy box: `baseline does not build; nothing below would mean anything`, EXIT 2. AFTER: `build()` called twice, the run gets past the baseline. False FAIL only, but it is a false FAIL nobody would have trusted twice.
  * `vacuity_gap()` is new. `--prove-vacuity` empties only HazardStripTests.swift, and the review established by hand with one grep that no other test file references the subject. A fact a harness depends on belongs in the harness: planting a second test file that mentions `HazardStrip` now gives `REFUSING: --prove-vacuity empties only HazardStripTests.swift, but these test files also reference the subject and could catch a mutation: Tests/ScenicKitTests/ZZHazardProbe.swift`, EXIT 2. On the HEAD copy the same planted file produces no refusal at all.
  * `FAIL_LINE` narrowed to `Test "..." recorded an issue` (F7). The old second branch, `Test run with .*failed`, is the RUN-LEVEL summary - "a non-zero exit, summarised", which the docstring promises the harness does not accept. Shown directly: against a run-level summary line OLD matches and NEW does not; against a named failure both match. The full run is `21 of 21` under the narrowed regex, so nothing was lost.
  * `SCRATCH` moved from `.build-mutate-hazards` to `.build/mutate-hazards` (F5). `.gitignore` has `.build/`, which never matched the old name, so the harness left `?? .build-mutate-hazards/` behind and `ops/sane` counted the worktree it had just certified as dirty. `.gitignore` is outside this task's `touches:`, so the fix is in the harness rather than the ignore file. Both stale directories deleted; `git status --porcelain` is empty.
  * the mutation named "sort by rank alone, losing the stable order within a severity" is renamed "sort by rank DESCENDING, so the strip is read worst-last". Its replacement also inverted the comparator, so its catch was evidence about direction and never about stability - the reviewer was right that the name claimed more than the mutation shows.
- ACCEPTANCE BLOCK POPULATED (F12) and both lines re-run verbatim from the committed state, not from prose: `.artifacts/redrun.py` produced the exact strings in the frontmatter, exit 2 and exit 1 respectively, and proved each restore by md5 against `git show HEAD:<path>` plus `git diff --quiet`, not by trusting a `finally`.
- LINE COUNTS, replacing the stale ones in the 19:05 entry (F13 - that entry said 234 when the file was already 256): HazardFlag 71, HazardStrip 106, HazardStripTests 244, ops/mutate/hazards.py 354. **[SUPERSEDED 2026-09-15 by the second round: 80 / 116 / 183 + HazardStripOmissionTests 162 / 487. The correction was made HERE and not in the 19:05 entry that made the claim, and the PR body was never touched at all - which is exactly how the false numbers survived to be found twice. Both are struck at source now.]** The three Swift files are under the 300-line cap. The harness is not, and P-SRC-02 does not apply to it: its assertion (`ops/lib/check-line-cap`) globs `Sources/**/*.swift` and `Tests/**/*.swift` only, and `ops/lib/queue.py` is 1007 lines. Flagging this explicitly because the PR #80 review measured this file against the cap and the next one will too.
- NOT CLOSED, and the PR should still be read as blocked on the first of these:
  * ~~**F1/F2 - a closure with an empty source is dropped entirely, and `emptyStringsIgnored` pins the dropping.** Reproduced, and I am not fixing it in this round: it is a product decision about what an unattributable rank-0 hazard should do (surface as `.closure` with blank provenance, surface as `.unrecognised`, or stay dropped), it changes shipped behaviour plus a shipped test plus the harness mutation "accept a closure with no source", and it was not in this round's scope. It wants its own task and a decision, not a fixer's guess. Note that the new `whitespaceSourcedClosureReachesTheStrip` does NOT cement it: if F1 is resolved by removing the guard, that test still passes, and only the closure line of `emptyStringsIgnored` has to move.~~ **[STRUCK 2026-09-15, second review. The deferral does not hold and the second reviewer was right to refuse it. "It wants its own task and a decision, not a fixer's guess" reads as leaving the question open; the PR was CLOSING it, in the direction of silence, with a test and a harness mutation standing guard over the silence - so two of the three options were already foreclosed and whoever reopened the question would have got a red suite and a red harness. Closed this round: the guard is gone, the closure surfaces, and both test and mutation now point the other way. See the 2026-09-15 blocker entry below.]**
  * F9 (`orderedByConsequence` is titled "whatever order the router reported them" but permutes nothing) - ~~the new `nothingIsTruncated` does feed both orderable inputs in reversed order, so the CLAIM is now covered by a test~~ **[STRUCK 2026-09-15, fourth review N1. `nothingIsTruncated` reverses the TAGS and nothing else; the closures in it are in feed order by necessity. There is exactly one orderable input at this interface, not two. F9 is closed properly in the fourth round below, by dropping the clause from the title.]**; the misleading title on the older test is untouched.
  * F10 (`surfaceUnknownMinimumKm` is an exclusive bound wearing an inclusive name), F11 (5 minutes is too low), F14 (`reviewer:` filled in by the owner). All three are judgement calls for a human or the reviewer, not defects a fixer should quietly settle. F11 in particular is the number the task asked the reviewer to argue with, and the reviewer argued; changing it is a product decision.
- ENVIRONMENTAL, checked and not attributed: `bash ops/test` is green on the Linux tier - `√ Test run with 34 tests in 4 suites passed` (29 before this round; the five new tests are the difference) - then `FAIL: services/api exists but vitest produced no report`, because `services/api/node_modules` is absent here and on main. That is T-0040. `bash ops/check-pins` -> `PINS ok=12 skipped=0 pending=2 expired=0 failed=0 tier=linux`, exit 0. `bash ops/queue-check` -> `QUEUE OK (119 tasks)`. `bash ops/sane` -> `SANE FAIL exit=10`, naming ten worktrees; nine belong to other agents working concurrently on this box and T-0126's own entry was this round's uncommitted work, which is now committed and pushed.
- NOT TRANSITIONED. This task stays in `queue/review/` with `state: review` for a different agent to review, per CLAUDE.md.

### 2026-09-15 — second fixer round, against the PR #80 review in `.artifacts/sg/pr80.md`

- **BLOCKING 1 CLOSED. THE DECISION: an unattributed closure SURFACES, as `.closure` with an empty source, at rank 0.** Reproduced first, not reasoned about: a temporary probe suite in this worktree printed `PROBE unattributed count = 0`, `PROBE named count = 1`, `PROBE unattributed flags = []` — the same route, the same closure, differing only in whether the feed named itself, and the rank-0 flag vanished. The reasoning, so the next round can argue with it rather than guess: the two empty strings in this type do not mean the same thing. For an unclassified TAG the string IS the hazard, so an empty one is nothing and is still skipped. For a CLOSURE the string is only the provenance; the hazard is the closure the router reported. The plan (`:144`) merges the closure blob from TWO feeds — 511 SF Bay Traffic Events and Caltrans LCS D4 — so a polygon arriving with neither named is a defect in the blob, not evidence that the road is open, and P-SAFE-08 (`:249`) says a route never crosses an active closure polygon. `.unrecognised` was considered and REJECTED: it moves a rank-0 hazard below a ford and a gate, which is the same loss in slower motion. ~~The type doc no longer asserts "Nothing is dropped" while dropping something; it says what a missing attribution means.~~ **[STRUCK 2026-09-15, fourth review N2. FALSE WHEN WRITTEN. The round added the "missing attribution" paragraph but left `HazardStrip.swift:11` reading "**Nothing is dropped.**" verbatim, three lines above a loop that skips an empty tag. The sentence describing the doc was edited; the doc was not. Fixed at source this round - the heading now reads "Nothing is dropped FOR BEING UNCLASSIFIABLE" and names the empty tag as the one input that is skipped - so the claim is true as of this commit and was not true of the commit that made it.]**
- **THE REVIEWER WAS RIGHT THAT THE DEFERRAL DID NOT HOLD, and the sentence that made it is struck at source.** The previous round's "it wants its own task and a decision, not a fixer's guess" reads as leaving the question open. The PR was closing it — `emptyStringsIgnored:142` asserted the dropping and `ops/mutate/hazards.py` required "accept a closure with no source" to be CAUGHT — so two of the three options were already foreclosed and whoever reopened the question got a red suite and a red harness. Struck in place, not corrected further down. Same treatment for the 19:05 entry's quoted harness line (`caught by a named test: 15   trapped: 0 ...`), which the harness has never been able to print in any state; for its stale line counts; and for "EQUIVALENT MUTANT" in the singular.
- **THE PR BODY IS REWRITTEN, not annotated.** Every number in it was false (F-B2, all eight re-checked): 15 mutations vs 21 shipped, that unprintable output line, one equivalent vs three, 29 tests vs 34, line counts 152/234 vs 244/354, the wrong test named in the red run, and `ops/mutate/hazards.py committed 100755` when it is committed `100644` and `100755` would FAIL P-OPS-01 (`ops/lib/check-exec-bits:44` classes `.py` under `ops/` as data; `git ls-tree HEAD` gives `100644`). The body now opens by saying it was wrong and what it said, because a corrected sentence further down leaves the false one standing for whoever reads top-down.
- **EVERY CATCH READ AS A TEST NAME, NEVER AS AN EXIT CODE** (`.artifacts/name_probe.py` / `.txt`; subjects extracted with `git show HEAD:`, each restore verified by hashing against HEAD, `git status --porcelain` empty at the end). Baseline first: `BASELINE exit=0  named failures=NONE`, `Test run with 37 tests in 5 suites passed`. Then, one mutation at a time:
  * `drop a closure whose source is empty` -> `"a closure with no source at all still reaches the strip, and still sorts first"`
  * `drop a closure whose source is only whitespace` -> that test AND `"a closure whose source is only whitespace still reaches the strip"`
  * `demote an unattributed closure to .unrecognised, rank 0 -> rank 3` -> `"a closure with no source at all still reaches the strip, and still sorts first"`
  * `truncate the strip to the first NINE flags` -> `"the strip's length follows its input; the cap is not merely moved to the next fixture's size"`
  * `no-cell stops firing above 100_000 minutes` -> `"no signal has a floor but no ceiling, all the way to the top of the type"`
  * `the surface advisory stops firing above 1e300 km` -> `"unsurveyed surface has a floor but no ceiling, all the way to the top of the type"`
  * `compare closure SOURCES order-blind` and `compare closure END TIMES as a set` -> `"two RouteFacts whose closures arrive in a different order are not equal"`
  * `accept an empty unknown tag as a hazard` -> `"an empty unknown TAG is not a hazard, because there the string IS the hazard"`
- **NON-BLOCKING 3 CLOSED BY REMOVING THE CEILING, NOT BY MOVING IT AGAIN.** The reviewer's point was exact: the previous round closed `.prefix(7)` and `< 600` by probing nine and 100_000, which made nine and 100_000 the new magic numbers, and `.prefix(9)` and `<= 100_000` then survived the whole suite. Ceilings are now probed at the top of the TYPE — `Int.max` for `noCellMinutes`, `.greatestFiniteMagnitude` for `surfaceUnknownKm` — above which no ceiling can hide, and the strip's length is SWEPT (`stripLengthFollowsTheInput`, closure counts 1/2/9/10/33/128/257, expecting `closureCount + 6` counted by hand from the inputs) instead of pinned at one fixture's size. A cap anywhere below 257 now fails. Honest limit: 258 would still survive, as it would for any finite test; what is gone is the cap EQUALLING the size of the one fixture that exists.
- **NON-BLOCKING 4 CLOSED, each half isolated.** `routeFactsEqualityIsOrderSensitiveOnClosures` uses two pairs on purpose, because each is invisible to the other's mutation: sources swapped with the end times held equal catches `.sorted()` on the source halves; the same feed twice with only the end times swapped catches `Set(...)` on the until halves. Both mutations are in the harness.
- **NON-BLOCKING 5 CLOSED — the harness now refuses a subject that is not HEAD, BEFORE it builds anything**, and verifies its restore against HEAD rather than against bytes it read off the same disk. A9b re-run verbatim (`.artifacts/plant_a9b.py` / `.txt`): the same rounding plant on the same `out.append(.surfaceUnknown(...))` line, mechanically confirmed unquoted by any anchor (`the planted line is quoted by an anchor: False   []`), planted md5 `6b36f929` against HEAD `537dd7a7` -> `REFUSING: the subject on disk is not HEAD`, `EXIT = 2`, and `'BASELINE' in output: False`, `'of 28' in output: False`. It printed `21 of 21`, exit 0 last round. Restored: `restored == git show HEAD: True`, `git diff --quiet exit = 0`, porcelain empty. Stated as a limit rather than a claim: this catches an uncommitted edit to the thing being measured, NOT an author who deletes the check — the modified code is what runs. The sibling `ops/mutate/handoff.py` on main still has the gap; that is not this task's `touches:`.
- **MINOR 6 CLOSED.** `HazardFlag.swift` credited CLAUDE.md with *every `Route` has `hazards: [HazardFlag]`*. CLAUDE.md contains no such line; the plan does (`:220`), and P-SAFE-02 is a plan row (`:243`) not yet in `pins/PINS.yaml`. Attribution corrected and the correction itself recorded in the doc, since `pins_affected: []` means nothing mechanical would catch a relapse.
- **MINOR 7 NOT CLOSED, and it CANNOT be closed by me.** `ops/lib/queue.py:428` requires a task in `review/` to carry a `reviewer:` that parses as `agent/<name>`; blanking it produces `in review/ without a reviewer`. So the stale `reviewer: agent/reviewer-pr80` has to stay until a reviewer overwrites it with their own name. Flagging it rather than fixing it: the field is owner-written by construction and the gate compares only `owner` against `reviewer`, so the next reviewer must OVERWRITE it, not inherit it.
- **F9 NOT CLOSED, deliberately.** `orderedByConsequence` is still titled "whatever order the router reported them" and still permutes nothing. ~~`nothingIsTruncated` and `stripLengthFollowsTheInput` both feed reversed input, so the CLAIM is covered~~ **[STRUCK 2026-09-15, fourth review N1. `nothingIsTruncated` reverses the tags only. `stripLengthFollowsTheInput` reverses NOTHING: its closures are generated `feed 0 ... feed N` in output order and it carries a single unknown tag. The claim was covered by one fixture, not two, and only for tags. F9 is closed in the fourth round below.]**; renaming an older test is the kind of cosmetic close this round is supposed to refuse, and it is a reviewer's call whether the title or the fixture should move.
- **THREE SURVIVORS STILL REFUSED**, unchanged and for the reviewer's own reason: "delete the sort" and "flatten every severityRank to 0" are the IDENTITY for every possible input, because `flags(for:)` appends in groups of non-decreasing rank, so they stay in EQUIVALENT where a catch FAILS the run — and the rank-only unstable sort stays in NEITHER arm, because `sorted(by:)` is not contractually stable in Swift and neither arm can honestly hold it. All three reported MISSED in the run above.
- **HARNESS, every guard shown RED then GREEN.** `.artifacts/harness_attack.py` / `.txt`, all by in-memory override; `ops/mutate/hazards.py` md5 `3838e81d` identical before and after, porcelain empty.
  * floors raised 21 -> 28 (MIN_EQUIVALENT stays 3), EQUAL to the shipped population: delete one mutation -> `REFUSING: 27 mutations and 3 equivalent mutants, expected at least 28 and 3.` EXIT 2; delete one equivalent -> `REFUSING: 28 mutations and 2 ...` EXIT 2; empty both -> `REFUSING: 0 mutations and 0 ...` EXIT 2.
  * `--prove-vacuity` now empties EVERY test file that mentions the subject, with a distinct empty type per file. `subject_test_files()` greps for the set each run; `vacuity_gap()` is the difference against `EMPTIED`. Full run: `VACUITY PROOF OK: with no tests present, caught=0 (need 0) and MISSED=28 of 28`, exit 0.
  * that gap check shown LOAD-BEARING, and the first attempt at showing it FAILED — reported as it happened. Reverting `EMPTIED` to one file and stubbing the check gave `baseline does not build`, EXIT 2, not a false proof, because HazardStripOmissionTests calls `HazardStripTests.date(_:)` and emptying one of the pair stops the other compiling. That is an accident of a shared fixture that would evaporate the day someone copies the helper, so it proves nothing. The second attempt used the real risk (`.artifacts/gap_necessity2.py`): a self-contained THIRD test file planted untracked. With the check: `vacuity_gap() = ['Tests/ScenicKitTests/ZZHazardProbe.swift']`, `REFUSING: --prove-vacuity empties HazardStripTests.swift, HazardStripOmissionTests.swift, but these test files also reference the subject and could catch a mutation: ...`, EXIT 2. With the check stubbed: `caught by a named test: 1 of 1`, `VACUITY PROOF FAILED: ... caught=1 (need 0)`, EXIT 1 — the planted file was never emptied and caught the mutation. Plant removed; `vacuity_gap()` back to `[]`.
  * the baseline build is still retried: one injected transient failure then a healthy box -> `build() calls = 2`, `got past the baseline: True`; control with a genuinely broken box -> `baseline does not build; nothing below would mean anything`, EXIT 2.
  * TWO mutations lost their anchor when the blocker was fixed (`where !c.source.isEmpty` no longer exists) and were REPLACED BY THREE, not deleted: reinstating the `isEmpty` guard, reinstating a `trimmingCharacters` guard, and demoting the flag to `.unrecognised` must each be CAUGHT. Every way of losing an unattributed closure that the old pair protected is still pinned, from the other side. One further mutation was added for a gap the fix itself created: taking the closure line out of `emptyStringsIgnored` left it with a single assertion and no mutation anywhere pinning it — a test that had quietly lost its ability to fail — so `accept an empty unknown tag as a hazard` now covers it.
- **THE SUITE RED RUN NEEDS A SCRATCH COMMIT NOW, and the acceptance line says so.** A consequence of the HEAD check that would otherwise make the acceptance block irreproducible: deleting a test by editing the working tree is refused before anything builds. Done in a throwaway worktree I created and removed (`.worktrees/t0126-red`, detached, its own `.build/`): delete `func unattributedClosureReachesTheStrip()` with its `@Test` attribute, COMMIT it, re-run -> `caught by a named test: 26 of 28   (trapped 0, compile-only 0, MISSED 2, skipped 0)`, naming `drop a closure whose source is empty` and `demote an unattributed closure to .unrecognised`, HARNESS EXIT 1. `drop a closure whose source is only whitespace` stayed CAUGHT, because `whitespaceSourcedClosureReachesTheStrip` still covers it — deleting one test cost exactly the mutations it was the only guard for, which is the non-flattering and correct result. Then `git reset --hard`, `restored md5 e19dca7d == HEAD: True`, `git diff --quiet exit = 0`, porcelain empty; worktree removed.
- **FILE SPLIT.** `HazardStripTests.swift` reached 310 lines with the new tests, over the 300 cap, so everything about a hazard GOING MISSING moved to `HazardStripOmissionTests.swift` (`@Suite("Hazard strip - nothing goes missing")`). One date fixture for both, deliberately not copied. `bash ops/lib/check-line-cap` -> `P-SRC-02: 13 Swift files tracked, none over 300 lines`.
- **VERIFICATION, re-run from the committed state.** `python ops/mutate/hazards.py` -> `caught by a named test: 28 of 28   (trapped 0, compile-only 0, MISSED 0, skipped 0)`, all 3 EQUIVALENT MISSED, `restored to HEAD: 537dd7a7, b0da69d0`, exit 0. `bash ops/check-pins --source-only` -> `PINS ok=5 skipped=9 pending=0 expired=0 failed=0 tier=linux source-only`, exit 0. `bash ops/check-pins` -> `PINS ok=12 skipped=0 pending=2 expired=0 failed=0 tier=linux`, exit 0. `bash ops/queue-check` -> `QUEUE OK (119 tasks)`. `git ls-tree HEAD` -> `100644 ops/mutate/hazards.py`, which is what P-OPS-01 requires.
- **ENVIRONMENTAL, CHECKED AND NOT ATTRIBUTED.** `bash ops/test` -> Linux tier `Test run with 37 tests in 5 suites passed`, then `FAIL: services/api exists but vitest produced no report`, exit 1. Checked against `main` at `a100ffb` in the main checkout: the identical `FAIL` line, and `services/api/node_modules` absent there too. T-0040. `floor_linux` still not raised — that ratchet is the reviewer's and cannot be verified while `ops/test` produces no total.
- **NOT TRANSITIONED.** Stays in `queue/review/` with `state: review` for a different agent, per CLAUDE.md. `reviewer:` still reads `agent/reviewer-pr80` because the schema will not accept it empty; the next reviewer must overwrite it.

### 2026-09-15 — third fixer round, against the fourth PR #80 review in `.artifacts/fn/pr80.md`

- **BLOCKING F1 CLOSED — the red run quoted in the PR DESCRIPTION did not reproduce, and it was the second body in a row to carry a number nobody re-ran.** The body said *"deleting `func noCellHasNoCeiling()` costs exactly one mutation — `caught by a named test: 27 of 28` with `MISSED: no-cell silently stops firing above 600 minutes`, exit 1."* Re-run on this commit in a throwaway worktree with the deletion COMMITTED (the harness refuses an uncommitted subject), `.artifacts/fn5/noceil.txt`:
  ```
  caught by a named test: 38 of 40   (trapped 0, compile-only 0, MISSED 2, skipped 0)
    MISSED: no-cell silently stops firing above 600 minutes (no test objected)
    MISSED: no-cell stops firing above 100_000 minutes, the last fixture's own top value (no test objected)
  HARNESS EXIT = 1
  ```
  It costs TWO mutations, not one. WHY IT WAS WRONG, not merely THAT it was wrong: the ancestor of that sentence is the first round's `caught by a named test: 20 of 21` / one MISSED, which was arithmetically sound at the time — `git show 2dc53a2:ops/mutate/hazards.py` has exactly ONE no-cell ceiling mutation and `MIN_MUTATIONS = 21`. The second round ADDED `no-cell stops firing above 100_000 minutes` as a sibling covered by the same single test, and the sentence was republished with `20 of 21` changed to `27 of 28` instead of being run. The arithmetic was updated; the measurement was not. The bumped sentence is gone from the body and the true output is pasted there; the `20 of 21` entry above is annotated rather than struck, because it was true of the commit that made it.
- **BLOCKING F2 CLOSED — five hazards could still be dropped silently, and the shipped suite did not object.** Reproduced FIRST against the shipped subject at `e8b2490` (`HazardStrip.swift` md5 `537dd7a7`, the reviewer's hash, bytes from `git show HEAD:`, only the harness swapped in), `.artifacts/fn5/red.txt`: `BASELINE exit=0  named failures=NONE`, then `SURVIVORS: 5 of 5`, every one `FAILING TEST NAME: NONE - no test objected`, the shipped suite green under each. THE BLIND SPOT, named so the next harness does not repeat it: the previous round swept the CLOSURE COUNT and left every other count and every string LENGTH pinned at whatever the fixtures happened to contain — at most two distinct unknown tags, never two closures from one feed, no closure source over eleven characters, no unknown tag over sixteen, no arrival more than two hours after dusk. NO SOURCE CHANGE: the code was right in all five cases, nothing pinned it. Five named tests and five mutations (`MIN_MUTATIONS` 28 -> 33), and the verdict on each read as a test NAME and never as an exit code (`.artifacts/fn5/green.txt`) — each caught by EXACTLY ONE test, the one written for it, and by no other:
  * `cap the unknown tags at the TWO any fixture used` -> `"every distinct unknown tag reaches the strip; that list is not capped either"`
  * `truncate an unknown tag's TEXT at twenty characters` -> `"an unknown tag reaches the strip with its text intact, however long it is"`
  * `dedupe closures by source` -> `"two closures from the same feed are two closures, not one"`
  * `drop a closure whose source is longer than the longest in any fixture` -> `"a closure reaches the strip whatever length its source is"`
  * `twilight silently stops firing more than two hours after dusk` -> `"twilight has a floor but no ceiling: arriving long after dusk is still arriving after dusk"`
- **THE LIMIT THAT REMAINS, restated rather than quietly dropped.** Any cap strictly above the largest value a sweep reaches still survives - 263 for the strip length, which the review MEASURED as its A7, and 129 for distinct tags, which is the same arithmetic and which I did not measure. That is a property of finite sweeps and these five tests do not remove it. What is gone is the cap EQUALLING the size of the one fixture that exists, which is the form this defect took in three rounds running.
- **N1 CLOSED, in BOTH places it stood.** `HazardStripOmissionTests.swift:98` said the closures in `nothingIsTruncated` are "fed in the OPPOSITE order to the one expected out". True of the TAGS (avalanche_gate is reported second and shown first); false of the closures, whose expected order IS their input order and cannot be otherwise, because `closureOrderIsStable` makes feed order product-visible. The identical sentence stood in this Log's own 2026-09-15 entry, and a second copy claimed `stripLengthFollowsTheInput` feeds reversed input too — it reverses nothing at all, generating `feed 0 ... feed N` in output order with a single unknown tag. All three are struck at source above.
- **N2 CLOSED AT THE DOC, not at the sentence about the doc.** The second round's Log said "the type doc no longer asserts 'Nothing is dropped' while dropping something". `HazardStrip.swift:11` still read "**Nothing is dropped.**" verbatim while `:96` skipped an empty tag: the sentence describing the doc had been edited and the doc had not. The heading now reads "**Nothing is dropped for being unclassifiable**" and names the empty tag as the one input that is skipped, so the claim is true as of this commit; the Log sentence is struck where it was made, because it was not true of the commit that made it.
- **N3 / F9 CLOSED BY DROPPING THE CLAUSE.** `orderedByConsequence` was titled "flags sort by consequence, whatever order the router reported them" while permuting nothing, through three reviews. The reviewer's call, and I agree with it: at the `RouteFacts` interface each hazard KIND has its own field, so there is no cross-kind input order to reverse and no fixture can make the clause true. Title is now "every kind of flag at once, in the one order a driver reads them", with a comment saying what can be reordered is reordered where it lives (tags in `nothingIsTruncated`, closures in `closureOrderIsStable`). No assertion changed.
- **HARNESS, every guard shown RED on this commit** (`.artifacts/fn5/attack.txt`, all by in-memory override except the plant; `ops/mutate/hazards.py` md5 `3a7f3820` identical before and after, `git status --porcelain: ''`):
  * one mutation deleted -> `REFUSING: 39 mutations and 3 equivalent mutants, expected at least 40 and 3.` EXIT 2, `'BASELINE' in output: False`; one equivalent deleted -> `REFUSING: 33 mutations and 2 equivalent mutants, ...` EXIT 2; both lists emptied -> `REFUSING: 0 mutations and 0 equivalent mutants, ...` EXIT 2. The floors EQUAL the shipped population, so no single deletion slips through.
  * `EMPTIED` reverted to the single-file shape -> `REFUSING: --prove-vacuity empties HazardStripTests.swift, but these test files also reference the subject and could catch a mutation: Tests/ScenicKitTests/HazardStripOmissionTests.swift` EXIT 2, no build.
  * baseline build retry: one injected transient failure then a healthy box -> `build() calls = 2   got past the baseline: True`; control with a genuinely broken box -> `baseline does not build; nothing below would mean anything`, EXIT 2.
  * the HEAD check, re-run on this commit with the same rounding plant on the `surfaceUnknown` append and the line mechanically confirmed unquoted (`the planted line is quoted by any anchor: False   []`): planted md5 `19d10b52` vs HEAD `205d66a0` -> `REFUSING: the subject on disk is not HEAD`, EXIT 2, with `BASELINE`, `pristine` and `of 40` all absent; `restored == git show HEAD: True`, `git diff --quiet exit = 0`, porcelain empty.
- **EVERY MUTATION'S CATCH READ AS A TEST NAME, all 33 of them, not just the new five** (`.artifacts/fn5/allnames.txt`). `BASELINE exit=0  named failures=NONE`, then all 33 `CAUGHT` and `SURVIVORS: 0 of 33`, no TRAPPED and no COMPILE-ONLY, `git status --porcelain: ''`, PROBE EXIT 0. The distribution, because a summary hides it: 16 of the 33 are caught by EXACTLY ONE named test, the most-covered is `a clean route gets a reassuring flag` at 23, and `truncate the strip to the first NINE flags` is now caught by two - `stripLengthFollowsTheInput` and the new `everyDistinctUnknownTagReachesTheStrip`, because 128 distinct tags is more than nine flags.
- **VERIFICATION, re-run from the committed state.** `python ops/mutate/hazards.py` -> `caught by a named test: 40 of 40   (trapped 0, compile-only 0, MISSED 0, skipped 0)`, all 3 EQUIVALENT MISSED, `restored to HEAD: 205d66a0, b0da69d0`, HARNESS EXIT 0. `--prove-vacuity` -> `VACUITY PROOF OK: with no tests present, caught=0 (need 0) and MISSED=40 of 40`, EXIT 0. `bash ops/check-pins --source-only` -> `PINS ok=5 skipped=9 pending=0 expired=0 failed=0 tier=linux source-only`, exit 0. `bash ops/queue-check` -> `QUEUE OK (119 tasks)`, exit 0. `bash ops/lib/check-line-cap` -> `P-SRC-02: 13 Swift files tracked, none over 300 lines`. `bash ops/lib/check-exec-bits` -> `P-OPS-01: 34 files, 23 required present, all modes correct`; `git ls-tree HEAD ops/mutate/` -> `100644`.
- **LINE COUNTS, measured with both `wc -l` and `awk END{print NR}`:** HazardFlag 80, HazardStrip 117, HazardStripTests 188, HazardStripOmissionTests 235 — all under the 300 cap. `ops/mutate/hazards.py` is 528 and P-SRC-02 does not glob it.
- **THE ACCEPTANCE BLOCK IS RE-RUN, not re-typed.** All five lines were executed against the committed state this round and every quoted string below is pasted from that run's stdout. Line 4 changed population (28 -> 33) and line 5 changed hashes, because the subject's doc comment moved; both were re-measured rather than adjusted.
- **ENVIRONMENTAL, CHECKED AND NOT ATTRIBUTED.** `bash ops/test` -> Linux tier `Test run with 42 tests in 5 suites passed`, then `FAIL: services/api exists but vitest produced no report`, exit 1. `services/api/node_modules` is absent in this worktree AND in the main checkout at `2c5300a`. T-0040. `floor_linux` still not raised — that ratchet is the reviewer's and cannot be verified while `ops/test` produces no total.
- **REFUSED, and named so the next round does not read silence as agreement.** MINOR 7 (`reviewer:` still reads `agent/reviewer-pr80`) cannot be closed by me: `ops/lib/queue.py:428` requires a task in `review/` to carry a `reviewer:` that parses as `agent/<name>`, so the next reviewer must OVERWRITE it, not inherit it. The three EQUIVALENT mutants stay equivalent and stay in the arm where a catch FAILS the run. The rank-only unstable sort stays in NEITHER arm.
- **NOT TRANSITIONED.** Stays in `queue/review/` with `state: review` for a different agent, per CLAUDE.md.
- 2026-09-16T08:40:00Z **ROUND 7 - agent/claude-opus-5 for the owner, answering agent/r6-pr80's FAIL.** The
  code for this round was committed as `3d8e697` by a fixer agent that was then killed by a session limit
  BEFORE it wrote this entry or re-ran the acceptance block. It also left four throwaway worktrees
  (`fx7-acc`, `fx7-r4`, `fx7-r6`, `fx7-vac`) each holding a live mutant on `HazardStrip.swift` - a
  `.prefix(9)` cap, a `< 100` ceiling, a deleted test file - which is T-0130's hazard exactly. So this entry
  is written by the orchestrator, and every claim in it was RE-MEASURED against `3d8e697` rather than taken
  from the dead agent's commit message. The four worktrees were diffed, recorded, and removed.

  **BLOCKING 1 - reproduced by the commit message, and re-verified here.** `.closure` carries two pieces of
  metadata, `source` and `until`, and only `source` was pinned; `until` took three values in the whole
  suite. The commit adds the `until` half in both directions - *"drop a closure that lifted before the
  epoch - a guard on `until`, the metadata nobody swept"* and *"drop a closure that lifts after 3.0e9 - the
  same guard on `until`, at the other end"* - plus the audit for siblings the review asked for, which found
  the same hole twice more (twilight's DUSK, the surface advisory's km). MUTATIONS 33 -> 40, MIN_MUTATIONS
  33 -> 40, equal to the population.

  **RE-MEASURED, every line, from a clean tree at `3d8e697`:**

      swift test --scratch-path .build-verify80   -> Test run with 44 tests in 5 suites passed        exit 0
      python ops/mutate/hazards.py                -> caught by a named test: 40 of 40 (trapped 0,
                                                     compile-only 0, MISSED 0, skipped 0)
                                                     restored to HEAD: 205d66a0, b0da69d0             exit 0
      --prove-vacuity                             -> VACUITY PROOF OK: ... MISSED=40 of 40             exit 0
      MUTATIONS.pop()                             -> REFUSING: 39 mutations and 3 equivalent mutants,
                                                     expected at least 40 and 3.                       exit 2
      EQUIVALENT.pop()                            -> REFUSING: 40 mutations and 2 ..., expected 40 and 3
      both cleared                                -> REFUSING: 0 mutations and 0 ..., expected 40 and 3

  Both `until` mutations and both siblings print `caught ... exit=1`. `git status --porcelain` is empty
  after every run.

  **The two suite red-runs**, each in its own throwaway worktree at `3d8e697` with the test deleted and
  COMMITTED (the harness refuses a test file that differs from HEAD): deleting
  `unattributedClosureReachesTheStrip` -> `caught by a named test: 38 of 40 ... MISSED 2`, naming
  `drop a closure whose source is empty` and `demote an unattributed closure to .unrecognised`, exit 1;
  deleting `noCellHasNoCeiling` -> `38 of 40 ... MISSED 2`, naming both no-cell ceilings, exit 1. The
  acceptance block's numbers were 33/31 and are now 40/38, changed only after the runs printed them.

  **One thing about the harness, recorded rather than fixed here.** Its floor is `len(MUTATIONS) <
  MIN_MUTATIONS`, a lower bound - so deleting one mutation refuses (shown above) but ADDING one without
  bumping the floor does not. `ops/mutate/budget.py` moved to equality for that reason. Same shape, not this
  task's finding, not slipped in between a FAIL and a re-review.

  **STILL OPEN:** nothing from round 6 that I can find. The reviewer's non-blocking notes were addressed in
  the commit (its message lists them); a reviewer should hold it to that.
