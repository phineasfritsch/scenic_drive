---
id: T-0134
title: ScenicKit SegmentScore: the geometric mean that keeps a curvy industrial road apart from a straight redwood one
state: review
owner: agent/unknown
owner_session: null
claimed_at: 2026-09-08T21:21:50Z
lease_expires_at: 2026-09-08T23:21:50Z
worktree: null
branch: task/T-0134
exclusive: []
touches: [Sources/ScenicKit/Scoring/, Tests/ScenicKitTests/, ops/mutate/]
pins_affected: []
reviewer: agent/reviewer-pr-segmentscore
depends_on: []
verify: [ops/test, ops/check-pins]
acceptance:
  - "swift test --scratch-path .artifacts/swift -> Test run with 35 tests in 5 suites passed, exit 0 (was 30 in 4 at 3311563)"
  - "python ops/mutate/segmentscore.py -> caught by a named test: 49 of 49   (trapped 0, compile-only 0, MISSED 0, skipped 0); EQUIVALENT 2 of 2 MISSED; exit 0"
  - "python ops/mutate/segmentscore.py --prove-vacuity -> VACUITY PROOF OK: with no tests present, caught=0 (need 0) and MISSED=49 of 49, exit 0"
  - "RED coverage: the same 49 mutations against the reviewed commit 3311563, in a detached worktree with TEST_FILES cut to the one test file that exists there -> caught by a named test: 35 of 49 (MISSED 14), exit 1"
  - "RED population floor: delete one term from UNIT_TERMS -> REFUSING: 48 mutations and 2 equivalent mutants, expected at least 49 and 2, exit 2; file restored, git hash-object 283afb3b identical before and after"
  - "RED vacuity scope: drop SegmentScoreValidationTests.swift from TEST_FILES, then --prove-vacuity -> VACUITY PROOF FAILED: with no tests present, caught=27 (need 0) and MISSED=22 of 49, exit 1; file restored, git hash-object 283afb3b identical before and after"
  - "bash ops/check-pins -> PINS ok=12 skipped=0 pending=2 expired=0 failed=0 tier=linux, exit 0"
  - "bash ops/test -> Swift tier green (Test run with 35 tests in 5 suites passed) then FAIL: services/api exists but vitest produced no report, exit 1 - pre-existing T-0040, services/api/node_modules is absent here and on main, NOT this PR"
---
## Brief

This is the scenic engine's centre. Everything else - the lambda search, the route score, the corridor
learner - is machinery around the number this produces per way.

From the plan:

```
score = GATE × M^0.35 × E^0.65          in [0,1]     alpha = 0.35, scenery-led, because this is a car product
M = 0.45·curv + 0.20·elev_gain + 0.20·speed_fit + 0.15·sinuosity
E = 0.24·canopy + 0.22·relief + 0.16·(1−impervious) + 0.14·poi + 0.12·water + 0.12·(1−furniture)
    (+0.15 byway, capped)
Soft multipliers: tunnel > 300 m ×0.15 ; within 150 m of a motorway ×0.7
Absent surface: primary/secondary/tertiary → treat as paved
                unclassified/residential   → ×0.8 and raise a surface_unknown flag
highway ∈ {motorway, motorway_link, trunk, trunk_link} → score 0 (dull, NOT a routing gate)
```

### The design decision the tests must protect

**The mean is geometric, not arithmetic**, and the plan says why: *"keeps a curvy industrial road (0.32) apart
from a straight redwood road (0.62)"*. A road that is thrilling to drive but ugly should not average out to
the same number as a road that is beautiful but dull, because those are different products - and an
arithmetic mean says they are the same. The exponents 0.35 / 0.65 are the statement that this is a **car**
product where scenery leads.

An arithmetic mean is exactly the "simplification" a later agent will reach for, and most fixtures will not
notice: the two means agree whenever M and E are close. **A test that does not include a case with M and E far
apart pins nothing.** Build the discriminating fixture deliberately and say in the test why those numbers.

### The other invariant

`highway = motorway` scores **0** and is **not gated**. Those are different mechanisms and this task must not
blur them: [[T-0133]] owns the gate (a motorway is `.allowed`), this owns the score (a motorway is dull). The
whole freeway-shoulders design depends on both being true at once. A test should assert the pair together, so
that anyone changing one meets the other.

### Do

1. `Sources/ScenicKit/Scoring/SegmentTerms.swift` - the inputs, as a plain value with named terms in `0...1`.
   Validate rather than clamp: a term outside `0...1` is a bug upstream, and quantising it hides that.
2. `Sources/ScenicKit/Scoring/SegmentScore.swift` - the formula, the soft multipliers, the surface rule, and
   the motorway-scores-zero rule. Every weight a named constant, since these are the numbers the tuning
   process in the plan will move.
3. Tests pinning, at minimum: each weight set summing to 1 (asserted against a literal, never by summing the
   constants under test); the geometric mean, with a fixture where arithmetic and geometric disagree
   **loudly**; the exponents; each soft multiplier at and either side of its threshold (tunnel 300 m, motorway
   proximity 150 m); the surface rule for both road classes; output always in `0...1`; and motorway scoring 0
   while `Gates.decide` allows it.
4. `ops/mutate/segmentscore.py` on the corrected contract - the reference is `ops/mutate/gates.py`, which is
   the only one carrying all of: pass condition `caught == len(MUTATIONS)`; `--prove-vacuity` requiring
   `caught == 0` AND `missed == len(MUTATIONS)` over **every** test file ([[T-0132]]); an `EQUIVALENT` arm
   asserted the other way round; and a **baseline build retried twice** before it is believed. **Most
   mutations must move a NUMBER** - this is a file of thirteen weights, and a structural-only mutation set
   would be blind exactly where it matters. `arithmetic mean instead of geometric` is the mutation that
   matters most.
5. **No `Package.swift` change**: `Sources/ScenicKit/Scoring/` is inside the existing target path.

### Do not invent an oracle

The plan quotes two worked outputs - 0.32 for a curvy industrial road and 0.62 for a straight redwood one -
but **does not give the term values that produce them**. Do not reverse-engineer inputs to hit those numbers
and then present the result as an oracle: that is fitting the answer to the target, and this repository has
already rejected one oracle for less. Pin the formula's *structure and behaviour* with fixtures whose expected
values are computed by hand from the stated weights, and say plainly in the log that the plan's two numbers
are unreproduced. When the ETL exists, the real oracle is the Curvature project's Vermont output, which the
plan names.

### Not in scope

The ETL, the raster inputs, `scenic_score`'s 0-10 scaling for GraphHopper, and the Bradley-Terry tuning. This
is the reference formula in Swift, on given terms.

## Log
- 2026-09-09T00:30:00Z filed by agent/claude-opus-5. Filed with id T-0134 by hand; `ops/new-task` allocated
  T-9902 again - see [[T-0128]].
- 2026-09-08T21:21:50Z claimed by agent/unknown; lease until 2026-09-08T23:21:50Z
- 2026-09-09T01:00:00Z GREEN: `swift test --scratch-path .build-T0134` -> **30 tests in 4 suites passed**. `bash ops/check-pins --source-only` -> `PINS ok=5 skipped=9 pending=0 expired=0 failed=0`. `bash ops/queue-check` -> `QUEUE OK`. Line counts 127 / 95 / 237 / 318 - the three Swift files are under the 300 cap; the harness is over it and the Python cap only lands with T-0058 (PR #44), which is flagged below rather than pre-empted.
- 2026-09-09T01:00:00Z **THE MEAN IS THE PRODUCT DECISION AND IT IS PINNED WHERE IT ACTUALLY DIFFERS.** An arithmetic mean agrees with the geometric one whenever M and E are close, which is most fixtures, so a suite without a deliberately unbalanced case pins nothing. Built two: M=0.9/E=0.1 (a hairpin through a scrapyard) and M=0.1/E=0.9 (a straight road through redwoods). **Expected values computed by hand from the weights before running anything** - `0.9^0.35 * 0.1^0.65 = 0.9637955 * 0.2238721 = 0.2157672` and `0.1^0.35 * 0.9^0.65 = 0.4466839 * 0.9338075 = 0.4171165` - and both matched to 1e-5 on the first run, which is independent confirmation rather than a value read off the code. The arithmetic mean would give 0.38 and 0.62 for the same two roads, so each test also carries a bound the arithmetic answer fails.
- 2026-09-09T01:00:00Z A companion test records the trap rather than only avoiding it: at M=E=0.5 both means give exactly 0.5, so `balancedRoadHidesTheDifference` exists to stop somebody "simplifying" the mean, seeing a balanced fixture pass, and believing it.
- 2026-09-09T01:00:00Z **BOTH WEIGHT SETS ARE PROVED TO SUM TO ONE THROUGH THE FORMULA, not by adding the constants.** Adding `curvatureWeight + elevationGainWeight + ...` and checking the total asks each set about itself. Instead: every term at 1 must give M=1 and E=1, so the score must be exactly 1. ~~which holds only if each set sums to 1~~ **CORRECTED IN ROUND 2 (reviewer F7): that is FALSE. `SM^a * SE^b == 1` is one equation in two unknowns and a whole curve of weight pairs satisfies it; the reviewer produced a counterexample by measurement.** The claim now rests on a second equation supplied by the byway cap - see the round-2 entry below. Two mutations move a single weight so a set sums to 1.05 (`speedFit` 0.20 -> 0.25) or 1.10 (`canopy` 0.24 -> 0.34) - the original line said 1.05 for both, which was inexact; both are caught. The individual weights are *also* written out as literals, because the plan's tuning process will move them and a moved weight should be a deliberate act with a visible diff.
- 2026-09-09T01:00:00Z **A MOTORWAY SCORES ZERO AND IS NOT GATED - and this branch can only assert half of that.** `Gates` lives on `task/T-0133` and is not on `main`, so the paired assertion is impossible here. `motorwayScoresZero` pins the half this file owns (all four dull classes score 0 given the *best possible* terms, and an otherwise identical `secondary` road scores 1.0, so the rule is about the class and not about the fixture). The gate half is pinned in T-0133. **The pairing test belongs wherever those two first meet on one branch, and does not exist yet** - said here rather than implied.
- 2026-09-09T01:00:00Z Thresholds pinned strictly and on both sides: a tunnel of exactly 300 m is *not* penalised while 301 m is; a road exactly 150 m from a motorway is *not* penalised while 149 m is. Both mutations that relax `>` to `>=` are caught. The multipliers **compound** rather than replacing one another (0.5 x 0.15 x 0.7), and the mutation making them mutually exclusive is caught.
- 2026-09-09T01:00:00Z Absent surface follows the plan and never becomes evidence of unpaved: `residential` with no `surface` tag takes x0.8 **and** raises the flag; `tertiary` takes neither; a `residential` road that *does* carry a surface tag takes neither. **CORRECTED IN ROUND 2 (reviewer F2): this was INCOMPLETE, not "follows the plan". The plan names `unclassified` as well as `residential`, and `primary`/`secondary` as well as `tertiary`; only one class on each side was exercised, so dropping `unclassified` from `unsurveyedClasses` changed nothing any test could see. All five are covered now.** The flag is a separate function from the score on purpose - the penalty is the scoring function's business and the flag is the hazard strip's, and a route can accumulate enough flagged distance to be worth a line on screen without any single way scoring badly.
- 2026-09-09T01:00:00Z Terms are **validated, not clamped**: a value outside `0...1`, a NaN, or a negative tunnel length returns nil. A plausible score computed from a wrong input is the hardest kind of error to find later, and `SegmentTerms.unitTerms` lists every unit term so a term added later cannot quietly escape validation - a mutation dropping one from that list is caught. **CORRECTED IN ROUND 2 (reviewer F1): that held for 3 of the 10 terms. The suite probed `curvature`, `canopy`, `water` and `tunnelMeters`, and the single mutation written against the list removed `water` - one of the ones it happened to probe. Seven terms could be deleted from `unitTerms` in silence. Measured: all seven MISSED at 3311563. Ten per-term mutations and a ten-term test now cover it.**
- 2026-09-09T01:00:00Z HARNESS `ops/mutate/segmentscore.py`: **31 of 31 caught by a named test**, 0 trapped, 0 compile-only, 0 MISSED, 0 skipped, exit 0. `--prove-vacuity` -> `caught=0 (need 0) and MISSED=31 of 31`, OK. Most mutations move a NUMBER, as the brief required - this is thirteen weights and two exponents, and a structural-only set would be blind exactly where it matters. Built on `ops/mutate/gates.py`, the only sibling carrying every correction (pass condition `caught == len(MUTATIONS)`, complete-MISSED vacuity over every test file, EQUIVALENT asserted the other way, and a **retried baseline build**).
- 2026-09-09T01:00:00Z **ONE MUTATION SCORED SKIP AND THE FIX WAS TO STOP ANCHORING ON A COMMENT.** "Swap the exponents" originally rewrote both declarations, which are separated by a doc comment, so the anchor spanned one - and CLAUDE.md forbids anchoring a guard on a comment precisely because a rewording breaks it silently. Re-expressed at the point of USE, `pow(m, sceneryExponent) * pow(e, driveExponent)`, which touches only identifiers. Caught.
- 2026-09-09T01:00:00Z **THE PLAN'S TWO WORKED NUMBERS ARE UNREPRODUCED, DELIBERATELY.** The plan quotes 0.32 for a curvy industrial road and 0.62 for a straight redwood one, but gives no term values that produce them. Reverse-engineering inputs to hit those targets and presenting the result as an oracle would be fitting the answer to the target - this repository already rejected an oracle for less (T-0011, where the first source disagreed with USNO). Every expected value here is hand-computed from the stated weights instead. The real oracle is the Curvature project's Vermont output, which arrives with the ETL.
- 2026-09-09T01:00:00Z NOT FIXED: `ops/mutate/segmentscore.py` is ~~318~~ **445** lines as of the round-2 commit. **CORRECTED IN ROUND 2 (reviewer F9): the line said 318 when the file was already 332 - commit 3311563 added 14 lines and the note was not refreshed. This number is the hand-off to T-0058 / PR #44, so it has to be the real one; re-measure it with `wc -l` before quoting it again.** The 300-line cap reaches Python only when T-0058 lands, so P-SRC-02 is green today and will not be then - flagged rather than pre-emptively split, since the split should follow whatever shape that task settles on. Same note as T-0119.
- 2026-09-09T01:00:00Z moved to review/. Reviewer must not be agent/claude-opus-5 (`ops/queue-check`).

### Round 2 - closing the review of PR #83 (verdict FAIL, reviewer agent/rv-pr83)

- 2026-09-15T19:00:00Z **SEVEN FINDINGS CLOSED, ONE NARROWED, TWO SURVIVORS REFUSED AS EQUIVALENT MUTANTS.** The measurement that frames all of it: the NEW mutation population, run unchanged against the OLD code at 3311563, scores **35 of 49 caught, 14 MISSED**. Run against this commit it scores **49 of 49, 0 MISSED, 0 skipped, 0 trapped, 0 compile-only**. The 14 are exactly the reviewer's coverage findings and no other verdict moved, so the before/after pair is the demonstration that every new assertion can fail. Full lists in `.artifacts/before-out.txt` and `.artifacts/after-out.txt` (gitignored).
- 2026-09-15T19:00:00Z **F1 CLOSED - validation covered 4 of the 10 unit terms, and the one mutation written against the list removed `water`, one of the 4.** New suite `Tests/ScenicKitTests/SegmentScoreValidationTests.swift`; `everyUnitTermIsValidated` walks all ten terms and refuses each at 1.5, -0.1, NaN, +inf and -inf, with each probe chosen so the score would be a finite, plausible number if the term escaped validation. **The term table is transcribed by hand, never read from `SegmentTerms.unitTerms`** - deriving the list of terms to test from the list under test makes the assertion true for any list, an empty one included, which is the cancellation this round was sent to hunt. The single `water` drop became ten generated per-term mutations. MEASURED: elevationGain, speedFit, sinuosity, relief, impervious, pointsOfInterest and furniture were all MISSED at 3311563 and are all caught here; curvature, canopy and water were already caught, which is why seven of the ten moved and not ten.
- 2026-09-15T19:00:00Z **F2 CLOSED - `unclassified`, half of the plan's absent-surface rule, had no test and no mutation.** `absentSurfaceRule` now loops both unsurveyed classes and all three assumed-paved ones, asserting the x0.8 and the flag on one side and neither on the other. Two mutations added: "unclassified stops taking the absent-surface penalty and the flag" (the mirror of the existing `dullClasses` one, which had no counterpart on this list) and "a primary road stops being assumed paved". Both MISSED at 3311563, both caught here.
- 2026-09-15T19:00:00Z **F3 CLOSED on the two halves that are real; REFUSED on the half that cannot change behaviour.** The whole `metersToNearestMotorway` guard could be deleted and `tunnelMeters.isFinite` dropped in silence. `tunnelLengthIsValidated` and `motorwayDistanceIsValidated` pin both, and pin the difference between the two guards on purpose: `.infinity` is the motorway field's DEFAULT and means "no motorway near", while an infinite tunnel is a broken measurement. Three mutations - guard deleted, negative distance accepted, infinite tunnel accepted - all MISSED at 3311563, all caught here. **REFUSED: "a NaN motorway distance is accepted" is NOT a coverage hole.** Every IEEE 754 comparison with NaN is false, so `NaN >= 0` is false and the remaining clause already refuses NaN; the `!isNaN` clause cannot change behaviour. It is in the EQUIVALENT arm with that proof, asserted to go MISSED, and it does. Banking it would have meant writing a test that reads the source rather than the behaviour.
- 2026-09-15T19:00:00Z **F4 CLOSED - the file's third threshold was pinned on neither side.** The other two (tunnel 300/301, proximity 150/149) were strict from the start; the `0...1` window was probed only at 1.5 and -0.1, far enough out that it could be widened to `0...1.25` - accepting a curvature of 1.2 - with all 30 tests green. `validationWindowIsExactlyZeroToOne` probes every term at `1.0`, `(1.0).nextUp`, `0.0` and `(0.0).nextDown`: the nearest representable Doubles either side, **a fact about IEEE 754 and not about the window under test**, so the probe cannot cancel against the code it is measuring. Two mutations, one widening and one narrowing; the widening was MISSED at 3311563 and is caught here.
- 2026-09-15T19:00:00Z **F5 CLOSED BY DELETION, which is the honest fix for a constant that implements nothing.** `assumedPavedClasses` was public, documented as the plan's rule, and read by nothing - `grep -rn assumedPavedClasses` over the tree returned its own declaration and nothing else, so it could be emptied or made to claim `residential` in direct contradiction of `unsurveyedClasses` with no behaviour changing. The reviewer was right to refuse to count those two survivors as coverage holes, and pinning a dead constant with a literal would have turned them green while leaving the rule stated twice, once inertly. It is gone. The plan's "primary/secondary/tertiary are treated as paved" half is what NOT being in `unsurveyedClasses` means; it is stated once, in that constant's doc comment, and pinned behaviourally for all three classes in `absentSurfaceRule`.
- 2026-09-15T19:00:00Z **F6 CLOSED - the test's name outran its assertion and its comment stated the opposite of the truth.** "curvature carries more of M than any other term" compared curvature with `sinuosity` alone, beside a comment claiming a weight swap "would leave `weightSetsSumToOne` green" - which it would not, since that test pins all ten weights as literals. `curvatureDominatesM` now asserts curvature against every other M term. The mutation attacks the **effective** weights at the point of use (`0.16 * t.curvature`, `0.49 * t.elevationGain`), where M still sums to 1.00 exactly and every literal stays green, so only the dominance claim can catch it. MEASURED at 3311563: that mutation failed **zero** named tests. Here it fails exactly one, "curvature carries more of M than any other term".
- 2026-09-15T19:00:00Z **F7 CLOSED BY A SECOND EQUATION, not by rewording. The reviewer was right and the round-1 claim was false.** `SM^a * SE^b == 1` is one equation in two unknowns, satisfied by a whole curve; the counterexample (M summing to 1.05, E to 1.05^(-a/b)) scores 1.0. The byway cap is the formula's only non-linearity and supplies the second equation: `SM^a * min(1, SE+B)^b == 1` with `B > 0` forces the cap to be active, hence `SE == 1`, and the first then forces `SM == 1`. All three are asserted inside `weightSetsSumToOne`, with `B > 0` established behaviourally - a byway below saturation must score strictly higher - rather than against the literal, because `B == 0` collapses the second equation back onto the first. MEASURED with the counterexample applied at the point of use: at 3311563 the named tests that went red were `bywayBonusIsCapped` and `motorwayScoresZero`, and **`weightSetsSumToOne` - the test whose comment made the claim - stayed GREEN**. Here it goes red, at 1.0172231953677011 against 1.0. **This was never a coverage hole and is not reported as one**: the mutation was already caught elsewhere. It was a claim wider than its assertion, and the claim is now true of the test that makes it.
- 2026-09-15T19:00:00Z F8 CLOSED. "water and canopy weights swapped" moved `canopyWeight` 0.24 -> 0.12 and never touched `waterWeight`, so sum(E) fell to 0.88 and it was behaviourally the entry above it. Replaced with a real sum-preserving swap, anchored across the six contiguous E-weight declarations, which carry no comment between them. It is caught by `weightSetsSumToOne`'s literals only, and that is the honest position: no behavioural test distinguishes canopy from water, and the literals are the pin. F9 and F10 closed - see the corrected line above, and `SCRATCH` moved from `.build-mutate-segmentscore`, which `.gitignore`'s `.build/` does not match, to `.artifacts/mutate-segmentscore`, which it does. The harness was leaving an untracked directory that made `ops/sane` count its own worktree as dirty.
- 2026-09-15T19:00:00Z **BRIEF ITEM "output always in 0...1", which the reviewer listed as NOT MET, is now met.** The top was pinned at one point by the byway cap and the bottom at one point by a zero axis; a range claim is about every point. `scoreAlwaysLandsInZeroToOne` walks 7x7 drive/scenery levels against both byway settings, four tunnel lengths, four motorway distances and six highway classes - 9,408 combinations, `7*7*2*4*4*6` - and asserts `0 <= score <= 1` against literals rather than against anything the formula computes. It counts the combinations it walked and asserts the count against the product of the literal list lengths, so emptying a fixture list cannot turn it into a test that passes having asserted nothing. DEMONSTRATED RED: under "the byway bonus is uncapped" the named tests that fail are `bywayBonusIsCapped`, `weightSetsSumToOne` and this one. Worth recording honestly: "exponents that no longer sum to one" does **not** make it red - with the exponents summing to 1.10 the score still never exceeds 1 - so this test is not the catcher for that mutation and is not claimed as one.
- 2026-09-15T19:00:00Z **THE VALIDATION SUITE IS DELIBERATELY SELF-CONTAINED.** It first called `SegmentScoreTests.uniform`, and the consequence only showed up when the TEST_FILES entry was demonstrated red: emptying the other file alone stopped this one compiling, so the harness refused with "baseline does not build" instead of reporting a vacuity failure. That is a refusal, not a measurement. The fixture builders are now a local copy, the RED demo produces a real `VACUITY PROOF FAILED` line, and the price is a duplicated four-line helper.
- 2026-09-15T19:00:00Z **HARNESS: the floor now refuses what its own comment says it refuses.** `MIN_MUTATIONS` was 28 against a population of 31, so three mutations could be deleted one at a time, each with a plausible reason, while the comment beside the number claimed to catch exactly that. Both floors are the real population now (49 and 2) and are demonstrated red below. `TEST_FILES` gained the new validation suite, without which `--prove-vacuity` leaves a catching suite standing and cannot reach caught=0 - also demonstrated red. The inherited `KNOWN_MISSED` arm was removed rather than left in place: `known` was never assigned, so the arm never ran and its guard passed for the wrong reason, which reads to the next agent like a third bucket being checked. The retried baseline build is present verbatim at both the baseline and the mutation site and was left alone; the I/O 512 symlink warning it exists for appeared again during this round's runs. **NOT CHANGED FROM HERE: `ops/mutate/gates.py` carries the same dead arm and the same un-ignored scratch path.** It belongs to another task; flagged, not touched.
- 2026-09-15T19:00:00Z **WHAT WAS NOT DONE, and why.** (a) The `!isNaN` clause in the motorway guard is redundant and stays as an EQUIVALENT mutant with the IEEE 754 proof, not as a closed finding - one reviewer this round correctly refused to bank three survivors for exactly this reason and the same standard applies to my own work. (b) `assumedPavedClasses` was deleted rather than pinned; if a later task wants the assumed-paved list as data it should arrive with a caller, not before one. (c) The motorway-scores-zero / `Gates.decide`-allows-it PAIR still cannot be asserted: `Gates` is on `task/T-0133` and not on `main`, so that is still half a test and still said here rather than implied. (d) `bash ops/test` still exits 1 at the `services/api` vitest step; `services/api/node_modules` is absent on this box and on `main`, this is [[T-0040]], and it is not attributed to this PR - the Swift tier is fully green and the failure reproduces unchanged on `main`. (e) The Python 300-line cap: the harness is well over it and gets no smaller here; that lands with T-0058 and is flagged, not pre-empted.
- 2026-09-15T19:00:00Z **THE ACCEPTANCE BLOCK WAS EMPTY AND IS NOW EIGHT LINES, EVERY ONE RE-RUN AGAINST THE COMMITTED TREE.** The reviewer had nothing to hold the PR to and fell back on the brief's own numbered list. Three of the eight are RED lines, because a check that has never been seen red is untested: the population floor refusing 48 against 49, the vacuity proof failing with `caught=27` when the new suite is left out of TEST_FILES, and the whole 49-mutation population scoring 35/49 against the reviewed commit. The two harness RED lines are edits to a tracked file, restored and verified by `git hash-object` (283afb3b, identical before and after both demos).
- 2026-09-15T19:00:00Z MECHANICAL, all in `.worktrees/T-0134` at this commit. `swift test --scratch-path .artifacts/swift` -> **35 tests in 5 suites passed**, exit 0. `python ops/mutate/segmentscore.py` -> **49 of 49 caught**, trapped 0, compile-only 0, MISSED 0, skipped 0; EQUIVALENT 2 of 2 MISSED; exit 0. `--prove-vacuity` -> **caught=0 (need 0), MISSED=49 of 49**, exit 0. `bash ops/check-pins` -> `PINS ok=12 skipped=0 pending=2 expired=0 failed=0 tier=linux`, exit 0. `bash ops/queue-check` -> `QUEUE OK (127 tasks)`, exit 0. `bash ops/test` -> Swift tier green, then `FAIL: services/api exists but vitest produced no report`, exit 1 - [[T-0040]], not this PR. `bash ops/sane` -> `SANE FAIL exit=10`, and the only line naming T-0134 is this branch being unpushed, which the push below clears; `_dupprobe`, `_t0075probe` and `T-0104` belong to other agents. **`untracked:0` across all 103 worktrees, where the round-1 harness left `.build-mutate-segmentscore/` behind - that is F10 measured rather than asserted.** Line counts: SegmentScore 129, SegmentTerms 95, SegmentScoreTests 280, SegmentScoreValidationTests 159 - all under the 300 cap, which `ops/lib/check-line-cap` reads from `git ls-files`, so the new file counts only once committed. `ops/mutate/segmentscore.py` is 445.
- 2026-09-15T19:00:00Z **RESTORATION DISCIPLINE.** Every demo that edited a tracked file restored it and the restore was verified by hashing, not by trusting a `finally`: the two harness RED demos compare `git hash-object` before and against after and abort if it differs, and the four mutation demos that measure which named tests go red compare the file's bytes against the pre-run snapshot. Two agents were interrupted mid-demo this week and left a mutation on disk, which is why the check is a comparison and not an assumption. The measurements against the pre-review code were run in a **separate detached worktree** (`.worktrees/T0134-before` at 3311563) whose only change was one untracked file; no tracked file in any worktree I do not own was touched.
