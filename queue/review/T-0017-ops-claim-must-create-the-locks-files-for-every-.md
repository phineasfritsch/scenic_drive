---
id: T-0017
title: ops/claim must create the LOCKS files for every exclusive: resource, and refuse if one is held
state: review
owner: agent/claude-opus-5
owner_session: 01SS4jAGs2oyr4Z4Wd8yK82t
claimed_at: 2026-09-07T07:15:18Z
lease_expires_at: 2026-09-07T10:15:18Z
worktree: ../wt/T-0017
branch: task/T-0017
exclusive: []
touches: [ops/lib/queue.py, ops/lock, queue/, queue/README.md]
pins_affected: []
reviewer: agent/reviewer-8
depends_on: []
verify: [ops/test, ops/check-pins]
acceptance: []
---
## Brief

Two queue-mechanics defects, both observed on 2026-09-07:

1. `ops/claim` accepts a task declaring `exclusive: [x]` without creating `queue/LOCKS/x.lock`. The operator has
   to remember. `ops/queue-check` catches it afterwards, but the lock is the thing that prevents a concurrent
   write, so catching it late is catching it wrong. FIX: `claim` creates every declared lock atomically and
   refuses the claim if any is already held; `sweep` releases them (it already does).

2. `next_id()` scans only the CURRENT worktree, so two branches allocate the same id. Observed: T-0015 was
   created on task/T-0007 (exec bits) and again on task/T-0011 (this task) because the first was unmerged.
   `queue-check` would flag the duplicate only after both merged. FIX: allocate ids against `origin/main`
   (`git ls-tree -r --name-only origin/main queue/`) plus the local tree, and fetch first.

RED for 1: claim a task with `exclusive: [floors]` -> LOCKS/floors.lock must exist and a second claim of another
task declaring the same resource must be refused. RED for 2: create a task on two branches from the same base
and show the ids differ.

## Log
- 2026-09-07T07:15:18Z claimed by agent/claude-opus-5; lease until 2026-09-07T10:15:18Z
- 2026-09-07T08:20:00Z DEFECT 1 WAS MIS-DIAGNOSED BY ME. I wrote that `ops/claim` accepts a task with `exclusive:` without creating the lock. It does not. Proved empirically: claiming a task declaring [floors, prod] creates both lock files, and a second claim of a task wanting `floors` is refused with exit 1. The brief was wrong.
- 2026-09-07T08:20:00Z THE REAL GAP: `exclusive:` edited AFTER a task is claimed has no lock and no way to acquire one - which is exactly what I did on T-0011. queue-check flags it, but only after the window where a concurrent write could be lost. FIX: new `ops/lock <task> [--owner x]` acquires declared-but-unheld locks for a claimed task, refuses if another task holds one, and appends to the task's ## Log.
- 2026-09-07T08:20:00Z RED/GREEN for ops/lock: edited `exclusive: [floors]` onto a claimed task -> queue-check exit 1 naming the missing lock; `ops/lock T-00xx` -> "acquired [floors]" and queue-check exit 0. A second task declaring the same resource -> `cannot lock floors: held by ...`, exit 1.
- 2026-09-07T08:20:00Z DEFECT 2 IS REAL and now fixed: next_id() scanned only the current worktree. Proof with a genuinely pushed branch (tmp/idprobe, since the fix only helps once a branch is pushed - the case that actually bit us on T-0015): branch A allocated and pushed T-0019; a second branch off origin/main allocated T-0019 with the OLD code (collision) and T-0020 with the NEW code. Probe branches deleted locally and on origin afterwards.
- 2026-09-07T08:20:00Z Residual, stated honestly: two agents allocating offline at the same instant can still pick the same id. The push is the compare-and-swap that settles it, exactly as for claims; documented in the _ids_in_refs docstring. _ids_in_refs is best-effort and offline-safe (fetch and ls-tree failures are swallowed, falling back to whatever refs exist).
- 2026-09-07T08:20:00Z Filed T-0019 for reviewer-5's finding that P-OPS-01 passes vacuously on an empty ops/ - not fixed here because pins/PINS.yaml is outside this task's touches.
- 2026-09-07T08:20:00Z moved to review/, reviewer agent/reviewer-6
- 2026-09-07T09:18:47Z reviewed by agent/reviewer-6: FAIL — re-ran and CONFIRMED correct: (1) the mis-diagnosis correction is accurate (probed with two fresh `exclusive: [floors]` ready tasks: claiming the first created `queue/LOCKS/floors.lock`, claiming the second exited 1 with "cannot claim, locks held: floors (held: ...)"); (2) `ops/lock` behaves exactly as specified for every case tested: post-claim `exclusive:` edit -> queue-check exit 1 (missing lock) -> `ops/lock <id> --owner <owner>` acquires (exit 0, prints "acquired [floors]", appends "acquired lock(s) floors for <owner>" to ## Log) -> queue-check exit 0; a second claimed task wanting the same resource is refused with "cannot lock floors: held by <holder>" exit 1; guards confirmed: ready/-state and review/-state (not claimed/) both refuse with "is in <state>/, not claimed/", wrong `--owner` refuses with "is owned by X, not Y", and a task with no `exclusive:` is a true no-op (exit 0, "declares no exclusive resources", no spurious ## Log line since `log()` is only called when `taken` is non-empty). (3) next_id/_ids_in_refs collision fix CONFIRMED against a real pushed branch: created branch tmp/rev6probe off origin/main in a throwaway worktree, allocated+committed+pushed T-0019 there with the OLD (pre-fix, unmodified) queue.py; from a second throwaway worktree off origin/main, the same OLD code allocated T-0019 again (collision reproduced); after copying in the NEW queue.py, the same worktree's next_id() correctly saw T-0019 via the fetched `refs/remotes/origin/tmp/rev6probe` and allocated T-0020 instead. Both probe branches deleted locally and on origin afterward (`git ls-remote --heads origin` shows no rev6probe refs). ops/test -> TESTS linux=50/50 ios=skipped failed=0 (exit 0); ops/check-pins -> PINS ok=9 skipped=0 pending=3 expired=0 failed=0 (exit 0); ops/queue-check -> QUEUE OK (19 tasks) (exit 0); `git ls-files -s ops/lock` -> 100755, correctly committed executable.
- 2026-09-07T09:18:47Z FAIL FINDING 1 — ops/lib/queue.py:100-127 (`_ids_in_refs`): the fallback is silent even on a definite, non-network-flake failure, and neither subprocess call's returncode is ever checked (only bare exceptions are caught). Proved it: ran `_ids_in_refs()`/`next_id()` with `HTTPS_PROXY`/`HTTP_PROXY` pointed at an unreachable localhost port. `git fetch --quiet --all` under that env exits 128 with `fatal: unable to access '...': Failed to connect ... Could not connect to server` (confirmed by running the same fetch directly) - a hard, unambiguous, immediately-detectable failure, not "offline." `_ids_in_refs()` swallowed it with zero output and `next_id()` proceeded to print a plain id as if the scan had succeeded, using whatever stale `refs/remotes/*` happened to already be cached locally. The docstring's stated residual risk is "two agents allocating offline at the same instant" - a real fetch error (bad proxy/VPN hiccup/auth failure/git-not-found) is a different, more common failure mode that gets the exact same silent treatment, undermining the fix's own purpose (stale-data collisions, which is what caused the original T-0015 bug) in precisely the case that matters most. FIX: after the `git fetch` and after the `for-each-ref`/`ls-tree` calls, check `.returncode` in addition to catching exceptions, and on any non-zero returncode or caught exception print one line to stderr, e.g. `warning: could not refresh remote refs (id collision protection may be using stale data): <stderr text>` - cheap, and lets an agent decide whether to retry before trusting the allocated id.
- 2026-09-07T09:18:47Z FAIL FINDING 2 — queue/README.md: the protocol section never mentions `ops/lock`. Step 3 ("Claim") only documents claim-time lock creation ("creates `LOCKS/<resource>.lock` for each `exclusive:` entry"); nothing in steps 1-8 tells an operator that a POST-claim edit to `exclusive:` (the exact T-0011 scenario this task exists to fix) requires running `ops/lock <id> --owner <owner>` before `queue-check` will pass. `ops/lock` is documented only in `ops/lib/queue.py`'s module docstring (queue.py:9) and its own function docstring (queue.py:286-291), not in the operator-facing README, which this task's own brief calls "the amnesiac operator's brief" - the one doc a context-less future agent is expected to read. Grep confirms: `grep -in lock queue/README.md` finds no mention of the `ops/lock` command anywhere. FIX: add a step (e.g. 3a, after the existing Claim step) to queue/README.md: "If `exclusive:` is edited on an already-claimed task, `ops/lock <id> [--owner agent/x]` acquires the newly-declared locks (refuses if another task already holds one); `ops/queue-check` will flag the gap in the meantime." Also worth a one-line mention in the `queue/LOCKS/` bullet at the top.
- 2026-09-07T09:18:47Z Non-blocking notes for the record (not required to re-review, but worth folding in opportunistically): (a) `bash ops/new-task "!!!" --state backlog"` produces filename `T-0020-.md` (empty slug) but the regex `/(T-(\d+))-` still matches it because `cmd_new`'s `f"{tid}-{slug}.md"` template always inserts a literal `-` after the id even when `slug` is empty - tested directly, not a defect. (b) timing: `ops/new-task` now costs ~1.5-1.6s per call on this repo (11 remote refs today: fetch ~1.1s network-bound + ~0.035-0.04s per `ls-tree` subprocess spawn); extrapolated to 50 remote refs that is roughly 2.5-3s, growing linearly and unpruned - confirmed the growth vector is real, not hypothetical: origin still carries a leftover `tmp/merge-probe` branch from a past review session that nobody deleted (left untouched per the never-delete-a-branch-you-did-not-create rule). (c) `_ids_in_refs` (queue.py:114, `refs/remotes` with no remote-name filter) scans every configured remote, not `refs/remotes/origin` specifically, which is broader than the brief's own "against origin/main" wording and than the README's origin-is-the-single-source-of-truth model (`git reset --hard origin/main` is the documented compare-and-swap); harmless today only because this repo happens to have exactly one remote configured.
- 2026-09-07T10:30:00Z reviewer-6 FAILED this task on two counts. Both fixed:
- 2026-09-07T10:30:00Z FIX 1 (high): _ids_in_refs swallowed every git failure, so a network hiccup silently degraded id allocation to a local-only scan with no warning - the exact failure mode the fix exists to guard against. Now every subprocess return code is checked and any failure prints a one-line stderr WARNING naming what failed and that a duplicate id is possible. Verified: normal run emits 0 stderr lines; with HTTPS_PROXY pointed at a dead port the run still succeeds but prints "WARNING: next_id could not fetch remotes (fatal: unable to access ...)".
- 2026-09-07T10:30:00Z FIX 2 (medium): queue/README.md step 4 now documents ops/lock for the post-claim exclusive: case, including that it refuses a lock held by another task and that queue-check fails until it is held.
- 2026-09-07T10:30:00Z reviewer-6's two informational findings are ACKNOWLEDGED, NOT FIXED, deliberately: (a) scanning all remotes rather than origin only is harmless with one remote and strictly safer if a second is added; (b) ops/new-task now costs ~1.5 s and grows with stale remote branches - real, but the alternative is a stale local cache, and pruning branches is a separate concern. Say so if you disagree.
- 2026-09-07T10:30:00Z LIVE EVIDENCE for why this task matters, from today: after merging T-0016 I ran ops/new-task twice on main and it allocated T-0019 and T-0020 - but T-0019 was already taken on this very branch, unmerged. main still runs the old local-only next_id(). I renumbered by hand to T-0021/T-0022 (commit a2283ad). The bug reproduced itself while its own fix sat in review.
- 2026-09-07T10:30:00Z re-review requested from agent/reviewer-8
