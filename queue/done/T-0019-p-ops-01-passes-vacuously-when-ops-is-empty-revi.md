---
id: T-0019
title: P-OPS-01 passes vacuously when ops/ is empty (reviewer-5 finding)
state: done
owner: agent/claude-opus-5
owner_session: 01SS4jAGs2oyr4Z4Wd8yK82t
claimed_at: 2026-09-07T10:04:12Z
lease_expires_at: 2026-09-07T13:04:12Z
worktree: ../wt/T-0019
branch: task/T-0019
exclusive: []
touches: [pins/PINS.yaml, ops/lib/check-exec-bits, ops/lib/check-line-cap]
pins_affected: [P-OPS-01]
reviewer: agent/reviewer-13
depends_on: []
verify: [ops/test, ops/check-pins]
acceptance: []
---
## Brief

agent/reviewer-5 found this while re-reviewing T-0015. P-OPS-01's assertion is

    test -z "$(git ls-files -s ops .githooks | awk ...)"

If `git ls-files -s ops .githooks` returns NOTHING - because someone deleted `.githooks/pre-commit`, or renamed
the directories - the awk output is empty, `test -z ""` is true, and the pin reports OK. It passes on an empty
artefact, which CLAUDE.md forbids in as many words.

FIX: require a minimum file count as well, e.g. prefix the assertion with
    test "$(git ls-files ops .githooks | wc -l)" -ge 17 &&
or better, assert the presence of the specific load-bearing files (ops/test, ops/sane, ops/check-pins,
.githooks/pre-commit, .githooks/commit-msg) so a rename cannot silently empty the set.

RED: `git rm --cached -r .githooks` (or move ops/ aside) and show ops/check-pins still reports P-OPS-01 OK today,
then fails after the fix. Restore afterwards.

This is the same class as any other vacuous guard: sweep the other pins for it in the same task.

## Log
- 2026-09-07T10:04:12Z claimed by agent/claude-opus-5; lease until 2026-09-07T13:04:12Z
- 2026-09-07T12:10:00Z The one-liner assertion was `test -z "$(git ls-files -s ops .githooks | awk ...)"`. Over an EMPTY file list awk prints nothing, `test -z ""` is true, and the pin reported OK - so a deleted .githooks/ or a renamed ops/ would have read as "all modes correct" forever. That is the "never weaken an assertion to something that passes on an empty artefact" rule, violated by the pin meant to enforce discipline.
- 2026-09-07T12:10:00Z Moved the logic into ops/lib/check-exec-bits so the pin is one readable line and the checks can carry their own error messages. Three checks now: (1) at least 17 tracked files under ops/ and .githooks/; (2) all 14 load-bearing scripts actually tracked by name, so a rename cannot empty the set; (3) modes - scripts 100755, data (*.json/*.txt/*.md and ops/api-url) 100644.
- 2026-09-07T12:10:00Z GREEN: "P-OPS-01: 22 files, 14 required present, all modes correct"; ops/check-pins -> PINS ok=9 pending=3 failed=0.
- 2026-09-07T12:10:00Z RED 1: chmod -x ops/sane -> "ops/sane (script, should be 100755, is 100644)", exit 1.
- 2026-09-07T12:10:00Z RED 2: chmod +x ops/lib/ro_cases.json -> "(data, should be 100644, is 100755)", exit 1. A blanket chmod +x still cannot satisfy the pin.
- 2026-09-07T12:10:00Z RED 3: git rm --cached ops/merge -> "load-bearing script(s) not tracked: ops/merge", exit 1. A rename or deletion is caught by name, not just by count.
- 2026-09-07T12:10:00Z RED 4 (the vacuous case this task exists for): ran the script in a fresh throwaway git repo containing only itself -> "only 1 files tracked ... An empty or truncated set must never read as 'all modes correct'", exit 1. Under the OLD assertion that same situation returned OK.
- 2026-09-07T12:10:00Z moved to review/, reviewer agent/reviewer-10
- 2026-09-07T14:30:00Z reviewed by agent/reviewer-10: FAIL — re-ran everything: `bash ops/lib/check-exec-bits` -> "P-OPS-01: 22 files, 14 required present, all modes correct" exit 0; `bash ops/check-pins` -> "PINS ok=9 skipped=0 pending=3 expired=0 failed=0" exit 0; reproduced RED a (chmod=-x ops/sane -> "should be 100755, is 100644", restored, git status clean), RED b (chmod=+x ops/lib/ro_cases.json -> "should be 100644, is 100755", restored, clean), RED c (git rm --cached ops/merge -> "load-bearing script(s) not tracked: ops/merge"; git add + chmod=+x restored blob f17ddeda... at 100755, clean). RED d (vacuous case) reproduced two ways in a throwaway repo: (1) truly empty ops/.githooks tree -> OLD assertion `test -z "$(git ls-files -s ops .githooks | awk ...)"` reconstructed from `git show main:pins/PINS.yaml` returns exit 0 (vacuous OK), NEW script returns exit 1 "only 0 files tracked"; (2) literal "containing only itself" setup (ops/lib/check-exec-bits alone, chmod +x'd before commit, matching the log's "only 1 files tracked") -> OLD assertion also returns exit 0, NEW script returns exit 1. Both confirm the log's central claim. `bash ops/queue-check` -> QUEUE OK. `git ls-files -s ops/lib/check-exec-bits` -> 100755. All of that holds — but two in-scope defects block PASS:
  1. pins/PINS.yaml:22 (P-SRC-02, "no Swift source file exceeds 300 lines") has the IDENTICAL vacuous-pass bug: `test -z "$(find Sources Tests -name '*.swift' -exec awk 'NR>300{print FILENAME; exit}' {} \;)"` is trivially true when `find` matches nothing (Sources/Tests deleted, renamed, or emptied) — confirmed by running the same construct against nonexistent dirs: exit 0. The brief for this task says in as many words "This is the same class as any other vacuous guard: sweep the other pins for it in the same task," and pins/PINS.yaml was already in this task's touches — the sweep was not done and the identical defect ships unfixed in the very same file. Fix: give P-SRC-02 the same treatment (require `find Sources Tests -name '*.swift'` to itself return a non-empty, plausible-size list before trusting an empty violations output, or route it through a small checked script the way P-OPS-01 now is).
  2. ops/lib/check-exec-bits:15-16 REQUIRED omits `ops/queue-next`, even though it is tracked 100755 and queue/README.md:19 documents it as the second step of the core queue workflow ("Pick: `ops/queue-next` prints the next `ready/` task...") — the same tier as `ops/queue-check`/`ops/queue-sweep`/`ops/claim`, all three of which ARE in REQUIRED. As written, a rename or deletion of `ops/queue-next` is invisible to the by-name check that is this pin's whole point ("a rename cannot silently empty the set") and would only ever be caught incidentally if the total tracked count fell below MIN_FILES=17 (a 5-file cushion below today's 22, not a name-anchored guarantee). Fix: add `ops/queue-next` to REQUIRED.
  Non-blocking notes for the fixer, no action required to re-pass: (a) MIN_FILES=17 vs current count 22 is a coarse floor with only 5 files of slack, but REQUIRED is the real name-anchored guarantee so this is fine as a secondary check; (b) the data-file regex `\.(json|txt|md)$` would misclassify a future ops/*.yaml (data) or ops/*.md (script) — no such file exists today, latent only; (c) CRLF-broken-but-100755 is already covered independently by `.gitattributes` (`ops/** text eol=lf`, `.githooks/** text eol=lf`, verified: a CRLF file added under ops/ is stored as LF in the blob) and by `ops/agent-preflight`'s repo-wide `grep -lI "\r"` scan plus its own `.gitattributes` integrity check — P-OPS-01 does not need to duplicate that.
- 2026-09-07T14:00:00Z agent/fixer-1, FIX 1 (reviewer-10 finding #2): added `ops/queue-next` to the REQUIRED array in ops/lib/check-exec-bits (queue/README.md:19 documents it as the "Pick" step, same tier as ops/queue-check/ops/queue-sweep/ops/claim which were already listed; a rename or deletion of it was previously invisible to the by-name check). GREEN: `bash ops/lib/check-exec-bits` -> "P-OPS-01: 23 files, 15 required present, all modes correct", exit 0 (23 not 22 because ops/lib/check-line-cap, added for FIX 2, is now also tracked under ops/).
- 2026-09-07T14:00:00Z agent/fixer-1, FIX 2 (reviewer-10 finding #1): pins/PINS.yaml's P-SRC-02 assertion had the identical vacuous-pass bug as the old P-OPS-01 one-liner (`test -z "$(find Sources Tests -name '*.swift' -exec awk 'NR>300{print FILENAME; exit}' {} \;)"` is trivially true when `find` matches nothing, e.g. a deleted/renamed/emptied Sources or Tests tree). Verified the real tracked count first: `git ls-files 'Sources/**/*.swift' 'Tests/**/*.swift' | wc -l` -> 9. Moved the logic into a new script ops/lib/check-line-cap (git ls-files instead of find, so untracked build junk is never counted and the check matches what is actually committed), which (a) requires >= 5 tracked .swift files under Sources/ and Tests/ before trusting an empty violations list, (b) fails naming any tracked .swift file over 300 lines with its line count, (c) prints a one-line summary on success, same style as check-exec-bits. Made it executable in git: `chmod +x ops/lib/check-line-cap && git update-index --add --chmod=+x ops/lib/check-line-cap`. Pointed P-SRC-02's assertion at `bash ops/lib/check-line-cap`. GREEN: `bash ops/lib/check-line-cap` -> "P-SRC-02: 9 Swift files tracked, none over 300 lines", exit 0.
- 2026-09-07T14:00:00Z agent/fixer-1, RED for the line cap: `python -c "open('Sources/ScenicKit/TooLong.swift','w').write('// x\n'*301)"`, `git add Sources/ScenicKit/TooLong.swift`, then `bash ops/lib/check-line-cap` -> "P-SRC-02: file(s) over the 300-line cap:\n  Sources/ScenicKit/TooLong.swift (301 lines)", exit 1. Restored: `git rm -q --cached Sources/ScenicKit/TooLong.swift && rm Sources/ScenicKit/TooLong.swift`; `git status --short Sources/` clean afterward.
- 2026-09-07T14:00:00Z agent/fixer-1, RED for the vacuous case: in a throwaway dir, `git init` a repo containing ONLY a copy of ops/lib/check-line-cap (committed 100755, nothing under Sources/ or Tests/), then `bash ops/lib/check-line-cap` -> "P-SRC-02: only 0 tracked .swift file(s) under Sources/ and Tests/ (expected >= 5).\n  An empty or truncated set must never read as 'no file exceeds 300 lines'.", exit 1 — confirms the new check fails hard on the exact vacuous case the OLD assertion would have passed. Temp dir removed afterward.
- 2026-09-07T14:00:00Z agent/fixer-1, full re-verify: `bash ops/check-pins > /tmp/p.log 2>&1; echo $?` -> 0, log reads "PINS ok=9 skipped=0 pending=3 expired=0 failed=0 tier=linux"; `bash ops/queue-check` -> "QUEUE OK (30 tasks)", exit 0; `git ls-files -s ops/lib/check-line-cap ops/lib/check-exec-bits` -> both 100755.
- 2026-09-07T14:00:00Z agent/fixer-1: reviewer-10 was right on both counts, and specifically right that this task's own brief said in as many words "sweep the other pins for it in the same task" — pins/PINS.yaml was already in T-0019's touches, P-SRC-02 sat right next to P-OPS-01 in that same file, and the first pass fixed P-OPS-01's vacuous-pass bug without sweeping the pin one entry down that had the exact same defect. P-SRC-02 shipped to review with the identical bug the task existed to remove. Both FIX 1 and FIX 2 above are applied; state/reviewer left untouched and file left in queue/review/ for re-review per instructions.
- 2026-09-07T14:35:00Z Fixes were applied by agent/fixer-1 and then RE-VERIFIED BY HAND rather than accepted on its report - a worker does not grade its own work, and that includes a worker I delegated to. Independently reproduced: 301-line file -> "P-SRC-02: file(s) over the 300-line cap: Sources/ScenicKit/TooLong.swift (301 lines)", exit 1; throwaway repo containing only the script -> "only 0 tracked .swift file(s) ... expected >= 5", exit 1; green after both -> "9 Swift files tracked, none over 300 lines". check-exec-bits now reports 23 files, 15 required present. ops/check-pins -> PINS ok=9 failed=0.
- 2026-09-07T14:35:00Z re-review requested from agent/reviewer-13
- 2026-09-07T14:40:00Z The pre-commit hook refused the first attempt: "ops/lib/check-line-cap is outside T-0019 touches". Correct - agent/fixer-1 created a new file that this task had never declared, and the hook caught it at commit time rather than after the fact. Widened touches: to declare it, which is the honest fix; bypassing the hook would have been the other option and is exactly the habit these guards exist to prevent.
- 2026-09-07T14:55:31Z reviewed by agent/reviewer-13: PASS — independently re-verified both of reviewer-10's majors, treating fixer-1's and the owner's own claims as unverified until reproduced. FIX 2 (ops/lib/check-exec-bits:15): `grep -n queue-next` shows it in REQUIRED; `bash ops/lib/check-exec-bits` -> "P-OPS-01: 23 files, 15 required present, all modes correct" exit 0. FIX 1 (pins/PINS.yaml P-SRC-02 now `bash ops/lib/check-line-cap`; read the script in full): GREEN "P-SRC-02: 9 Swift files tracked, none over 300 lines" exit 0 (9 confirmed independently via `git ls-files 'Sources/**/*.swift' 'Tests/**/*.swift' | wc -l`); RED over-cap reproduced with a fresh 301-line Sources/ScenicKit/TooLong.swift staged (not committed) -> named the file "(301 lines)" exit 1, restored (`git rm --cached` + `rm`), `git status --short` clean; RED vacuous reproduced in a throwaway `git init` repo containing only a copy of the script -> "only 0 tracked .swift file(s) ... expected >= 5" exit 1, temp dir removed. Old-assertion vacuity independently proved, not just trusted: reconstructed `test -z "$(find Sources Tests -name '*.swift' -exec awk 'NR>300{print FILENAME; exit}' {} \;)"` verbatim from `git show main:pins/PINS.yaml` and ran it in an empty temp dir with no Sources/Tests -> exit 0 despite find erroring "No such file or directory" on both, confirming the task's premise held. 300/301 boundary: a fresh 300-line file (300 trailing-newline-terminated lines) -> PASS exit 0; a 301-line file -> FAIL exit 1 naming "(301 lines)"; both cleaned up after. `bash ops/check-pins` -> exit 0, "PINS ok=9 skipped=0 pending=3 expired=0 failed=0"; `bash ops/queue-check` -> "QUEUE OK (30 tasks)" exit 0; `git ls-files -s ops/lib/check-line-cap ops/lib/check-exec-bits` -> both 100755. Process check: touches: history across the branch's own commits (293039a -> add284c -> 548b4b7) shows it was genuinely widened step by step to [pins/PINS.yaml, ops/lib/check-exec-bits, ops/lib/check-line-cap], which is exactly the file set the branch's commits touch (`git diff --name-only $(git merge-base main HEAD) HEAD`, excluding the always-allowed queue/* self-file) — hook was not bypassed. Both of reviewer-10's MAJORs are fixed and hold up under independent reproduction. Adversarial notes for a follow-up task, none block this PASS (none reproduce the empty/renamed-tree vacuous-pass class this task targets): (a) ops/lib/check-exec-bits:15-17 REQUIRED lists only top-level entrypoints — ops/lib/queue.py (backs 6 REQUIRED wrappers: claim/lock/new-task/queue-check/queue-next/queue-sweep), ops/lib/pins.py (backs ops/check-pins), ops/lib/junit_count.py (backs ops/test), ops/lib/ro_grammar.py (backs ops/prod-read), and check-exec-bits/check-line-cap themselves are load-bearing but unlisted by name, so their deletion is only caught by the blunt MIN_FILES=17-vs-23 floor, not the by-name guarantee; (b) ops/lib/check-line-cap counts only git-tracked files (`git ls-files`) — a staged-but-uncommitted oversized file IS caught (verified), but a genuinely untracked one is invisible (verified: an untracked 400-line Sources/ScenicKit file passed clean), and since .githooks/pre-commit never invokes check-line-cap/check-pins itself, nothing forces a re-check between staging and commit — a real narrowing versus the old `find`-based scan, which counted all files on disk regardless of tracking, traded for correctly excluding build junk; (c) `wc -l` undercounts a file missing a trailing newline on its last line by exactly 1 — verified: 300 newline-terminated lines + one more line with no trailing "\n" (301 lines by any editor/compiler count) reads as 300 via `wc -l` and passes clean; no `.gitattributes` or other check enforces a final newline; (d) confirmed empirically in a scratch repo that `git ls-files 'Sources/**/*.swift' 'Tests/**/*.swift'` only matches paths rooted at the repo top level, not nested ones — once T-0010 (still backlog, apps/ios/ does not exist yet) lands apps/ios/Packages/ScenicApp/{Sources,Tests}, its Apple-side Swift files will be silently exempt from the unconditional P-SRC-02 statement ("No Swift source file exceeds 300 lines"); flag for T-0010's acceptance criteria or a fast-follow, untestable now since the directory doesn't exist. `git status --short` clean throughout and at handoff.
