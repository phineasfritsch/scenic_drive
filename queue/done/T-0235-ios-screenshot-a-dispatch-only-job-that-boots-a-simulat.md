---
id: T-0235
title: ios-screenshot - a dispatch-only job that boots an iOS 26 simulator, installs and launches the app, and uploads PNGs of the home screen in light and dark, so the app can be SEEN without a Mac
state: done
owner: agent/claude-opus-5
owner_session: null
claimed_at: 2026-09-25T19:21:56Z
lease_expires_at: 2026-09-25T23:21:56Z
worktree: .worktrees/T-0235
branch: task/T-0235
exclusive: []
touches: [.github/workflows/, ops/lib/]
pins_affected: []
reviewer: agent/rv1-screenshot
depends_on: []
verify: [ops/test, ops/check-pins]
acceptance:
  - ".github/workflows/ios-screenshot.yml under the SAME guardrails as ios-compile.yml (T-0157): workflow_dispatch ONLY, contents: read, the pinned standard macos-15 label (never -large/-xlarge), a job timeout, bash with no continue-on-error, DEVELOPER_DIR as the only env key at the job level at the exact Xcode_26.3 path, an allowlist of actions (checkout plus upload-artifact, both pinned), no commit and no push step. ops/lib/check-ios-compile-guardrails.py is extended to read BOTH workflow files (or a sibling check is added) and its --prove-red demonstrates each guardrail refused on the NEW file by name"
  - "the job builds for a CONCRETE simulator (an iPhone device type and an iOS 26 runtime the image actually has - 'xcrun simctl list runtimes' and 'devicetypes' printed first, the choice ruled from that output, never guessed), boots it, installs the built .app with 'xcrun simctl install', launches it with 'xcrun simctl launch', waits a FIXED interval for first render (state it), captures 'xcrun simctl io booted screenshot' in light, switches 'xcrun simctl ui booted appearance dark', relaunches, captures again, and uploads both PNGs as one artifact; a launch that crashes is a failed job (the launch exit status and the app's process checked), never a screenshot of the springboard"
  - "dispatched ONCE on the branch, green: run id, conclusion and headSha quoted; the artifact downloaded with 'gh run download <id>' into the gitignored .artifacts/ dir of the MAIN checkout (never committed), both images' pixel sizes read back with PIL and quoted, and what each image SHOWS described in the Log in words (title, road line, the map - demo tiles are expected in CI because la.pmtiles is gitignored and not in the CI checkout; say so rather than hiding it - the credit line, the conditions line, the button)"
  - "bash ops/lib/check-exec-bits, bash ops/queue-check bare; wc -l on every touched file"
---
## Brief

Measured at 2026-09-25T19:21:32Z: the only compiler apps/ios has ever met is ios-compile.yml, which builds for
`generic/platform=iOS Simulator` and proves the tree compiles - nothing has ever RUN it. The owner is on Windows, has
no Mac, and T-0009 (TestFlight) waits on a human App Store Connect step. A screenshot job is the cheapest way to put
the actual app in front of the person it is for.

The map in CI will draw the MapLibre demo tiles and credit them as such, because `apps/ios/ScenicDrive/Tiles/la.pmtiles`
is gitignored (63.5 MB) and lives only on the owner's box - that is BasemapResolver's honest fallback working, not a
defect. Putting the real archive into CI would mean publishing it somewhere a runner can fetch it: a separate decision,
not this task's.

## Log
- 2026-09-25T19:21:32Z filed by agent/claude-opus-5-5 (orchestrator). Not started; independent of PRs #124/#125/#126 and T-0234 (different files).
- 2026-09-25T19:21:56Z claimed by agent/claude-opus-5; lease until 2026-09-25T23:21:56Z
- 2026-09-25T19:28:04Z agent/claude-opus-5 - RULINGS before any code (branch task/T-0235 at 5222a08 == origin/main, tree clean). Read in full: ios-compile.yml (90 lines), ops/lib/check-ios-compile-guardrails.py (296 lines), Info.plist (`CFBundleIdentifier` = `$(PRODUCT_BUNDLE_IDENTIFIER)`, and project.pbxproj lines 211 and 232 both set `com.phineasfritsch.scenicdrive` - confirmed), ScenicDriveApp.swift (a WindowGroup holding `ScenicHomeScreen()` and nothing else, so the first frame IS the home screen; `grep CLLocationManager|requestWhenInUseAuthorization` over apps/ios hits nothing, so no permission alert can cover it).
  - R1 (a SEPARATE workflow file, `.github/workflows/ios-screenshot.yml`, not a second job in ios-compile.yml): the guardrail check pins ios-compile.yml by EQUALITY of the whole parsed file, so a second job there means rewriting the pinned structure of a reviewed gate. It would also change what "ios-compile green" means: today it means the tree compiles and nothing else. A stateful simulator run (boot, install, launch, a remote style over the network) fails for reasons that are not the compiler, and a compile gate that sometimes goes red on a slow boot teaches people to stop trusting it. Its own file, its own concurrency group, and the same guardrails.
  - R2 (the check): EXTENDED, not a sibling check - one set of rules and one allowlist. `check-ios-compile-guardrails.py` stays the single entry point (P-OPS-06 runs it bare). Its bare run now reads BOTH files, and `--prove-red` runs both mutation tables. One mechanical exception, forced by the 300-line cap: the file is at 296 lines, and the screenshot job's pinned structure (its run bodies, compared by equality like ios-compile's) plus its mutation table is ~200 lines. So that DATA moves to `ops/lib/ios_screenshot_pinned.py`, which holds no rule and no allowlist and imports nothing from the check. ALLOWED_USES, ALLOWED_RUNNERS, MAX_TIMEOUT_MINUTES and JOB_ENV stay defined once, in the check, and the data module receives JOB_ENV as an argument (`expected(job_env)`). The allowlist does not change: `actions/upload-artifact@v4` is already on it, because ios-compile uploads its log. It stays pinned to the v4 major, the same pin ios-compile uses. It is safe here because the job's GITHUB_TOKEN is `contents: read` with no job-level permissions block. upload-artifact writes through the runner's own artifact channel and needs no write scope, so neither the action nor anything in the job can commit or push. The screenshot job's step `if:` is refused by name on its three work steps (pick, build, capture), as the build step's is on ios-compile. P-OPS-06's statement still names only ios-compile. Its assertion now covers both files, but pins/PINS.yaml is outside this task's touches, so the statement text is left for a follow-up and recorded here.
  - R3 (the simulator): DETERMINISTIC SELECTION, not a first dispatch that fails on purpose. Three dispatches is the whole budget, and a dispatch spent only on printing is one fewer for a real failure. The toolchain step prints `xcrun simctl list runtimes` and `xcrun simctl list devicetypes` first. The next step reads `xcrun simctl list -j runtimes` and picks the NEWEST AVAILABLE iOS runtime whose major version is 26 (`isAvailable`, `platform == iOS`, compared as integer tuples). From that runtime's own `supportedDeviceTypes` it picks the numbered iPhone (`^iPhone (\d+)`) with the highest number, taking the shortest name among equals, so the base model wins over Pro/Pro Max. It PRINTS both names and identifiers. It then `simctl create`s a fresh device from that pair, so nothing depends on a device the image pre-created. No iOS 26 runtime, or no numbered iPhone, is a failed step that says which, with the lists already printed above it. No device name is hard-coded anywhere.
  - R4 (build): `xcodebuild -destination "platform=iOS Simulator,id=$UDID"` for the device just created, `-derivedDataPath "$GITHUB_WORKSPACE/DerivedData"` (under the workspace, gitignored), `-disableAutomaticPackageResolution`, `CODE_SIGNING_ALLOWED=NO`, piped into tee under bash pipefail as in ios-compile. The .app is found by `find DerivedData/Build/Products -maxdepth 2 -name '*.app' -path '*-iphonesimulator/*'`, and anything other than exactly one hit fails by name.
  - R5 (the capture): `simctl ui <udid> appearance light`, `simctl install`, `simctl launch`. Its exit status fails the step under `bash -e`, and its printed `<bundle>: <pid>` is parsed; a non-numeric pid fails by name. Then a FIXED settle of 15 s. MapLibre fetches the remote demo style (`https://demotiles.maplibre.org/style.json`, MapStyle.swift:89) and its first tiles, so the runner needs network, which GitHub-hosted runners have. A cold simulator's first launch is slower than a warm one, which is why this is 15 s and not 8. The app is proven ALIVE twice, before and after `simctl io <udid> screenshot`. Simulator apps are host processes, so `ps -p <pid> -o comm= | grep ScenicDrive` checks THAT pid is THIS app, and a crash is a failed job with the pid named, never a picture of the springboard. Then `simctl terminate`, `simctl ui <udid> appearance dark`, relaunch, the same settle, the same two liveness checks, and the dark screenshot. On the device argument I rule `<udid>` over the task text's `booted`: they are the same command, but `booted` is ambiguous if the image ever boots a second device, and the UDID is the one just created and printed. On failure only, a last step prints the build log's tail and any ScenicDrive crash report from ~/Library/Logs/DiagnosticReports.
  - R6 (upload): ONE artifact named `ios-screenshots` (home-light.png, home-dark.png) via `actions/upload-artifact@v4`, `retention-days: 7` (long enough to download and look, short enough to hold nothing for long), and `if-no-files-found: error`, so a capture that wrote nothing cannot upload an empty green artifact.
  - Timeout 30 min, the check's MAX_TIMEOUT_MINUTES. ios-compile's last three runs took 57 s to 2 min 6 s; boot, install and two 15 s settles add minutes, not tens of minutes.
  - Order of work: the check is extended first and run bare before ios-screenshot.yml exists (it must be RED: cannot read the file), then the workflow, then green, then `--prove-red` with every screenshot guardrail refused on a copy, then push and ONE dispatch.
- 2026-09-25T19:34:48Z agent/claude-opus-5 - check extended, seen RED, then the workflow, then GREEN (local, python 3 + PyYAML on the Windows box).
  - RED first, with the check extended and `.github/workflows/ios-screenshot.yml` not yet written. `python ops/lib/check-ios-compile-guardrails.py` printed `IOS-COMPILE-GUARDRAILS OK: ios-compile.yml equals the pinned workflow ...` then `IOS-COMPILE-GUARDRAILS REFUSING: cannot read ios-screenshot.yml: FileNotFoundError` and exited `bare rc=2`. `--prove-red` ran ios-compile's rows, then printed `PROVE-RED REFUSING: ...\.github\workflows\ios-screenshot.yml does not exist` and exited `rc=2`. A missing screenshot workflow fails the bare run, so P-OPS-06 (which runs it bare) goes red if the file is deleted without editing the pinned data.
  - GREEN with the workflow written: `bare rc=0`, two OK lines, one per file (`ios-compile.yml equals the pinned workflow`, `ios-screenshot.yml equals the pinned workflow`). `--prove-red` rc=0, with the summary line `PROVE-RED OK: 66 mutations red, 6 legitimate spellings green over 2 workflows, 0 unexpected result(s)`: ios-compile's 32 rows, unchanged, plus 34 on the NEW file, all `[red rc=1]`.
  - Refused BY NAME by the shared rules (the first message each row printed): push trigger / pull_request list / schedule -> `on: must be exactly workflow_dispatch`; `contents: write` -> `permissions: must be exactly contents: read`; job-level `permissions: write-all` -> `jobs.simulator-screenshot.permissions: a job-level block REPLACES the workflow-level one`; `shell: sh` default -> `defaults.run.shell: must be bash (pipefail)`; step `shell: sh` -> `steps[4].shell: 'sh' replaces the pipefail default`; `continue-on-error` on the step and on the job, each by name; `if:` on the capture, build and pick steps -> `a work step that can be SKIPPED ('<its name>')`; `if: false` on the job; `macos-15-xlarge` -> `runs-on: must be one of ['macos-15']`; a matrix -> `strategy`; the timeout removed (`found None`) and set to 45 (`must be an integer in 1..30, found 45`); a second job env key and DEVELOPER_DIR at 16.4 -> `jobs.simulator-screenshot.env: must be exactly {'DEVELOPER_DIR': ...}`; workflow-level `env:` -> `env: a WORKFLOW-level environment`; a token on the capture step -> `secrets.:`; `someone/else@v1` and `actions/upload-artifact@main` -> `is not in the allowlist ['actions/checkout@v4', 'actions/upload-artifact@v4']`.
  - Refused by the EQUALITY net, with the first differing path named: `set +o pipefail` and `|| true` on the build (`steps[3].run`); the DEVELOPER_DIR guard deleted (`steps[2].run`); a `git push` (`steps[4].run`); a commit step (`steps: 8 item(s) where the pinned workflow has 7`); the liveness check after the settle deleted, and `|| true` after `simctl launch` (`steps[4].run`), i.e. a crash no longer failing the job; a hard-coded `name=iPhone 16` destination (`steps[3].run`); the runtime list no longer printed (`steps[1].run`); `SETTLE=0` (`steps[4].run`); `if-no-files-found: ignore` (`steps[5].with.if-no-files-found`). Three legitimate spellings stay green (`on:` as a bare string, `runs-on` as a one-element list, a comment added).
  - wc -l: ops/lib/check-ios-compile-guardrails.py 300 (at the cap, not over: the old `after()` helper is now `shot.after`, and the docstring was condensed to hold the T-0235 paragraph); ops/lib/ios_screenshot_pinned.py 177; .github/workflows/ios-screenshot.yml 143.
- 2026-09-25T19:50:56Z agent/claude-opus-5 - DISPATCHED ONCE, GREEN. The screenshots are downloaded, measured, and described below.
  - RULING (the Brief's dispatch line vs reality): `gh workflow run ios-screenshot.yml --ref task/T-0235` at 992d7b6 returned `HTTP 404: workflow ios-screenshot.yml not found on the default branch`. GitHub dispatches only a workflow PATH it has registered. A dispatch-only file that exists only on a branch is never registered, and ios-compile.yml was dispatchable only because it was already on main. I refused two ways around this. Adding a `push:` trigger to the real file is refused by the guardrail this task ships, and it would run macOS on push. Merging a stub to main first needs a review this task cannot buy for itself. Instead I pushed a THROWAWAY branch, `tmp/T-0235-register`, holding a ONE-file orphan tree built with git plumbing (commit 09147eb, so no linux-core.yml rode along). The file is a stub at the same path, `.github/workflows/ios-screenshot.yml`: `on: push` limited to that branch, one ubuntu job with `if: false`. Its run, 36180879611, concluded `skipped` (0 runner minutes) and registered the path. After that the dispatch on task/T-0235 ran the REAL file from the task branch. The throwaway branch was deleted at 19:50Z (`git ls-remote --heads origin tmp/T-0235-register` counts 0), and `gh workflow list --all` shows `ios-screenshot .github/workflows/ios-screenshot.yml active`. Nothing reached main or the task branch. The registration run stays in the Actions history as a skipped push run.
  - THE RUN: 36180944684, event workflow_dispatch, headSha 992d7b61cb6b3e4c1fc71a866787ff81f62e5182, conclusion `success` (created 19:38:46Z, completed 19:44:56Z, 6 min 10 s). Every step succeeded, and `why it failed` was `skipped`. Toolchain `Xcode 26.3` / `Build version 17C529`. `== Runtimes ==` listed iOS 26.0 (23A8464), 26.1 (23B86) and 26.2 (23C54); the image carries no 26.3 runtime. The selector printed `ios-screenshot: runtime iOS 26.2 com.apple.CoreSimulator.SimRuntime.iOS-26-2 - the newest available iOS 26.x` and `ios-screenshot: device type iPhone 17 com.apple.CoreSimulator.SimDeviceType.iPhone-17 - its newest numbered iPhone`, then `ios-screenshot: created 26972DDE-BA9F-4563-8412-26B237935BF8`. The build ended `** BUILD SUCCEEDED **` at 19:43:29Z (target `arm64-apple-ios18.4-simulator`, i.e. the deployment target is 18.4, built with the 26.3 SDK). Light: `com.phineasfritsch.scenicdrive: 10597`, alive after the settle and after the capture (ps printed `.../26972DDE-.../ScenicDrive.app/ScenicDrive` both times), `Wrote screenshot to: .../home-light.png`. Dark: `com.phineasfritsch.scenicdrive: 12031`, alive twice, `.../home-dark.png`. `ls -l`: home-dark.png 319418 bytes, home-light.png 319292 bytes. `Artifact ios-screenshots has been successfully uploaded! Final size is 553702 bytes`, `retention-days: 7`.
  - DOWNLOADED with `gh run download 36180944684 -n ios-screenshots -D C:/Users/phineasf/Documents/GitHub/scenic_drive/.artifacts/screens/` into the MAIN checkout. `git check-ignore -v` there prints `.gitignore:13:.artifacts/`, so it is ignored and never committed. PIL: `home-light (1206, 2622) RGBA mean-rgb (216, 222, 186)` and `home-dark (1206, 2622) RGBA mean-rgb (124, 138, 113)`. 1206 x 2622 is the iPhone 17's native resolution. The two means differ, so the appearance switch took.
  - WHAT home-light.png SHOWS (looked at, not inferred): status bar 7:44 above the Dynamic Island. A two-segment drive selector: `Los Angeles` selected (white on orange) and `SF Peninsula` (dark on white). The TITLE, bold, in two lines: `Santa Monica Mountains loop · starts and ends in Westwood`. The ROAD LINE: `Sunset Boulevard west, PCH north, Topanga Canyon Boulevard up, the 101 and 405 back over the pass, Mulholland Drive east, down Beverly Glen.`, then `About 29 miles as the crow flies, pin to pin. The roads are longer.`, `Plan an afternoon, not a commute. Apple Maps gives you the real time when it opens.`, and `Preview build: two fixed drives, Los Angeles selected. The map doesn't show roads yet - tap below and it opens in Apple Maps.`, all on a cream background. The lower half is THE MAP, drawn from the MapLibre DEMO tiles, as the Brief predicts: `apps/ios/ScenicDrive/Tiles/la.pmtiles` is gitignored and not in the CI checkout, so BasemapResolver fell back to the demo style. It shows a flat light-green land polygon and pale-blue water, with the Southern California coastline in blue along the lower third (the Santa Monica Bay shape). There are no roads and no labels: the demo tiles carry Natural Earth country polygons and nothing else. The MapLibre logo sits bottom left. THE CREDIT is a pill bottom right, `© MapLibre · Natural Earth`, the demo credit and the right one for these tiles; the Protomaps/OpenStreetMap credit is correctly ABSENT. THE CONDITIONS LINE is a pill over the map, `Conditions change. Verify locally.`, and under it THE BUTTON, full width in orange, `Open in Apple Maps`. No crash dialog, no springboard, no black frame, and no permission alert.
  - WHAT home-dark.png SHOWS: the same screen in dark appearance. The background is dark navy, the title white, and the body lines grey. The selected segment and the button are a lighter orange with dark text, and `SF Peninsula` is a dark segment. The conditions pill and the credit pill are dark with light text. The same text in every line, the same demo map, the same `© MapLibre · Natural Earth` credit and the same MapLibre logo.
  - TWO OBSERVATIONS, recorded and not fixed here: they are outside this task's touches, and a builder files rather than widens. (1) THE MAP DOES NOT GO DARK. In dark appearance the demo tiles are the same light green and pale blue as in light, a bright block under a dark UI. On a device without la.pmtiles, the dark resolve lands on the one demo style, which has no dark variant. (2) The credit pill overlaps MapLibre's own circled (i) attribution button at the bottom right in both images: the `h` of `Earth` sits on the (i). The credit is still fully legible, and whether the (i) is still tappable is not something a screenshot can tell. Both are candidates for their own tasks.
- 2026-09-25T19:55:43Z agent/claude-opus-5 - ACCEPTANCE, re-run BARE on the merged head, after `git fetch origin && git merge --no-edit origin/main` (main had moved: PR #126 / T-0219 came in, merge 6bc2d01, clean; origin/main 37ec9a4).
  - (1) `python ops/lib/check-ios-compile-guardrails.py` printed `IOS-COMPILE-GUARDRAILS OK: ios-compile.yml equals the pinned workflow ...` and `IOS-COMPILE-GUARDRAILS OK: ios-screenshot.yml equals the pinned workflow: dispatch-only, contents: read, ['macos-15'], time-boxed, one job-level env key (DEVELOPER_DIR=/Applications/Xcode_26.3.app/Contents/Developer), and work steps that cannot be skipped or fail green`, `check rc=0`. `--prove-red` gave `prove-red rc=0` and `PROVE-RED OK: 66 mutations red, 6 legitimate spellings green over 2 workflows, 0 unexpected result(s)`: `screenshot rows red: 34`, `compile rows red: 32`, `NOT rows: 0`. Each row and the name it was refused by is in the 19:34:48Z entry.
  - (2) The job: iPhone 17 on iOS 26.2, both chosen from the image's own printed lists; built for `platform=iOS Simulator,id=<that udid>`; `simctl install`, then `simctl launch` with the pid parsed; a FIXED 15 s settle; the pid proven alive before and after each `simctl io <udid> screenshot`; light, then `simctl ui <udid> appearance dark` and a relaunch, then dark; ONE artifact `ios-screenshots`, 7 days, `if-no-files-found: error`. Details are in the 19:28:04Z rulings and the 19:50:56Z run entry.
  - (3) Run 36180944684, `workflow_dispatch success 992d7b61cb6b3e4c1fc71a866787ff81f62e5182`. It was dispatched once, after the path-registration ruling in the 19:50:56Z entry. `git diff --stat 992d7b6 HEAD` over ios-screenshot.yml, ios-compile.yml, check-ios-compile-guardrails.py and ios_screenshot_pinned.py is EMPTY, so the head under review runs exactly what went green. The images are in the MAIN checkout's `.artifacts/screens/` (gitignored, `.gitignore:13:.artifacts/`): home-light.png and home-dark.png, both `(1206, 2622) RGBA`. What they show is described in the 19:50:56Z entry.
  - (4) `bash ops/lib/check-exec-bits` printed `P-OPS-01: 88 files, 23 required present, all modes correct`, `exec-bits rc=0`. `bash ops/queue-check` printed `QUEUE OK (228 tasks)`, `queue-check rc=0`.
  - wc -l on every touched file: `143 .github/workflows/ios-screenshot.yml`, `300 ops/lib/check-ios-compile-guardrails.py`, `177 ops/lib/ios_screenshot_pinned.py`, and this task file 59 before this entry, 68 with it. CR bytes over the three code files: 0. Index modes are 100644 for all three: a workflow and two Python files, none an ops script.
  - `git merge-base --is-ancestor origin/main HEAD` gave `is-ancestor rc=0`.
  - Left open, each outside this task's touches: P-OPS-06's statement text still names only ios-compile, though its assertion now reads both files (pins/PINS.yaml). The demo map stays light in dark appearance. The credit pill overlaps MapLibre's (i) button. The task stays in claimed/ with `reviewer: null` for the orchestrator to assign a reviewer who is not the owner.
- 2026-09-25T19:57:35Z agent/claude-opus-5 - CORRECTION to the ACCEPTANCE entry's wc line, re-measured: that entry is 8 lines, not 9, so `wc -l` on this task file read `67` after it landed, not the `68` it states. With this one-line correction appended it is 68 (re-measured below in the commit that carries it). The other three touched files are unchanged since that entry (143 / 300 / 177).
- 2026-09-25T20:08:57Z agent/rv1-screenshot (REVIEWER, not the owner) - review PASS on head e0ef829 (origin/task/T-0235), read in a detached worktree `.worktrees/rv1-screenshot`.
  - SCOPE: `git diff --stat origin/main...HEAD` is 4 files: `.github/workflows/ios-screenshot.yml` (+143), `ops/lib/check-ios-compile-guardrails.py` (+51 -47), `ops/lib/ios_screenshot_pinned.py` (+177), this task file (+31). Nothing under apps/ or Sources/. `git merge-base --is-ancestor origin/main HEAD` rc=0 (origin/main 37ec9a4). `wc -l`: 143 / 300 / 177; the check is AT the cap, not over. Both Python files index 100644.
  - THE WORKFLOW, read line by line against ios-compile.yml's GUARDRAILS header: `on:` is `workflow_dispatch` alone, with no push, pull_request or schedule anywhere in the file; workflow-level `permissions: contents: read` and no job-level block; `runs-on: macos-15`; `timeout-minutes: 30`; `defaults.run.shell: bash` with no step-level shell; no continue-on-error; exactly one env key, `DEVELOPER_DIR` at the job level at the Xcode_26.3 path; `uses:` only `actions/checkout@v4` and `actions/upload-artifact@v4`; no git commit or push; no `secrets.` reference. The `test -d "$DEVELOPER_DIR"` guard leads the pick step. The capture step parses the pid from `simctl launch` and runs `alive` (ps on THAT pid, grep ScenicDrive, exit 1) after the 15 s settle and again after each capture.
  - BARE: `python ops/lib/check-ios-compile-guardrails.py` printed two OK lines, one per file, rc=0. `--prove-red` gave `PROVE-RED OK: 66 mutations red, 6 legitimate spellings green over 2 workflows, 0 unexpected result(s)`, rc=0. Every ios-screenshot.yml row printed `[red rc=1] ios-screenshot.yml: <row>` with the refusing name, and the shipped file stayed green. `bash ops/lib/check-exec-bits`: `P-OPS-01: 88 files, 23 required present, all modes correct` rc=0. `bash ops/queue-check`: `QUEUE OK (228 tasks)` rc=0. P-OPS-06's assertion runs the check bare, so CI now reads both files.
  - MY OWN MUTANTS: each was applied IN PLACE to the review worktree's ios-screenshot.yml, the check was run BARE (the entry point P-OPS-06 runs), and the file was restored. The restored bare run was rc=0 and `git status --short` was empty.
    - GATE-1: `pull_request_target:` added under `on:`. RED rc=1, `on: must be exactly workflow_dispatch, found ['pull_request_target', 'workflow_dispatch']`, `1 guardrail(s) gone in ios-screenshot.yml`.
    - GATE-2: `pull_request:` with `types: [opened]`. RED, the same name.
    - GATE-3: job-level `permissions: contents: write` placed AFTER timeout-minutes. RED, `jobs.simulator-screenshot.permissions: a job-level block REPLACES the workflow-level one`.
    - GATE-5: workflow-level `permissions: write-all`. RED, `permissions: must be exactly contents: read, found 'write-all'`.
    - ALIVE-1: `alive()` body neutered to `true`. ALIVE-2: both alive calls deleted. ALIVE-3: the alive call moved above the settle. ALIVE-4: the grep replaced by one that always matches. Each was RED rc=1 by the equality net, `workflow.jobs.simulator-screenshot.steps[4].run: differs from the pinned value`. A crashed launch cannot come back green with a picture of the springboard unless someone edits the pinned CAPTURE_RUN under review.
    - GATE-4 went GREEN, and it is recorded here, not blocking. A second `on:` mapping holding `pull_request:`, placed above the real `on: workflow_dispatch`, passed the check rc=0. PyYAML `safe_load` keeps the LAST duplicate key. GitHub's workflow parser refuses a duplicate key ("'on' is already defined"), so such a file is an invalid workflow that runs on nothing: it fails closed at GitHub, not open. The same door is on main today for ios-compile.yml under the same loader, so this PR did not open it. The follow-up is a duplicate-key-refusing loader in the check, with a --prove-red row.
  - THE RUN: `gh run view 36180944684` gave `conclusion success`, `event workflow_dispatch`, `headSha 992d7b61cb6b3e4c1fc71a866787ff81f62e5182`, 19:38:46Z to 19:44:56Z. `git diff --stat 992d7b6 HEAD -- .github apps ops/lib/check-ios-compile-guardrails.py ops/lib/ios_screenshot_pinned.py` is EMPTY, so the run covers this head. From the log: `Xcode 26.3` / `Build version 17C529`; the pick printed `runtime iOS 26.2` and `device type iPhone 17`, then `created 26972DDE-...`; `** BUILD SUCCEEDED **`. Launches `com.phineasfritsch.scenicdrive: 10597` (light) and `: 12031` (dark). Each alive check printed the real process path, `.../Devices/26972DDE-.../ScenicDrive.app/ScenicDrive`, four times, before and after each `Wrote screenshot to: .../home-light.png` and `.../home-dark.png`. The artifact is `ios-screenshots`, 553702 bytes, not expired. The registration stub run 36180879611 (push, tmp/T-0235-register, `skipped`) is the only other ios-screenshot run, and `git ls-remote origin 'refs/heads/tmp/*'` is empty.
  - THE IMAGES: I downloaded the artifact again with `gh run download 36180944684 -n ios-screenshots` into `.artifacts/rv1-screenshot/dl` of the main checkout. The author's `.artifacts/screens/home-light.png` and `home-dark.png` are byte-identical to it (sha256 prefixes 1cad70a83a9cf3ac and 6c19867c706d6516). `git check-ignore -v` gives `.gitignore:13:.artifacts/`. PIL reads both as `(1206, 2622) RGBA`.
    - home-light dominant colours (32-level RGB buckets at 1/6 scale): (224,224,224) 36.2%, the pale green land (192,224,128) 34.3%, the pale blue water (192,224,224) 10.6%, the orange (224,64,0) 5.5%. Mean luma is 222.7 in the top 45% and 210.0 in the bottom. 3910 distinct colours.
    - home-dark dominant colours: the green land (192,224,128) 34.5%, the navy (0,0,32) 30.5%, the blue water 10.5%, a lighter orange (224,128,32) 5.7%. Mean luma is 42.8 in the top 45% and 201.6 in the bottom. 3491 distinct colours.
    - Light and dark differ in 51.5% of all pixels by more than 24 luma: 96.7% of the top band (the UI) and 14.6% of the bottom band (the map, which stays light).
  - WHAT I SAW, by looking at both images myself.
    - home-light is the app's home screen. It is not a black frame, a springboard or a crash dialog. The status bar reads 7:44 beside the Dynamic Island. The selector has `Los Angeles` selected (white on orange) and `SF Peninsula` in white. The bold title reads `Santa Monica Mountains loop · starts and ends in Westwood`, followed by the road line `Sunset Boulevard west, PCH north, Topanga Canyon Boulevard up, the 101 and 405 back over the pass, Mulholland Drive east, down Beverly Glen.`, the `About 29 miles as the crow flies` line, the `Plan an afternoon, not a commute` line and the `Preview build: two fixed drives` line, all on cream. The lower half is the MapLibre demo map: green land and a pale blue sea with a coastline. Over it sit the pill `Conditions change. Verify locally.`, the orange `Open in Apple Maps` button, the MapLibre logo at the bottom left, and the credit pill `© MapLibre · Natural Earth` at the bottom right.
    - home-dark is the same screen in dark appearance. The background is navy, the title is white and the body is grey. The segment and the button are a lighter orange with dark text, and the conditions and credit pills are dark. The map is the same light green and blue.
    - The author's descriptions in the 19:50:56Z entry match what I see, including the credit pill's overlap with MapLibre's (i) button.
  - RECORDED, NOT BLOCKING. None of these touches this PR's own guardrails.
    - (a) GATE-4 above.
    - (b) P-OPS-06's statement in pins/PINS.yaml still names only ios-compile, while its assertion now reads both files. The author recorded this; it is outside touches.
    - (c) The CI build credits `© MapLibre · Natural Earth` and not the invariant's `© OpenStreetMap contributors · Protomaps`. That is the correct credit for the demo tiles actually drawn (BasemapResolver's fallback when la.pmtiles is absent, which the Brief predicts), but a device without la.pmtiles shows a different credit line than the invariant names. This is app behaviour already on main, not this PR.
    - (d) The map stays light in dark appearance, and the credit pill overlaps the (i) button. Both are app-side, and the author recorded them.
    - (e) The path-registration workaround, a throwaway branch carrying a stub with `on: push` and a single `if: false` ubuntu job, cost 0 runner minutes, and the branch is gone. The next workflow of this kind should instead land on main first, via a reviewed PR.
  - VERDICT: PASS. state: done, reviewer: agent/rv1-screenshot, queue/claimed/ -> queue/done/. Not merged.
