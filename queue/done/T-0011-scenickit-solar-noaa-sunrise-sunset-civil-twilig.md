---
id: T-0011
title: ScenicKit Solar: NOAA sunrise/sunset/civil twilight/golden hour, 20 NOAA-checked fixtures (P-SAFE-05)
state: done
owner: agent/claude-opus-5
owner_session: 01SS4jAGs2oyr4Z4Wd8yK82t
claimed_at: 2026-09-07T04:21:42Z
lease_expires_at: 2026-09-07T08:21:42Z
worktree: ../wt/T-0011
branch: task/T-0011
exclusive: [floors]
touches: [Sources/ScenicKit/Solar/, Sources/ScenicKit/Model/, Tests/ScenicKitTests/, Tests/Fixtures/solar/, pins/PINS.yaml, pins/floor_linux.txt]
pins_affected: [P-SAFE-05]
reviewer: agent/reviewer-3
depends_on: []
verify: [ops/test, ops/check-pins]
acceptance: []
---
## Brief

(what, why, and the exact demonstration that proves it — including the red run)

## Log
- 2026-09-07T05:10:00Z backlog -> ready: no dependencies; M3 work that is independent of the data pipeline. pins/PINS.yaml added to touches to make P-SAFE-05 real.
- 2026-09-07T04:21:42Z claimed by agent/claude-opus-5; lease until 2026-09-07T08:21:42Z
- 2026-09-07T06:00:00Z ORACLE REJECTED AND REPLACED. First fixtures came from sunrise-sunset.org; our values disagreed with it by 60-620 s on rise/set while civil twilight matched to 2-4 s - the signature of a zenith mismatch, not a bug. Cross-checked three sites against the US Naval Observatory: OUR values match USNO within 5-34 s (SF rise +5 s, Fairbanks rise +24 s, Key West set -13 s) while sunrise-sunset.org was off by 84-343 s. The oracle was wrong, not the code. Refetched all fixtures from USNO (35 site-days, 21N-65N, 138 instants) and kept the rejection in the fetcher docstring + oracle.json as the evidence trail.
- 2026-09-07T06:00:00Z GREEN: swift test -> 16 tests in 3 suites passed; worst error 33 s at fairbanks 2026-12-21 dusk against a 90 s tolerance (USNO rounds to the minute, +/-30 s; our model ~35 s). ops/test -> TESTS linux=50/3 ios=skipped failed=0. ops/check-pins -> PINS ok=8 pending=3 failed=0.
- 2026-09-07T06:00:00Z RED 1 (the math): horizonZenith 90.833 -> 90.0, a plausible 'simplification' -> sunset fixtures fail, worst error 1548 s; restored, green again.
- 2026-09-07T06:00:00Z RED 2 (the oracle): rewrote oracle.json's provenance to 'my own code, trust me' -> P-SAFE-05 fails (PINS ok=7 failed=1). The pin asserts the fixtures come from USNO, not just that the numbers match, so regenerating fixtures from our own output cannot make it green.
- 2026-09-07T06:00:00Z polar cases assert nil, never a fabricated time: Utqiagvik midnight sun and polar night return nil sunrise/sunset (and nil civil dusk under the midnight sun).
- 2026-09-07T06:00:00Z P-SAFE-05 is no longer pending: assertion checks USNO provenance + >=20 fixtures + the fixture suite passing.
- 2026-09-07T06:00:00Z floor_linux raised 3 -> 50 (3 geo + 13 solar + 34 worker). Raising a floor is the reviewer's ratchet; verify the number before merging.
- 2026-09-07T06:00:00Z moved to review/, reviewer agent/reviewer-2
- 2026-09-07T06:05:00Z SELF-REPORTED PROCESS SLIP: I added `exclusive: [floors]` to this task to cover the floor_linux raise but never created queue/LOCKS/floors.lock while it was in claimed/. ops/queue-check caught it (and P-PROC-01 went red with it) for the minutes between the edit and the move to review/; it is green now only because the exclusive check applies to claimed/ tasks. The check worked; the operator did not. Nothing else held that lock, so no write was actually lost. Reviewer: treat this as evidence the lock discipline needs to be part of ops/claim rather than a thing a human remembers - filed as T-0017.
- 2026-09-07T07:40:00Z reviewed by agent/reviewer-3: PASS. Re-ran everything in worktree ../wt/T-0011 (HEAD 010f6df), touched only this task file, tree left clean (`git status --short` empty).
  (1) `bash ops/test` -> `TESTS linux=50/50 ios=skipped failed=0 skipped=0` / OK, exit 0. Counted the 50 myself: swift's own JUnit report (`.artifacts/spm-junit-swift-testing.xml`) lists 16 `<testcase>` entries - 3 Geo + 7 SolarMath + 6 SolarFixtureTests function-level tests (a parameterized test with N arguments counts as ONE testcase in this report, not N - e.g. `sunrise(fx:)` with 35 fixtures is 1 entry) - plus vitest's own `numTotalTests=34` from `.artifacts/vitest.json`. 16+34=50 exactly. 50 is the TRUE total under this repo's counting convention (JUnit testcase rows); it is not an over- or under-count relative to what ops/test measures, though it understates actual assertion volume (worstCase alone compares 138 individual instants inside one "testcase"). floor_linux.txt=50 matches current total exactly - correct ratchet.
  (2) `bash ops/check-pins` -> `PINS ok=8 skipped=0 pending=3 expired=0 failed=0` (plus `tier=linux`, additive, harmless), exit 0. Confirmed pins/PINS.yaml:92-99 P-SAFE-05 has no `pending:` line, and its assertion literally runs `swift test --filter SolarFixtureTests` plus greps for 'Naval Observatory' and a >=20-fixture count - all three conjuncts are real, not decorative.
  (3) RED math: `cp` SolarEvents.swift to /tmp, changed `horizonZenith` 90.833->90.0, `swift test --filter SolarFixtureTests` -> exit 1, "Test run with 6 tests in 1 suite failed... 71 issues", worst error 1548.9s at fairbanks 2026-06-21 sunset (matches the owner's own RED-1 log entry). Restored from /tmp, reran -> exit 0, 6/6 passed. git status clean throughout.
  (4) RED oracle: `cp` oracle.json to /tmp, edited `"primary"` to remove "Naval Observatory", `bash ops/check-pins` -> `ok=7 ... failed=1`, P-SAFE-05 listed as the failure. Restored from /tmp -> `ok=8 failed=0`. git status clean throughout.
  (5) CENTRAL QUESTION - independently verified the USNO oracle with my own fresh `urllib` calls (not reading oracle.json), reproducing fetch_oracle.py's exact `tz = round(longitude/15)` convention, for 3 fixtures at different latitudes: honolulu 2026-06-21 (21.3N, tz=-11), denver 2026-12-21 (39.7N, tz=-7), fairbanks 2026-09-22 (64.8N, tz=-10). All 12 values (sunrise/sunset/civil-begin/civil-end x 3 sites) I fetched live from https://aa.usno.navy.mil/api/rstt/oneday and converted to UTC myself matched the committed strings in Tests/ScenicKitTests/SolarFixtures.swift EXACTLY, to the second, e.g. honolulu sunrise 15:50Z sunset 05:16Z(+1d) civilBegin 15:26Z civilEnd 05:41Z(+1d); denver sunrise 14:18Z sunset 23:39Z civilBegin 13:47Z civilEnd 00:09Z(+1d); fairbanks sunrise 15:35Z sunset 03:51Z(+1d) civilBegin 14:45Z civilEnd 04:40Z(+1d). No fabrication found - the oracle is real and the fetcher's tz convention is exactly as documented.
  (6) Adversarial on tests: (a) an all-nil `compute` cannot pass - `check()` (SolarFixtureTests.swift:26-38) records an Issue for `(nil, .some)`, and 33/35 fixtures have real (non-nil) sunrise/sunset, so this is caught, confirmed by reading the switch and by the RED-1 run's 71 real issues. (b) an empty fixture list cannot pass - `fixtureCoverage()` (SolarFixtureTests.swift:40-45) asserts `count >= 20` directly, and P-SAFE-05's own assertion independently greps SolarFixtures.swift for >=20 `SolarFixture(name:` literals, so two independent guards exist. (c) parameterized tests are real, not decorative - confirmed by the RED-1 run producing 35 genuine per-fixture issues on `sunrise`/`sunset`. worstCase (SolarFixtureTests.swift:59-79) printed "compared 138 instants, worst 33 s at fairbanks 2026-12-21 dusk" on the green run - matches the owner's log exactly. 138 = 35 fixtures x 4 events - 2 nils (fairbanks 2026-06-21 civil fields). `compared >= 80` is a real, if loose (138 actual vs 80 floor), floor - it would catch fixtures collapsing to mostly-nil. (d) tolerance: 90s vs 33s actual worst gives ~57s headroom over USNO's +/-30s rounding + ~35s model error: reasonable, not decorative - the zenith-mutation RED produced 1548s (~17x tolerance), so the tolerance is tight enough to catch a real regression while accommodating legitimate noise. Minor/non-blocking observation: the 57s of headroom could in principle mask a smaller systematic error than the zenith mutation produced, but that is inherent to any fixed-tolerance oracle test and is disclosed, not hidden.
  (7) Adversarial on math: `SolarMath.hourAngle` (SolarMath.swift:61-66) returns nil via an explicit `guard cosHA >= -1, cosHA <= 1 else { return nil }` - not a clamp - confirmed by reading. `SolarEvents.event`/`eventMinutes` (SolarEvents.swift:39-45, 70-75) propagate that nil via `guard let/guard var ... else { return nil }` rather than fabricating a time. Added my own extra case via a throwaway file (Tests/ScenicKitTests/ReviewerThrowawayPolarTest.swift, removed after, `git status --short` empty afterward): Utqiagvik 71.29N,-156.79W on 2026-01-15. Result: sunrise/sunset nil as expected, but civilDawn/civilDusk were NON-nil (my initial assertion that they'd be nil was astronomically wrong, not a code bug) - solar-noon elevation came back -2.27 deg, matching the 90-|lat-dec| formula independently validated by SolarMathTests.noonElevation(); at this latitude even the Dec-21 solstice solar-noon elevation (~-4.7 deg) never drops below the -6 deg civil threshold, so true "no civil twilight" polar night does not occur at 71.29N at all - the model's behavior is correct and this extra case, once I understood it, corroborates rather than contradicts the nil-propagation claim.
  No fatal or blocking findings. Two informational/non-blocking notes only: Tests/ScenicKitTests/SolarFixtures.swift:7 names the file plural while the type is singular (`struct SolarFixture`), a soft deviation from CLAUDE.md's filename==type convention that is not mechanically enforced (only the 300-line cap is, via P-SRC-02) and is a machine-generated fixture-data file, not hand-authored logic; and the `compared >= 80` guard in worstCase is a loose floor relative to the actual 138 compared. Neither blocks. floor_linux=50 confirmed correct and tight to the current total. P-SAFE-05 confirmed real: no `pending:`, assertion runs the real suite, both RED demonstrations reproduce cleanly, and the oracle itself is independently confirmed non-fabricated against live USNO data at 3 different latitudes.
