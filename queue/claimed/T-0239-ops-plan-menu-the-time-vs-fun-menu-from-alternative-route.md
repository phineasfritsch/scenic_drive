---
id: T-0239
title: ops/plan --menu - the time-vs-fun menu for one trip, from alternative_route over the lambda ladder (max_exploration_factor 2.0), deduplicated, a Pareto frontier of extra minutes against fun km, recorded and replayed offline
state: claimed
owner: agent/claude-opus-5
owner_session: null
claimed_at: 2026-09-25T23:40:52Z
lease_expires_at: 2026-09-26T07:40:52Z
worktree: .worktrees/T-0239
branch: task/T-0239
exclusive: []
touches: [Sources/ScenicKit/Menu/, Sources/ScenicPlanCLI/, Tests/ScenicKitTests/Menu/, Tests/ScenicPlanCLITests/, Tests/Fixtures/t0239/, Tests/Fixtures/t0182-recorder/, ops/plan, ops/mutate/, ops/lib/mutate-population-allowlist.json, ops/lib/check-mutate-population.py, ops/lib/mutate_population_table.py]
pins_affected: [P-SAFE-04]
reviewer: null
depends_on: [T-0182]
verify: [ops/test, ops/check-pins]
acceptance:
  - "MEASURED population (quoted here, the frontier probe of 2026-09-25, .artifacts/frontier/SUMMARY.md, t0213 canyon-window graph, GraphHopper 11): at alternative_route's default max_exploration_factor 1.0 the T4 trip (Zuma -> Agoura) returns ONLY the fastest path at every lambda; at 2.0 (max_paths 4, max_weight_factor 2.0, max_share_factor 0.7) the five trips yield 6/7/3/6/7 distinct routes (way-id Jaccard >= 0.9 = same). RULED in the Log before code: the hints, the lambda ladder {0,2,4,6,8}, the dedup threshold, and the frontier's axes - DEFAULT extra minutes against fun km (distance on scenic_score >= 6), a route kept only if it adds >= 2 fun km over every quicker kept route, capped at +45 min; fun share and mean score are displayed, not ranked on (the owner's taste: T4's Latigo Canyon route at +16.6 min has the most fun km, 19.7, but a lower share, 56%, than The Snake's 58% at +5.4 min, because 8 km of PCH leads to it)"
  - "the menu is a ScenicKit type (Foundation only) computed from recorded paths: a Linux test over RECORDED fixtures for T1 (Topanga -> Malibu) and T4 (Zuma -> Agoura) asserts the menu rows (extra minutes to 0.1, fun km to 0.1) and that T4's menu contains a row whose roads include Latigo Canyon Road; RED first by name with the frontier rule inverted, then green; the fixtures are recorded by the committed recorder, not the probe's scratch Java"
  - "the budget stays a ceiling: every menu row satisfies ETA <= fastest + its displayed extra minutes, and 'ops/plan --menu <O> <D> --max 20' prints no row above +20 (P-SAFE-04's property, a test by name); each row prints an Apple Maps URL with <= 9 waypoints from the committed handoff builder"
  - "the new numeric module ships its population under ops/mutate/ with a literal floor (frontier comparison, dedup threshold, fun-km threshold, the cap); MUTATE OK and --prove-vacuity quoted"
---
## Brief

The owner asked (2026-09-25): 'could we also have an inverse search where it show (within reason) the most top rated
routes from a to b with also filterable by time?' and 'you know to answer the question, how much more would I have to
drive to have a fun drive'. The lambda sweep alone gives at most 2 distinct routes per trip; the probe showed
alternative_route at exploration factor 2.0 gives the menu. Answers from the probe, free-flow, no traffic:
Topanga -> Malibu: +12 min takes the fun share from 34% to 71%, +17 to +20 min gives Saddle Peak (82-84%).
Topanga -> Agoura: the fastest is already 81% fun, +4.8 min gives Saddle Peak and Stunt (92%).
Zuma -> Agoura: +5.4 min gives Mulholland and The Snake, +16.6 min gives all of Latigo Canyon.
Owner-facing preview of the probe's menus: .artifacts/preview/route-menu.html (gitignored).

## Log
- 2026-09-25T23:39:48Z filed by agent/claude-opus-5 (orchestrator) from the frontier probe (one agent, exploration output only). FOR THE OWNER: rank the menu by fun km (Latigo wins T4) or by fun share (The Snake wins)? Default ruled: fun km.
- 2026-09-25T23:40:52Z claimed by agent/claude-opus-5; lease until 2026-09-26T07:40:52Z
- 2026-09-25T23:46:50Z RULINGS before code (agent/claude-opus-5). Read: .artifacts/frontier/{SUMMARY.md,run.sh,frontier.py,T1.json,T4.json}, src/Frontier.java diffed against the committed Tests/Fixtures/t0182-recorder/Recorder.java, Sources/ScenicPlanCLI/*, Sources/ScenicKit/Plan/{RoutePath,RecordedRouteSource,PlanTable,PlanWaypoints,RouteDifference,ScenicPlan}.swift, Sources/Handoff/AppleMapsDirections.swift, ops/mutate/straightline*.py, ops/lib/check-mutate-population.py.
  - R1 HINTS. `algorithm=alternative_route`, `alternative_route.max_paths=4`, `max_weight_factor=2.0`, `max_share_factor=0.7`, `max_exploration_factor=2.0` - on car_fast with no model, and on car_scenic with each ladder model. NO penalty re-routes: the probe used them only when alt-route distinct < 3, and alt-route alone gave 6/7/3/6/7, so frontier.py never used them (T1.json/T4.json `penalty_routes_used: false`). NO `--via`: forced-road probes are exploration only (T4's Latigo midpoint was an out-and-back spur).
  - R2 LADDER {0,2,4,6,8}. Each model file is `ops/plan --emit-model <L>`'s output byte for byte (the Worker's model), not the probe's copies.
  - R3 DEDUP. Two routes are the same route when their OSM way-id sets have Jaccard >= 0.9 (`RouteDifference.overlap`, the committed measure). Candidates are taken in duration order (recording order on a tie) and the quicker one is kept. The count of distinct routes within the cap is PRINTED (`distinct=`) and asserted, because on a time-then-fun frontier a near-duplicate is dropped by the frontier anyway and a dedup threshold nobody can see is a threshold no test can hold.
  - R4 FRONTIER (the DEFAULT the Brief rules: fun km, not fun share). fun metres = metres of the route's PlanTable rows with scenic_score >= 6. The reference `fastest` is the QUICKEST duration in the whole pool (fastest.json in both recordings - the conservative choice for a ceiling). Walking the distinct routes in duration order: the quickest is always row 0; a later route is a row iff its duration <= fastest + cap AND its fun metres >= (the most fun metres of any quicker ROW) + 2000. cap = 45 min; `--max N` lowers it, 0 < N <= 45, anything above 45 refused by name (exit 2), never clamped. Fun share and mean score are displayed, never ranked on. Measured prediction off the probe's numbers: T1 rows +0 / +12.2 (21.5 fun km) / +17.0 (26.2); T4 rows +0 / +5.4 (The Snake, 15.5) / +16.6 (Latigo, 19.7).
  - R5 DISPLAYED EXTRA MINUTES are the CEILING of (duration - fastest)/60 to 0.1 min, never the nearest: the number printed is itself a promise, so ETA <= fastest + displayed extra holds on every row by construction and is asserted row by row.
  - R6 WHERE. Sources/ScenicKit/Menu/ (Foundation only, one type per file): RouteMenu.swift (dedup, frontier, cap, the report), MenuRow.swift (one route's measured facts: extra, km, fun metres, share, mean, roads), RecordedAlternatives.swift (file names; decodes EVERY `paths[]` entry with RoutePath's own decoders). CLI: Sources/ScenicPlanCLI/MenuArguments.swift + MenuCommand.swift; main.swift dispatches `--menu` first, as it does `--emit-model`.
  - R7 RECORDER. The COMMITTED Tests/Fixtures/t0182-recorder/Recorder.java is extended (one recorder, no sibling): `--alternatives on` adds the `street_name` path detail (road names come from the graph, not the probe's window-doc.json) and, beside `fastest.json`, writes `alternatives-fastest.json` and `alternatives-lambda-<L>.json` carrying EVERY path in `paths[]` (the documented /route shape for alternatives). The R1 hints are fixed in the recorder and written into each header. Layout: Tests/Fixtures/t0239/topanga-malibu/ (T1, 34.0944,-118.6013 -> 34.0365,-118.687, the plan-pair endpoints) and Tests/Fixtures/t0239/zuma-agoura/ (T4, 34.017,-118.823 -> 34.144,-118.76, .artifacts/routes/B/fastest.json recorded.from/to). Graph: the preserved services/routing/work/t0213/graph-la-window, config.yml + profiles mounted as T-0182 did, the shipped router jar.
  - R8 ROADS per row: `street_name` runs grouped by name, a name listed when the route covers >= 300 m of it in total, in order of first appearance, unnamed stretches skipped. The full list is printed, not the probe's `via` difference.
  - R9 HANDOFF: each row's waypoints are `PlanWaypoints.decisionPoints` over that row's own PlanTable (the committed pin rule: <= 9, route order, road changes onto the longest stretches) and its URL is Handoff's `AppleMapsDirections(source: O, destination: D, waypoints:).url()` - the committed builder, which refuses more than 9.
  - R10 CLI: `ops/plan --menu <O> <D> [--max N] --recorded <dir>` replays offline and is what CI runs. `--menu --router <url>` is REFUSED by name (exit 2): no served graph exists (T-0209/T-0221) and a live alternative_route body is not built by this task - a flag that parses and then plans against nothing is the failure PlanArguments exists to refuse.
  - R11 TOUCHES WIDENED by ops/lib/check-mutate-population.py: a runnable ops/mutate driver outside its literal DRIVERS whitelist is a refusal (exit 2), and COVERED_FLOOR is where a new population's subjects are pinned; the Brief's "register it wherever check-mutate-population wants" names that file.
  - R12 P-SAFE-04 is in pins_affected but `grep -n SAFE-04 pins/PINS.yaml` finds nothing and PINS.yaml is not in touches: the property is carried by the test by name (MenuCLITests); the pin row stays open and is named in the PR.
  - R13 POPULATION: ops/mutate/menu.py (CLI, floors, proof arms) + menu_mutations.py + menu_run.py, straightline's three-file shape; SUBJECT_MODULES = RouteMenu.swift, MenuRow.swift, MenuArguments.swift; killers are suites RouteMenuTests and MenuCLITests; literal MIN_MUTATIONS; --prove-vacuity empties exactly those test files.
- 2026-09-25T23:57:58Z RECORD RUN landed (agent/claude-opus-5). The committed Tests/Fixtures/t0182-recorder/Recorder.java, extended per R7 (`--alternatives on`, 256 lines), compiled in WSL docker (maven:3.9-eclipse-temurin-21@sha256:c2a2c585...) against .artifacts/routes/_build/scenic-router.jar and run over a copy of services/routing/work/t0213/graph-la-window with services/routing/config.yml + profiles mounted; ladder models from `ops/plan --emit-model {0,2,4,6,8}` (cmp: byte-identical to the probe's models). Count lines, verbatim:
  `JAR_SHA256 7931f599a14bd00844793ef441ce6228e475e5d5482be5702a75e506f076fe43` / `GRAPH_PROPERTIES_SHA256 170bfe37bda146a51821f74fa53d9a42084e1cb3db27e2841205fe432c20b188` / `COMPILED 1 classes`
  `TRIP topanga-malibu 34.0944,-118.6013 -> 34.0365,-118.687`: car_fast `path=0 time_ms=1075693 distance_m=18224.3 points=478 way_runs=61`; alternatives-fastest paths 0-3 time_ms 1075693 / 1496192 / 1804912 / 1853263; lambda-0 the same four; lambda-2 1075693 / 1804912 / 1853263 / 2253369; lambda-4 1075693 / 2253369 / 1853263 / 2095756; lambda-6 2253369 / 1853263 / 1075693 / 1804912; lambda-8 2253369 / 1853263 / 1804912 / 1075693. 24 alternative paths, 6 distinct durations (1075693, 1496192, 1804912, 1853263, 2095756, 2253369) - the probe's T1 6.
  `TRIP zuma-agoura 34.017,-118.823 -> 34.144,-118.76`: car_fast `path=0 time_ms=1099444 distance_m=23231.6 points=400 way_runs=64`; alternatives-fastest 1099444 / 1639607 / 1728833 / 1740880; lambda-0 1099444 / 1425753 / 1639607 / 1728833; lambda-2 1099444 / 1425753 / 1639607 / 2092858; lambda-4, -6, -8 1099444 / 1425753 / 2092858 / 1639607. 24 alternative paths, 6 distinct durations (1099444, 1425753, 1639607, 1728833, 1740880, 2092858) - the probe's T4 6. `Latigo Canyon Road` occurs in the street_name detail of alternatives-lambda-{2,4,6,8}.json (the 2092858 ms path, +16.6 min), matching the probe's found_by.
  `WROTE` 14 files; Tests/Fixtures/t0239/ is 1.2 MB (7 files per trip, alternatives-*.json 73-83 KB each).
- 2026-09-26T00:08:02Z RED THEN GREEN (agent/claude-opus-5). Code: Sources/ScenicKit/Menu/{RouteMenu,MenuRow,RecordedAlternatives}.swift, Sources/ScenicPlanCLI/{MenuArguments,MenuCommand}.swift, main.swift dispatches `--menu`; tests Tests/ScenicKitTests/Menu/RouteMenuTests.swift (3) and Tests/ScenicPlanCLITests/MenuCLITests.swift (5). Expected rows were written from the probe's measured numbers before the run, re-ranked on fun km (R4) and rounded UP (R5); a Python cross-check over the committed fixtures (Geo's radius, .build/t0239/measure.py, not committed) agreed row for row: T1 distinct 6, max pairwise Jaccard among distinct 0.794; T4 distinct 6, 0.803.
  RED, frontier rule INVERTED (`if route.funMeters < best + Self.funStepMeters`), `swift test --scratch-path .build/T0239 --filter "RouteMenuTests|MenuCLITests"` exit=1, `Test run with 8 tests in 2 suites failed after 5.206 seconds with 8 issues.`, red by name:
    `Test "Topanga to Malibu: the menu rows to 0.1 min and 0.1 fun km"` - `(menu.rows.map(\.extraMinutes) → [0.0, 7.1]) == [0.0, 12.2, 17.1]`, `[6.1, 7.6]) == [6.1, 21.5, 26.2]`
    `Test "Zuma to Agoura: the menu rows to 0.1, and Latigo Canyon Road is on it"` - `→ [0.0, 9.1, 10.5]) == [0.0, 5.5, 16.6]`, `menu.rows.first { $0.roads.contains("Latigo Canyon Road") } → nil`
    `Test "every menu row keeps ETA within fastest plus its displayed extra minutes"` - `(menu.rows.count → 2) >= 3`
    `Test "--max below a row drops that row and keeps every quicker one"` - `(t1 → [0.0, 7.1]) == [0.0, 12.2]`, `→ [0.0, 9.1]) == [0.0, 5.5]`
    (green under the inversion, as they should be - none of them is about the frontier: the distinct count, the refusals, the URL waypoint count, and `--max 20`, which on these two trips cuts nothing either way.)
  GREEN, rule restored (`>=`), full `swift test --scratch-path .build/T0239` exit=0: `Test run with 332 tests in 46 suites passed after 4.307 seconds.` (324 at 07739aa + 8).
  THE MENUS, real runs of `SCENIC_PLAN_SCRATCH=.build/T0239 bash ops/plan --menu <O> <D> --recorded Tests/Fixtures/t0239/<trip>` (exit 0 both), verbatim minus the URL bodies (every row prints `URL <n> waypoints=9 https://maps.apple.com/directions?source=...&mode=driving`):
    `MENU fastest=17m56s cap=+45.0min candidates=25 distinct=6 rows=3 (fun = scenic_score >= 6; each row adds >= 2.0 fun km over every quicker row; extra minutes rounded up; free-flow, no traffic)`
    `ROW 0 extra=+0.0min eta=17m56s km=18.22 fun_km=6.1 fun_share=34% mean=2.97 roads=North Topanga Canyon Boulevard, South Topanga Canyon Boulevard, Topanga Canyon Boulevard, Pacific Coast Highway`
    `ROW 1 extra=+12.2min eta=30m05s km=30.05 fun_km=21.5 fun_share=71% mean=5.97 roads=North Topanga Canyon Boulevard, Old Topanga Canyon Road, Mulholland Highway, Cold Canyon Road, Piuma Road, Malibu Canyon Road, North Malibu Canyon Road, South Malibu Canyon Road, Civic Center Way`
    `ROW 2 extra=+17.1min eta=34m56s km=32.07 fun_km=26.2 fun_share=82% mean=6.45 roads=North Topanga Canyon Boulevard, South Topanga Canyon Boulevard, Fernwood Pacific Drive, Tuna Canyon Road, Saddle Peak Road, Stunt Road, Mulholland Highway, Cold Canyon Road, Piuma Road, Malibu Canyon Road, North Malibu Canyon Road, South Malibu Canyon Road, Civic Center Way`
    `MENU fastest=18m19s cap=+45.0min candidates=25 distinct=6 rows=3 (...)`
    `ROW 0 extra=+0.0min eta=18m19s km=23.23 fun_km=10.6 fun_share=46% mean=4.54 roads=Pacific Coast Highway, Kanan Dume Road, Kanan Road, Agoura Road`
    `ROW 1 extra=+5.5min eta=23m46s km=26.67 fun_km=15.5 fun_share=58% mean=4.92 roads=Pacific Coast Highway, Kanan Dume Road, Mulholland Highway, Mulholland Highway (The Snake), Cornell Road, Kanan Road, Agoura Road`
    `ROW 2 extra=+16.6min eta=34m53s km=35.37 fun_km=19.7 fun_share=56% mean=4.77 roads=Pacific Coast Highway, Latigo Canyon Road, Kanan Dume Road, Kanan Road, Agoura Road`
  `--max 20` on T1 prints the same three rows (cap=+20.0min); `--max 10` on T4 prints `distinct=3` and rows +0.0 and +5.5 only; `--max 46` exits 2 with `ops/plan: --max 46 is above the menu's cap of 45 minutes; it lowers the cap, never raises it` and the usage line. Owner-facing reading: Topanga -> Malibu, +12.2 min buys 15.4 more fun km (Old Topanga/Mulholland/Piuma) and +17.1 min Saddle Peak and Stunt (26.2 fun km, 82%); Zuma -> Agoura, +5.5 min The Snake, +16.6 min all of Latigo Canyon (the most fun km, 19.7, at a lower share than The Snake - displayed, not ranked on).
- 2026-09-26T00:46:34Z POPULATION landed (agent/claude-opus-5, at 0d56930). ops/mutate/menu.py + menu_mutations.py + menu_run.py; SUBJECT_MODULES = RouteMenu.swift, MenuRow.swift, MenuArguments.swift; MIN_MUTATIONS = 16, MIN_EQUIVALENT = 2, MIN_TEST_FILES = 2 (the real counts). The acceptance's four classes: frontier comparison (1-3), fun-km threshold (4-5, `MenuRow.funScore`), dedup threshold (6-7, killed by the printed distinct count - measured closest distinct pairs 0.794 T1 / 0.803 T4), the cap (8-10, 14-16); plus R5's round-up (11-12) and R8's 300 m (13). Registered: `menu.py` in DRIVERS and the three subjects in COVERED_FLOOR of ops/lib/check-mutate-population.py (reflowed to stay at 300 lines, max 113 cols); RecordedAlternatives.swift and MenuCommand.swift allowlisted with reasons.
  `python ops/mutate/menu.py` exit=0: `caught by the test that names it: 16 of 16   (wrong killer 0, trapped 0, compile-only 0, MISSED 0, skipped 0)` / both EQUIVALENT entries `MISSED ... exit=0  no test objected` (the strict `>` frontier: no gain in either recording is exactly 2_000.0 m; the quickest-in-pool reference: fastest.json IS the pool minimum, 1_075_693 / 1_099_444 ms) / `MUTATE OK  caught=16/16 equivalent_caught=0`.
  `python ops/mutate/menu.py --prove-vacuity` exit=0: `caught by the test that names it: 0 of 16   (wrong killer 0, trapped 0, compile-only 0, MISSED 16, skipped 0)` / `VACUITY PROOF OK: with the 2 test file(s) emptied, caught=0 (need 0) and MISSED=16 of 16`.
  `python ops/mutate/menu.py --prove-floor` exit=0: `FLOOR PROOF OK: 7 of 7 arms refused and the control did not` (the subject arm pads the count back to 16 so it is refused by `no MUTATIONS entry edits MenuArguments.swift`, not by the count).
  REGISTRATION seen red, then green (in memory over this tree, .build/t0239/red_register.py, not committed): menu.py taken out of DRIVERS -> `exit=2 REFUSAL ops/mutate: runnable driver(s) in neither DRIVERS nor PROBES: menu.py`; RouteMenu.swift neither declared nor allowlisted -> `exit=1 P-PROC-06: module(s) added by this branch with no mutation population: Sources/ScenicKit/Menu/RouteMenu.swift`; unpatched -> exit=0. `python ops/lib/check-mutate-population.py` exit=0 `P-PROC-06: every added module is covered or allowlisted; the floor of 37 holds` (95 modules, 38 covered by 15 populations, 35 allowlisted, 5 added by this branch); `--prove-red` exit=0 `all 13 cases behaved as stated`.
- 2026-09-26T01:27:28Z FINAL PRE-REVIEW - the whole acceptance block re-run BARE on the merged head 30e5108 (origin/main 5d1e260 merged; main had moved by queue files only), and re-quoted (agent/claude-opus-5):
  1. MEASURED POPULATION + RULINGS: ruled in the 2026-09-25T23:46:50Z entry (R1-R13) before any code. The committed recorder's own record run (2026-09-25T23:57:58Z entry) reproduced the probe's population on the two recorded trips: 25 paths per trip (fastest.json + 4 alternative_route paths x 6 requests), 6 distinct at Jaccard >= 0.9 on both T1 and T4 (the probe's 6 and 6); the closest distinct pairs overlap 0.794 / 0.803. Asserted by `alternative_route over the ladder yields 6 distinct routes per trip at Jaccard 0.9` and printed as `candidates=25 distinct=6`.
  2. THE MENU IS A SCENICKIT TYPE: RouteMenu/MenuRow/RecordedAlternatives under Sources/ScenicKit/Menu/, Foundation only. RED by name with the frontier rule inverted (2026-09-26T00:08:02Z entry: T1 `[0.0, 7.1]`, T4 `[0.0, 9.1, 10.5]` and no Latigo row), then green. Rows, to 0.1: T1 extra [0.0, 12.2, 17.1] fun km [6.1, 21.5, 26.2]; T4 extra [0.0, 5.5, 16.6] fun km [10.6, 15.5, 19.7]; T4's +16.6 row's roads are exactly Pacific Coast Highway, Latigo Canyon Road, Kanan Dume Road, Kanan Road, Agoura Road. Fixtures recorded by the committed Tests/Fixtures/t0182-recorder/Recorder.java (`--alternatives on`), not the probe's Frontier.java.
  3. THE BUDGET STAYS A CEILING: `every menu row keeps ETA within fastest plus its displayed extra minutes` (per row, duration_ms <= fastest_ms + extraTenths * 6000, and the printed extras equal the rows'), `ops/plan --menu --max 20 prints no row above +20`, `--max below a row drops that row and keeps every quicker one` (--max 15 on T1 -> [0.0, 12.2]; --max 10 on T4 -> [0.0, 5.5], no Latigo), `--max above 45 and --router are refused by name`, `every row prints an Apple Maps URL with at most 9 waypoints`. Real runs, `SCENIC_PLAN_SCRATCH=.build/T0239 bash ops/plan --menu <O> <D> --max 20 --recorded Tests/Fixtures/t0239/<trip>`, exit 0 both: T1 `MENU fastest=17m56s cap=+20.0min candidates=25 distinct=6 rows=3`, rows +0.0 / +12.2 / +17.1; T4 `MENU fastest=18m19s cap=+20.0min candidates=25 distinct=6 rows=3`, rows +0.0 / +5.5 / +16.6 (Latigo Canyon Road); every row `URL <n> waypoints=9 https://maps.apple.com/directions?source=...` from Handoff's AppleMapsDirections over PlanWaypoints.decisionPoints. On these trips `--max 20` cuts nothing (slowest row +17.1), which is why the cut is also asserted where it bites.
  4. POPULATION: `python ops/mutate/menu.py` exit=0 `caught by the test that names it: 16 of 16   (wrong killer 0, trapped 0, compile-only 0, MISSED 0, skipped 0)`, both EQUIVALENT entries MISSED, `MUTATE OK  caught=16/16 equivalent_caught=0`; `python ops/mutate/menu.py --prove-vacuity` exit=0 `VACUITY PROOF OK: with the 2 test file(s) emptied, caught=0 (need 0) and MISSED=16 of 16`. Literal floors MIN_MUTATIONS = 16, MIN_EQUIVALENT = 2.
  GATES, bare, on 30e5108: `swift test --scratch-path .build/T0239` exit=0 `Test run with 332 tests in 46 suites passed after 4.262 seconds.`; `python ops/lib/check-mutate-population.py` exit=0 `P-PROC-06: every added module is covered or allowlisted; the floor of 37 holds`; `bash ops/lib/check-line-cap` exit=0 `P-SRC-02: 118 Swift files tracked (Sources=48, Tests=49, apps/ios=21), none over 300 lines`; `bash ops/lib/check-exec-bits` exit=0 `P-OPS-01: 98 files, 23 required present, all modes correct`; `bash ops/queue-check` exit=0 `QUEUE OK (234 tasks)`; `bash ops/check-pins --source-only` exit=0 `PINS ok=15 skipped=16 pending=1 expired=0 failed=0 tier=linux source-only`. Measured files: RouteMenu.swift 96, MenuRow.swift 87, RecordedAlternatives.swift 57, MenuArguments.swift 72, MenuCommand.swift 33, main.swift 74, RouteMenuTests.swift 56, MenuCLITests.swift 100, Recorder.java 256, ops/mutate/menu.py 174, menu_mutations.py 112, menu_run.py 112, ops/lib/check-mutate-population.py 300 lines.
  OPEN, named in the PR: P-SAFE-04 has no row in pins/PINS.yaml and PINS.yaml is outside touches (R12) - the property is carried by the named test only; a live `--menu --router` is refused until T-0221 serves a graph; MenuCommand (the URL wiring) is allowlisted, not mutated - its guard is the URL test's waypoint count.
- 2026-09-26T01:29:38Z R14 (agent/claude-opus-5), ruled before the edit: origin/main moved during the final re-run - T-0208 merged and MOVED DRIVERS, PROBES and COVERED_FLOOR out of ops/lib/check-mutate-population.py into a new data file, ops/lib/mutate_population_table.py (the gate had reached 302 lines). The merge conflicted on the gate; resolved by taking main's gate WHOLE (`git checkout --theirs`, so this branch's diff to it is now empty) and registering this population where the tables now live: touches widened by ops/lib/mutate_population_table.py for R11's reason (the whitelist and the floor are that file now), `menu.py` added to its DRIVERS and RouteMenu.swift, MenuRow.swift and MenuArguments.swift to its COVERED_FLOOR. The acceptance block is re-run bare on the new merged head before the push.
- 2026-09-26T02:28:30Z RESUMED after a usage-limit stop (agent/claude-opus-5). The worktree was clean at 23dd86b (the R14 registration), two commits ahead of origin/task/T-0239 at 841bad0; pushed as they were (841bad0..23dd86b), nothing reset. origin/main had moved by dc58864 (queue files only), merged -> bc84030. The WHOLE acceptance block re-run BARE on bc84030 and re-quoted:
  1. MEASURED POPULATION + RULINGS: R1-R13 ruled in the 2026-09-25T23:46:50Z entry before any code, R14 before its edit. The committed recorder's record run (2026-09-25T23:57:58Z entry) reproduced the probe's population on both trips: 25 paths per trip, 6 distinct at Jaccard >= 0.9 on T1 and T4. Asserted by `alternative_route over the ladder yields 6 distinct routes per trip at Jaccard 0.9`, printed as `candidates=25 distinct=6`.
  2. THE MENU IS A SCENICKIT TYPE: RouteMenu/MenuRow/RecordedAlternatives under Sources/ScenicKit/Menu/, Foundation only; RED by name with the frontier rule inverted (2026-09-26T00:08:02Z entry), then green. `SCENIC_PLAN_SCRATCH=.build/T0239 bash ops/plan --menu 34.0944,-118.6013 34.0365,-118.687 --max 20 --recorded Tests/Fixtures/t0239/topanga-malibu` exit=0:
    `MENU fastest=17m56s cap=+20.0min candidates=25 distinct=6 rows=3 (fun = scenic_score >= 6; ...)`
    `ROW 0 extra=+0.0min eta=17m56s km=18.22 fun_km=6.1 fun_share=34% mean=2.97` / `ROW 1 extra=+12.2min eta=30m05s km=30.05 fun_km=21.5 fun_share=71% mean=5.97` / `ROW 2 extra=+17.1min eta=34m56s km=32.07 fun_km=26.2 fun_share=82% mean=6.45`
    `bash ops/plan --menu 34.017,-118.823 34.144,-118.76 --max 20 --recorded Tests/Fixtures/t0239/zuma-agoura` exit=0:
    `MENU fastest=18m19s cap=+20.0min candidates=25 distinct=6 rows=3` / `ROW 0 extra=+0.0min eta=18m19s km=23.23 fun_km=10.6 fun_share=46% mean=4.54` / `ROW 1 extra=+5.5min eta=23m46s km=26.67 fun_km=15.5 fun_share=58% mean=4.92 roads=..., Mulholland Highway (The Snake), Cornell Road, ...` / `ROW 2 extra=+16.6min eta=34m53s km=35.37 fun_km=19.7 fun_share=56% mean=4.77 roads=Pacific Coast Highway, Latigo Canyon Road, Kanan Dume Road, Kanan Road, Agoura Road`
  3. THE BUDGET STAYS A CEILING: the tests by name (`every menu row keeps ETA within fastest plus its displayed extra minutes`, `ops/plan --menu --max 20 prints no row above +20`, `--max below a row drops that row and keeps every quicker one`, `--max above 45 and --router are refused by name`, `every row prints an Apple Maps URL with at most 9 waypoints`) pass inside the swift run below; the two real `--max 20` runs above print 3 URL lines each, every one `waypoints=9`.
  4. POPULATION: `python ops/mutate/menu.py` exit=0 `caught by the test that names it: 16 of 16   (wrong killer 0, trapped 0, compile-only 0, MISSED 0, skipped 0)`, both EQUIVALENT entries `MISSED ... exit=0  no test objected`, `MUTATE OK  caught=16/16 equivalent_caught=0`; `python ops/mutate/menu.py --prove-vacuity` exit=0 `caught by the test that names it: 0 of 16 ... MISSED 16` / `VACUITY PROOF OK: with the 2 test file(s) emptied, caught=0 (need 0) and MISSED=16 of 16`. Literal floors MIN_MUTATIONS = 16, MIN_EQUIVALENT = 2.
  REGISTRATION re-seen red on the moved table (.build/t0239/red_register.py, in memory, not committed): menu.py out of DRIVERS -> `exit=2 REFUSAL ops/mutate: runnable driver(s) in neither DRIVERS nor PROBES: menu.py`; RouteMenu.swift undeclared and not allowlisted -> `exit=1 P-PROC-06: module(s) added by this branch with no mutation population: Sources/ScenicKit/Menu/RouteMenu.swift`.
  GATES, bare, on bc84030: `swift test --scratch-path .build/T0239` exit=0 `Test run with 332 tests in 46 suites passed after 4.186 seconds.`; `python ops/lib/check-mutate-population.py` exit=0 `P-PROC-06: every added module is covered or allowlisted; the floor of 39 holds`; `--prove-red` exit=0 `all 13 cases behaved as stated`; `bash ops/lib/check-line-cap` exit=0 `P-SRC-02: 118 Swift files tracked (Sources=48, Tests=49, apps/ios=21), none over 300 lines`; `bash ops/lib/check-exec-bits` exit=0 `P-OPS-01: 101 files, 23 required present, all modes correct`; `bash ops/queue-check` exit=0 `QUEUE OK (235 tasks)`; `bash ops/check-pins --source-only` exit=0 `PINS ok=15 skipped=16 pending=1 expired=0 failed=0 tier=linux source-only`. Measured: RouteMenu.swift 96, MenuRow.swift 87, RecordedAlternatives.swift 57, MenuArguments.swift 72, MenuCommand.swift 33, main.swift 74, RouteMenuTests.swift 56, MenuCLITests.swift 100, Recorder.java 256, menu.py 174, menu_mutations.py 112, menu_run.py 112, mutate_population_table.py 39; check-mutate-population.py is main's, unchanged by this branch.
  OPEN, named in the PR: P-SAFE-04 has no row in pins/PINS.yaml (R12) - carried by the named test only; `--menu --router` refused until T-0221; MenuCommand allowlisted, guarded by the URL test's waypoint count.
- 2026-09-26T04:26:42Z PRE-REVIEW MUTANT PASS - the four survivors closed (agent/claude-opus-5; fix 0ef31b3, merged head 2a9a9b8 with origin/main 4368aeb). Each was ruled here before its test, and every test below binds to the shipping entry (`MenuCommand.run`/`.menu`, or `RouteMenu.frontier`, which `RouteMenu.init` runs). Red on its mutant = the population run below, then green:
  M2a, the cap's slack at `--max` (P-SAFE-04's property). `--max 15`/`--max 10` sat 2.0/6.6 min under the next row, and `--max 20` cannot cut (the slowest recorded route is +19.7). RULED: test the boundary at every row, through `MenuCommand.run`: `--max <the row's displayed extra>` keeps the row, and `--max <that - 0.1>` drops it and nothing quicker. Test `--max 0.1 below a row's displayed extra drops that row, and --max at it keeps it`. Population 17 `the cap a minute loose` (`+ 60_000`) and 18 `the cap 100 ms loose` (`+ 100`; T1's +17.1 row is 1_020_063 ms, 63 ms past 17.0) are both caught by it. The verifier's killing inputs, real runs on 2a9a9b8: `bash ops/plan --menu 34.017,-118.823 34.144,-118.76 --max 16 --recorded Tests/Fixtures/t0239/zuma-agoura` exit=0 `MENU fastest=18m19s cap=+16.0min candidates=25 distinct=5 rows=2`, rows +0.0 and +5.5. T1 `--max 16.5` exit=0 `cap=+16.5min candidates=25 distinct=4 rows=2`, rows +0.0 and +12.2.
  M1a, the shipping ladder. RULED: assert the counts on `MenuCommand.run`'s header, not on a ladder the test builds itself. Test `ops/plan --menu reads every rung: candidates=25 distinct=6 on both trips`. Population 19 `the shipping menu drops the ladder's top rung` (MenuCommand.swift, `Array(RouteMenu.ladder.dropLast())`) is caught by it, and also by the rung-swap test below. MenuCommand.swift moves from the allowlist into the population (menu.py SUBJECT_MODULES, COVERED_FLOOR).
  M3, what a recording says it is. RULED: `RecordedAlternatives.load` now takes the trip's origin and destination and refuses, as `PlanFailure.malformedResponse`, any file where one of these holds:
    - its `recorded` (profile, model) is not the one its name stands for (`car_fast -` for the two car_fast files, `car_scenic lambda-<L>.json` for a rung);
    - an alternatives file lacks the `alternative_route` algorithm, or fastest.json carries it;
    - its from/to lie farther than `endpointTolerance` = 0.005 degrees from the asked endpoints (the 2-decimal rounding the verifier allowed).
  Tests:
    - `a rung whose file carries another rung's recording is refused`: T1 is copied to a temp dir and replays once as a control, then lambda 4's file is placed under lambda 8's name.
    - `a recording of another trip is refused, not replayed under these endpoints`: T4's endpoints over T1's recording.
  Population 21 `a recording's model header not read` (profile only, `said[0] == expected[0]`) and 22 `a recording's endpoints not read` are each caught by their own test. RecordedAlternatives.swift moves from the allowlist into the population. The verifier's CLI case on 2a9a9b8: `bash ops/plan --menu 34.017,-118.823 34.144,-118.76 --recorded Tests/Fixtures/t0239/topanga-malibu` now exits 3 with `ops/plan REFUSED: the router's response could not be read: fastest.json was recorded from 34.0944,-118.6013 to 34.0365,-118.687, not from 34.017,-118.823 to 34.144,-118.76`. OPEN: the pre-existing `RecordedRouteSource` (`ops/plan --recorded`, T-0182) reads no header either. It lives in Sources/ScenicKit/Plan/, outside this task's touches, and is named in the PR for a follow-up task.
  M2c, "the most fun of EVERY quicker row". RULED: a test, not an EQUIVALENT entry. It builds T4's three rows plus a recorded T1 route whose fun km lies between Snake + 2 km (Snake is the higher share, 58%) and Latigo + 2 km (Latigo is the more fun km, 19.7), and `RouteMenu.frontier` must return T4's rows unchanged. Test `a slower route must add 2 fun km over the most fun km of every quicker row, not the highest share`. Population 20 `the frontier steps from the highest-share quicker row` (`rows.max(by: funShare)`) is caught by it.
  POPULATION on 0ef31b3:
    - `python ops/mutate/menu.py` exit=0: `caught by the test that names it: 22 of 22   (wrong killer 0, trapped 0, compile-only 0, MISSED 0, skipped 0)`. Both EQUIVALENT entries report `MISSED ... exit=0  no test objected`. Summary: `MUTATE OK  caught=22/22 equivalent_caught=0`.
    - `--prove-vacuity` exit=0: `caught by the test that names it: 0 of 22   (wrong killer 0, trapped 0, compile-only 0, MISSED 22, skipped 0)` / `VACUITY PROOF OK: with the 2 test file(s) emptied, caught=0 (need 0) and MISSED=22 of 22`.
    - `--prove-floor` exit=0: `FLOOR PROOF OK: 7 of 7 arms refused and the control did not`.
    - Literal floors: MIN_MUTATIONS 16 -> 22, MIN_EQUIVALENT 2.
  GATES, bare, on the merged head 2a9a9b8:
    - `swift test --scratch-path .build/T0239` exit=0: `Test run with 351 tests in 49 suites passed after 10.721 seconds.`
    - `python ops/lib/check-mutate-population.py` exit=0: `P-PROC-06: every added module is covered or allowlisted; the floor of 41 holds`. With `--prove-red`, exit=0: `all 13 cases behaved as stated`.
    - `bash ops/lib/check-line-cap` exit=0: `P-SRC-02: 126 Swift files tracked (Sources=50, Tests=52, apps/ios=24), none over 300 lines`.
    - `bash ops/lib/check-exec-bits` exit=0: `P-OPS-01: 103 files, 23 required present, all modes correct`.
    - `bash ops/queue-check` exit=0: `QUEUE OK (237 tasks)`.
    - `bash ops/check-pins --source-only` exit=0: `PINS ok=15 skipped=16 pending=1 expired=0 failed=0 tier=linux source-only`.
    - The default T1 run still prints `candidates=25 distinct=6 rows=3`.
  Measured: RecordedAlternatives.swift 101, RouteMenu.swift 96, MenuRow.swift 87, MenuArguments.swift 72, MenuCommand.swift 35, RouteMenuTests.swift 90, MenuCLITests.swift 150, menu.py 175, menu_mutations.py 141, menu_run.py 112.
