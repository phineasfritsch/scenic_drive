---
id: T-0239
title: ops/plan --menu - the time-vs-fun menu for one trip, from alternative_route over the lambda ladder (max_exploration_factor 2.0), deduplicated, a Pareto frontier of extra minutes against fun km, recorded and replayed offline
state: claimed
owner: agent/claude-opus-5
owner_session: null
claimed_at: 2026-09-25T23:40:52Z
lease_expires_at: 2026-09-26T07:40:52Z
worktree: .worktrees/T-0239
branch: task/T-0239
exclusive: []
touches: [Sources/ScenicKit/Menu/, Sources/ScenicPlanCLI/, Tests/ScenicKitTests/Menu/, Tests/ScenicPlanCLITests/, Tests/Fixtures/t0239/, Tests/Fixtures/t0182-recorder/, ops/plan, ops/mutate/, ops/lib/mutate-population-allowlist.json, ops/lib/check-mutate-population.py]
pins_affected: [P-SAFE-04]
reviewer: null
depends_on: [T-0182]
verify: [ops/test, ops/check-pins]
acceptance:
  - "MEASURED population (quoted here, the frontier probe of 2026-09-25, .artifacts/frontier/SUMMARY.md, t0213 canyon-window graph, GraphHopper 11): at alternative_route's default max_exploration_factor 1.0 the T4 trip (Zuma -> Agoura) returns ONLY the fastest path at every lambda; at 2.0 (max_paths 4, max_weight_factor 2.0, max_share_factor 0.7) the five trips yield 6/7/3/6/7 distinct routes (way-id Jaccard >= 0.9 = same). RULED in the Log before code: the hints, the lambda ladder {0,2,4,6,8}, the dedup threshold, and the frontier's axes - DEFAULT extra minutes against fun km (distance on scenic_score >= 6), a route kept only if it adds >= 2 fun km over every quicker kept route, capped at +45 min; fun share and mean score are displayed, not ranked on (the owner's taste: T4's Latigo Canyon route at +16.6 min has the most fun km, 19.7, but a lower share, 56%, than The Snake's 58% at +5.4 min, because 8 km of PCH leads to it)"
  - "the menu is a ScenicKit type (Foundation only) computed from recorded paths: a Linux test over RECORDED fixtures for T1 (Topanga -> Malibu) and T4 (Zuma -> Agoura) asserts the menu rows (extra minutes to 0.1, fun km to 0.1) and that T4's menu contains a row whose roads include Latigo Canyon Road; RED first by name with the frontier rule inverted, then green; the fixtures are recorded by the committed recorder, not the probe's scratch Java"
  - "the budget stays a ceiling: every menu row satisfies ETA <= fastest + its displayed extra minutes, and 'ops/plan --menu <O> <D> --max 20' prints no row above +20 (P-SAFE-04's property, a test by name); each row prints an Apple Maps URL with <= 9 waypoints from the committed handoff builder"
  - "the new numeric module ships its population under ops/mutate/ with a literal floor (frontier comparison, dedup threshold, fun-km threshold, the cap); MUTATE OK and --prove-vacuity quoted"
---
## Brief

The owner asked (2026-09-25): 'could we also have an inverse search where it show (within reason) the most top rated
routes from a to b with also filterable by time?' and 'you know to answer the question, how much more would I have to
drive to have a fun drive'. The lambda sweep alone gives at most 2 distinct routes per trip; the probe showed
alternative_route at exploration factor 2.0 gives the menu. Answers from the probe, free-flow, no traffic:
Topanga -> Malibu: +12 min takes the fun share from 34% to 71%, +17 to +20 min gives Saddle Peak (82-84%).
Topanga -> Agoura: the fastest is already 81% fun, +4.8 min gives Saddle Peak and Stunt (92%).
Zuma -> Agoura: +5.4 min gives Mulholland and The Snake, +16.6 min gives all of Latigo Canyon.
Owner-facing preview of the probe's menus: .artifacts/preview/route-menu.html (gitignored).

## Log
- 2026-09-25T23:39:48Z filed by agent/claude-opus-5 (orchestrator) from the frontier probe (one agent, exploration output only). FOR THE OWNER: rank the menu by fun km (Latigo wins T4) or by fun share (The Snake wins)? Default ruled: fun km.
- 2026-09-25T23:40:52Z claimed by agent/claude-opus-5; lease until 2026-09-26T07:40:52Z
- 2026-09-25T23:46:50Z RULINGS before code (agent/claude-opus-5). Read: .artifacts/frontier/{SUMMARY.md,run.sh,frontier.py,T1.json,T4.json}, src/Frontier.java diffed against the committed Tests/Fixtures/t0182-recorder/Recorder.java, Sources/ScenicPlanCLI/*, Sources/ScenicKit/Plan/{RoutePath,RecordedRouteSource,PlanTable,PlanWaypoints,RouteDifference,ScenicPlan}.swift, Sources/Handoff/AppleMapsDirections.swift, ops/mutate/straightline*.py, ops/lib/check-mutate-population.py.
  - R1 HINTS. `algorithm=alternative_route`, `alternative_route.max_paths=4`, `max_weight_factor=2.0`, `max_share_factor=0.7`, `max_exploration_factor=2.0` - on car_fast with no model, and on car_scenic with each ladder model. NO penalty re-routes: the probe used them only when alt-route distinct < 3, and alt-route alone gave 6/7/3/6/7, so frontier.py never used them (T1.json/T4.json `penalty_routes_used: false`). NO `--via`: forced-road probes are exploration only (T4's Latigo midpoint was an out-and-back spur).
  - R2 LADDER {0,2,4,6,8}. Each model file is `ops/plan --emit-model <L>`'s output byte for byte (the Worker's model), not the probe's copies.
  - R3 DEDUP. Two routes are the same route when their OSM way-id sets have Jaccard >= 0.9 (`RouteDifference.overlap`, the committed measure). Candidates are taken in duration order (recording order on a tie) and the quicker one is kept. The count of distinct routes within the cap is PRINTED (`distinct=`) and asserted, because on a time-then-fun frontier a near-duplicate is dropped by the frontier anyway and a dedup threshold nobody can see is a threshold no test can hold.
  - R4 FRONTIER (the DEFAULT the Brief rules: fun km, not fun share). fun metres = metres of the route's PlanTable rows with scenic_score >= 6. The reference `fastest` is the QUICKEST duration in the whole pool (fastest.json in both recordings - the conservative choice for a ceiling). Walking the distinct routes in duration order: the quickest is always row 0; a later route is a row iff its duration <= fastest + cap AND its fun metres >= (the most fun metres of any quicker ROW) + 2000. cap = 45 min; `--max N` lowers it, 0 < N <= 45, anything above 45 refused by name (exit 2), never clamped. Fun share and mean score are displayed, never ranked on. Measured prediction off the probe's numbers: T1 rows +0 / +12.2 (21.5 fun km) / +17.0 (26.2); T4 rows +0 / +5.4 (The Snake, 15.5) / +16.6 (Latigo, 19.7).
  - R5 DISPLAYED EXTRA MINUTES are the CEILING of (duration - fastest)/60 to 0.1 min, never the nearest: the number printed is itself a promise, so ETA <= fastest + displayed extra holds on every row by construction and is asserted row by row.
  - R6 WHERE. Sources/ScenicKit/Menu/ (Foundation only, one type per file): RouteMenu.swift (dedup, frontier, cap, the report), MenuRow.swift (one route's measured facts: extra, km, fun metres, share, mean, roads), RecordedAlternatives.swift (file names; decodes EVERY `paths[]` entry with RoutePath's own decoders). CLI: Sources/ScenicPlanCLI/MenuArguments.swift + MenuCommand.swift; main.swift dispatches `--menu` first, as it does `--emit-model`.
  - R7 RECORDER. The COMMITTED Tests/Fixtures/t0182-recorder/Recorder.java is extended (one recorder, no sibling): `--alternatives on` adds the `street_name` path detail (road names come from the graph, not the probe's window-doc.json) and, beside `fastest.json`, writes `alternatives-fastest.json` and `alternatives-lambda-<L>.json` carrying EVERY path in `paths[]` (the documented /route shape for alternatives). The R1 hints are fixed in the recorder and written into each header. Layout: Tests/Fixtures/t0239/topanga-malibu/ (T1, 34.0944,-118.6013 -> 34.0365,-118.687, the plan-pair endpoints) and Tests/Fixtures/t0239/zuma-agoura/ (T4, 34.017,-118.823 -> 34.144,-118.76, .artifacts/routes/B/fastest.json recorded.from/to). Graph: the preserved services/routing/work/t0213/graph-la-window, config.yml + profiles mounted as T-0182 did, the shipped router jar.
  - R8 ROADS per row: `street_name` runs grouped by name, a name listed when the route covers >= 300 m of it in total, in order of first appearance, unnamed stretches skipped. The full list is printed, not the probe's `via` difference.
  - R9 HANDOFF: each row's waypoints are `PlanWaypoints.decisionPoints` over that row's own PlanTable (the committed pin rule: <= 9, route order, road changes onto the longest stretches) and its URL is Handoff's `AppleMapsDirections(source: O, destination: D, waypoints:).url()` - the committed builder, which refuses more than 9.
  - R10 CLI: `ops/plan --menu <O> <D> [--max N] --recorded <dir>` replays offline and is what CI runs. `--menu --router <url>` is REFUSED by name (exit 2): no served graph exists (T-0209/T-0221) and a live alternative_route body is not built by this task - a flag that parses and then plans against nothing is the failure PlanArguments exists to refuse.
  - R11 TOUCHES WIDENED by ops/lib/check-mutate-population.py: a runnable ops/mutate driver outside its literal DRIVERS whitelist is a refusal (exit 2), and COVERED_FLOOR is where a new population's subjects are pinned; the Brief's "register it wherever check-mutate-population wants" names that file.
  - R12 P-SAFE-04 is in pins_affected but `grep -n SAFE-04 pins/PINS.yaml` finds nothing and PINS.yaml is not in touches: the property is carried by the test by name (MenuCLITests); the pin row stays open and is named in the PR.
  - R13 POPULATION: ops/mutate/menu.py (CLI, floors, proof arms) + menu_mutations.py + menu_run.py, straightline's three-file shape; SUBJECT_MODULES = RouteMenu.swift, MenuRow.swift, MenuArguments.swift; killers are suites RouteMenuTests and MenuCLITests; literal MIN_MUTATIONS; --prove-vacuity empties exactly those test files.
- 2026-09-25T23:57:58Z RECORD RUN landed (agent/claude-opus-5). The committed Tests/Fixtures/t0182-recorder/Recorder.java, extended per R7 (`--alternatives on`, 256 lines), compiled in WSL docker (maven:3.9-eclipse-temurin-21@sha256:c2a2c585...) against .artifacts/routes/_build/scenic-router.jar and run over a copy of services/routing/work/t0213/graph-la-window with services/routing/config.yml + profiles mounted; ladder models from `ops/plan --emit-model {0,2,4,6,8}` (cmp: byte-identical to the probe's models). Count lines, verbatim:
  `JAR_SHA256 7931f599a14bd00844793ef441ce6228e475e5d5482be5702a75e506f076fe43` / `GRAPH_PROPERTIES_SHA256 170bfe37bda146a51821f74fa53d9a42084e1cb3db27e2841205fe432c20b188` / `COMPILED 1 classes`
  `TRIP topanga-malibu 34.0944,-118.6013 -> 34.0365,-118.687`: car_fast `path=0 time_ms=1075693 distance_m=18224.3 points=478 way_runs=61`; alternatives-fastest paths 0-3 time_ms 1075693 / 1496192 / 1804912 / 1853263; lambda-0 the same four; lambda-2 1075693 / 1804912 / 1853263 / 2253369; lambda-4 1075693 / 2253369 / 1853263 / 2095756; lambda-6 2253369 / 1853263 / 1075693 / 1804912; lambda-8 2253369 / 1853263 / 1804912 / 1075693. 24 alternative paths, 6 distinct durations (1075693, 1496192, 1804912, 1853263, 2095756, 2253369) - the probe's T1 6.
  `TRIP zuma-agoura 34.017,-118.823 -> 34.144,-118.76`: car_fast `path=0 time_ms=1099444 distance_m=23231.6 points=400 way_runs=64`; alternatives-fastest 1099444 / 1639607 / 1728833 / 1740880; lambda-0 1099444 / 1425753 / 1639607 / 1728833; lambda-2 1099444 / 1425753 / 1639607 / 2092858; lambda-4, -6, -8 1099444 / 1425753 / 2092858 / 1639607. 24 alternative paths, 6 distinct durations (1099444, 1425753, 1639607, 1728833, 1740880, 2092858) - the probe's T4 6. `Latigo Canyon Road` occurs in the street_name detail of alternatives-lambda-{2,4,6,8}.json (the 2092858 ms path, +16.6 min), matching the probe's found_by.
  `WROTE` 14 files; Tests/Fixtures/t0239/ is 1.2 MB (7 files per trip, alternatives-*.json 73-83 KB each).
