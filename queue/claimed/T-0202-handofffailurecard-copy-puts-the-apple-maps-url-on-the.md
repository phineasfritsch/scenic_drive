---
id: T-0202
title: HandoffFailureCard - Copy puts the Apple Maps URL from SkylineHandoff.directions() on the clipboard beside the road list, and the button's label says what it copies
state: claimed
owner: agent/claude-opus-5
owner_session: null
claimed_at: 2026-09-19T13:05:19Z
lease_expires_at: 2026-09-19T21:05:19Z
worktree: .worktrees/T-0202
branch: task/T-0202
exclusive: []
touches: [apps/ios/Packages/ScenicApp/Sources/FeatureScenicHome/, apps/ios/Packages/ScenicApp/Sources/DesignSystem/, Sources/Handoff/, Tests/HandoffTests/, ops/lib/]
pins_affected: []
reviewer: null
depends_on: [T-0170]
verify: [ops/test, ops/check-pins]
acceptance:
  - "the clipboard payload on a failed handoff is the maps.apple.com URL SkylineHandoff.directions() builds, THEN the road list, the straight-line line and the timing line (the URL first: pasted into Messages or Notes it is the tappable thing); the button label names the payload ('Copy the drive' or better - rule the words with both drivers' quotes in the Log: DRIVER ONE 'paste it where?', DRIVER TWO 'Copy to Notes'); identifiers unchanged; ios-compile dispatch green with the run id quoted"
  - "the payload is a DriveFacts/HandoffFailureCard static a Linux-free structural check can read, so the URL and the rendered card cannot drift; bash ops/lib/check-line-cap and bash ops/queue-check bare"
---
## Brief

From the 00:13 panel's two LA drivers (grounded): on a failed handoff 'Copy the roads' (HandoffFailureCard.swift:96)
copies three lines of prose (:56) - DRIVER ONE: 'paste it where?'; DRIVER TWO presses Try again twice and quits.
The URL the app already builds is the one payload a friend can tap from a text message. Relabelling alone
(DRIVER TWO's 'Copy to Notes') would mislabel a prose payload; the payload changes first.

## Log
- 2026-09-19T06:29:45Z filed by agent/claude-fable-5-1 from the 00:13 panel's grounded synthesis (DRIVER ONE-3, DRIVER TWO-3). Not started; after #110 merges.
- 2026-09-19T12:44:08Z PROMOTED to ready/ by agent/claude-fable-5-1 (05:13 panel, grounded): its dependencies (T-0170 via #110, T-0178 via #115) are in done/. ONE BRANCH with its three siblings (T-0202, T-0203, T-0210, T-0212 all edit DriveFacts / DriveCopy / HandoffFailureCard / StraightLineDistance): claim all four together, one PR, one ios-compile. T-0211 (the freeway middle leg) stays in backlog pending the owner's return-leg choice.
- 2026-09-19T13:05:19Z claimed by agent/claude-opus-5; lease until 2026-09-19T21:05:19Z
- 2026-09-19T13:06:31Z touches: widened to the union of T-0202, T-0203, T-0210 and T-0212 by agent/claude-fable-5-1 (orchestrator): the 05:13 panel ruled the four ONE branch and ONE PR, and the pre-commit hook reads the touches: of the task the BRANCH names (task/T-0202). The siblings' acceptance blocks all bind this PR; the reviewer's PASS moves all four to queue/done/ in one commit.
- 2026-09-19T13:13:49Z RULINGS by agent/claude-opus-5 (owner), before any code, for all four tasks on this branch (T-0202, T-0203, T-0210, T-0212). Quotes are the panels' grounded driver lines carried in the four Briefs.
  R0 THE BOX HAS NO SWIFT. `wsl -e bash -lc "which swift; docker images"` -> no swift on PATH, no swift image among the ten (scenic-routing, scenic-etl, maven, temurin, dibs). The sibling Logs (T-0170, T-0178) ran `swift test --scratch-path .build/TXXXX --filter HandoffTests` on a box that had one; this session cannot. So the HandoffTests count line is CI's `linux-core` job (ops/test inside it), and the RED-first demonstration required by CLAUDE.md is bought the only way it can be here: the PR is opened on the RED commit so `linux-core` (on: pull_request) runs the failing suite, then the GREEN commit re-runs it. The PR body and the acceptance block are re-quoted at the final commit either way.
  R1 THE CLIPBOARD PAYLOAD (T-0202). Order: the `maps.apple.com` URL FIRST, then the road list, then the straight-line line, then the timing line - DRIVER ONE's 'paste it where?' is answered by the first line of the paste being the tappable thing in Messages or Notes, and DRIVER TWO's 'Copy to Notes' is then a true description of what lands there. ONE URL BUILDER: `SkylineHandoff.directions(for:)` becomes a forward to `HandoffDrive.directions`, and the payload is composed in `Sources/Handoff` by `HandoffDrive.clipboardPayload(roadList:straightLine:timing:)`, which takes its URL from `try? url()` - the same `AppleMapsDirections` the button opens through `SkylineHandoff.open(`. `HandoffFailureCard.clipboardText` calls that shipping symbol and nothing else, so the card cannot drift from the URL. DISAGREEMENT RULED: the task text prefers a new file under `Sources/Handoff`, but P-PROC-06 refuses a module added by this branch with no mutation population, and its allowlist (`ops/lib/mutate-population-allowlist.json`) is OUTSIDE this branch's `touches:` - a new file could not be legally accompanied by its entry. The composition therefore lands on the EXISTING, already-allowlisted `HandoffDrive.swift` ('it selects a route, it computes no number' - still true of a string join), no module is added, and `python ops/lib/check-mutate-population.py` stays at '0 added by this branch'. No structural check under `ops/` is written for the same reason (`ops/` is outside `touches:`) and none is needed: the payload is a Linux-testable shipping symbol. BUTTON WORDS: 'Copy the roads' is now false - it copies a link and the roads. Label: 'Copy link + roads', tapped state 'Copied'. `home.error.copy` unchanged.
  R2 MILES (T-0203). `StraightLineDistance.wholeMiles(through:)` floors the SAME `meters(through:)` the kilometre figure floors - one computation, two renderings, no second chain and no km->mi re-derivation. `wholeMiles(for: drive)` beside `wholeKilometers(for: drive)`. Literals pinned in `Tests/HandoffTests` beside the km ones: 69 mi for the Skyline chain (112_268.093 m), 29 mi for the LA chain (47_445.124 m); RED first with each literal one too high. THE LABEL: DRIVER TWO read '112 km ... through the pins' as an engineer's number and guessed 90 minutes. The screen now reads 'About N miles as the crow flies, pin to pin. The roads are longer.' - 'as the crow flies' is the phrase that cannot be read as driving distance, 'pin to pin' says which points, and the third sentence keeps the understatement explicit. Kilometres stay computed and test-pinned; the US-only launch scope means the SCREEN renders miles only.
  R3 THE TIMING SENTENCE, PER SURFACE AND PER DRIVE (T-0210). `DriveFacts.timing` becomes `DriveFacts.timing(for:)`; the accessibility identifier stays `home.timing`. NO number, no minutes, no hours figure on either surface. LA loop (DRIVER ONE, 03:13: 'I have 25 minutes... it is an afternoon, not a commute. But the screen never says that.'): 'Plan an afternoon, not a commute. Apple Maps gives you the real time when it opens.' Peninsula loop, ruled from its length and not from a clock - its straight line is more than twice the LA loop's (112 km of chain against 47), so it cannot be the shorter outing: 'Plan a long afternoon, not a commute - this one runs further than the LA loop. Apple Maps gives you the real time when it opens.' THE FAILURE CARD GETS ITS OWN SENTENCE, because the home sentence's second half is a promise the card cannot keep: `HandoffFailureCard.timingNote` = 'Apple Maps did not open, so nothing here can promise a time.', rendered at `home.error.timing`. THE PASTE carries the HOME sentence, not the card's: the paste travels to somebody who will open Maps themselves from the first line, and 'Apple Maps did not open' would describe this phone's failure to them.
  R4 THE SECOND CHIP (T-0212). Ruled A, not B: the chip NAMES ITS PLACE - `DriveCopy.shortName(for: .skyline)` 'Bay Area' -> 'SF Peninsula' (the title beside it already says 'starts and ends in San Francisco'), so DRIVER TWO's 'somebody else's bookmark left in my app' becomes a second drive that is plainly somewhere else. B (hide non-local drives) is REFUSED: deciding 'local' needs a location, and `HandoffDrive.defaultDrive`'s ruling is that this app reads none - no CoreLocation, no locale finer than `US`. Identifiers `home.drive.skyline` / `home.drive.la` unchanged; `DriveCopy.mapCaption` picks the new words up through `shortName` with no edit of its own.
  R5 P-SAFE-03. `bash ops/lib/check-safety-disclaimer` BEFORE any edit -> exit 0, the tracked set quoted: `SkylineHandoff.open(` once (GatedHandoffButton.swift line 87) dominated by the guard at line 82, `GatedHandoffButton(` once in ScenicHomeScreen.swift, `isSafetyDisclaimerAcknowledged` by occurrence GatedHandoffButton.swift(2) ScenicHomeScreen.swift(4), the key ScenicHomeScreen.swift(1). Nothing in R1-R4 adds a door: the card composes a URL STRING and never opens it (no `SkylineHandoff.open(` outside the gated button), no file gains or loses an acknowledgement read, and `ScenicHomeScreen.swift` is not edited at all - which also keeps the later merge with PR #119 trivial. Re-run bare after the edits.
- 2026-09-19T13:28:45Z BUILT, RED THEN GREEN, and the acceptance block re-run BARE on the merged head by agent/claude-opus-5. PR #121 (https://github.com/phineasfritsch/scenic_drive/pull/121), base main, head task/T-0202.
  RED FIRST, through CI, because R0: commit 5eaa8cb armed two failures on purpose - `HandoffDrive.payload` appending the URL LAST, and the mile literals one too high (70 Skyline, 30 LA). linux-core run 35445514660 on headSha 5eaa8cb -> conclusion FAILURE: "Test run with 293 tests failed after 0.276 seconds with 8 issues", among them `✘ Test "the URL is the FIRST line of the payload, for both drives" failed ... with 4 issues`, `✘ Test "the payload is the four lines in order: URL, roads, straight line, timing" failed ... with 2 issues`, and the two literals by name - `StraightLineDistanceTests.theWholeMileFigureIsTheNumberTheScreenRenders() - Expectation failed: (miles -> 69) == (Self.straightLineMiles -> 70): got 69 mi` and `SantaMonicaMountainsChainTests...: (miles -> 29) == (Self.straightLineMiles -> 30): got 29 mi`. The floored figures CI measured, 69 and 29, are the literals the green commit types.
  GREEN: commit 4ec6832 -> linux-core run 35445868310 on headSha 4ec6832 -> conclusion SUCCESS, `TESTS linux=1611/76 ios=skipped failed=0 skipped=0`. That is the HandoffTests count line for this branch: no Swift toolchain exists on this box (R0), so it is CI's linux-core job that runs `ops/test`, not `swift test --scratch-path` as T-0170/T-0178 could.
  MERGE: `git fetch origin && git merge --no-edit origin/main` -> merge commit 2ee8cd2, 2 files changed (queue/backlog/T-0216, T-0217), NO conflict, no Swift file touched by the merge.
  ACCEPTANCE, BARE, ON THE MERGED HEAD:
    bash ops/lib/check-safety-disclaimer -> exit 0, the tracked set UNCHANGED by these edits: SkylineHandoff.open( once (GatedHandoffButton.swift line 87) dominated by the guard at line 82; GatedHandoffButton( once in ScenicHomeScreen.swift with no `: true`; the write once at line 138 inside the accept closure; by occurrence isSafetyDisclaimerAcknowledged GatedHandoffButton.swift(2) ScenicHomeScreen.swift(4), the key ScenicHomeScreen.swift(1) - identical to the counts quoted in R5 BEFORE any edit. The card composes a URL string and never opens one.
    bash ops/lib/check-line-cap -> exit 0, "P-SRC-02: 91 Swift files tracked (Sources=29, Tests=41, apps/ios=21), none over 300 lines" (90 -> 91: HandoffDriveClipboardPayloadTests.swift).
    bash ops/lib/check-exec-bits -> exit 0, "P-OPS-01: 79 files, 23 required present, all modes correct".
    python ops/lib/check-mutate-population.py -> exit 0, "P-PROC-06: every added module is covered or allowlisted; the floor of 23 holds" and "0 added by this branch" - R1's ruling holding: the payload landed on the existing allowlisted HandoffDrive.swift, so no module was added and no entry in ops/lib/ (outside touches:) was needed.
    bash ops/queue-check -> exit 0, "QUEUE OK (210 tasks)".
    wc -l on every touched file: 124 Sources/Handoff/HandoffDrive.swift, 101 Sources/Handoff/StraightLineDistance.swift, 84 Tests/HandoffTests/HandoffDriveClipboardPayloadTests.swift, 156 Tests/HandoffTests/StraightLineDistanceTests.swift, 190 Tests/HandoffTests/SantaMonicaMountainsChainTests.swift, 85 apps/ios/Packages/ScenicApp/Sources/FeatureScenicHome/DriveFacts.swift, 86 .../DriveCopy.swift, 156 .../HandoffFailureCard.swift, 92 .../SkylineHandoff.swift. ScenicHomeScreen.swift: NOT TOUCHED (PR #119).
  THE RENDERED STRINGS, BOTH DRIVES - the owner reads these:
    LOS ANGELES (default drive, `home.drive.la` chip "Los Angeles"; title "Santa Monica Mountains loop · starts and ends in Westwood")
      home.distance: "About 29 miles as the crow flies, pin to pin. The roads are longer."
      home.timing:   "Plan an afternoon, not a commute. Apple Maps gives you the real time when it opens."
      home.error.timing (failure card only): "Apple Maps did not open, so nothing here can promise a time."
      copy button:   "Copy link + roads" / "Copied"
      clipboard payload, in full, four lines:
        https://maps.apple.com/directions?destination=34.06110,-118.44548&waypoint=34.05820,-118.47930&waypoint=34.04739,-118.52581&waypoint=34.03857,-118.55562&waypoint=34.04011,-118.57930&waypoint=34.09312,-118.60182&waypoint=34.12587,-118.60045&waypoint=34.16801,-118.60576&waypoint=34.13208,-118.45321&waypoint=34.12922,-118.44168&mode=driving
        Sunset Boulevard west, PCH north, Topanga Canyon Boulevard up, the 101 and 405 back over the pass, Mulholland Drive east, down Beverly Glen.
        About 29 miles as the crow flies, pin to pin. The roads are longer.
        Plan an afternoon, not a commute. Apple Maps gives you the real time when it opens.
    SF PENINSULA (`home.drive.skyline` chip "SF Peninsula" - was "Bay Area"; title "Skyline loop · starts and ends in San Francisco")
      home.distance: "About 69 miles as the crow flies, pin to pin. The roads are longer."
      home.timing:   "Plan a long afternoon, not a commute - this one runs further than the LA loop. Apple Maps gives you the real time when it opens."
      clipboard payload, in full, four lines:
        https://maps.apple.com/directions?destination=37.78794,-122.40752&waypoint=37.70526,-122.47165&waypoint=37.44197,-122.26667&waypoint=37.47579,-122.30852&waypoint=37.50621,-122.34073&waypoint=37.47272,-122.35680&waypoint=37.38776,-122.26638&waypoint=37.36648,-122.24765&mode=driving
        I-280 south, Cañada Road north, CA-92 west, Skyline Boulevard south, then back to the city.
        About 69 miles as the crow flies, pin to pin. The roads are longer.
        Plan a long afternoon, not a commute - this one runs further than the LA loop. Apple Maps gives you the real time when it opens.
      The two URLs are the shipped literals at AppleMapsDirections.coordinateDecimals = 5, in `destination`, `waypoint` (route order), `mode` order; the equality that binds them to what the button opens is HandoffDriveClipboardPayloadTests, not this transcription.
  THE COMPILER, ONE DISPATCH, GREEN FIRST TIME: `gh workflow run ios-compile.yml --ref task/T-0202` -> run 35446213122; `gh run view 35446213122 --json status,conclusion,headSha` -> {"conclusion":"success","headSha":"ff4d04f38663be509267297c51aafa72cceac1e5","status":"completed"} - headSha IS this branch's head. The last Swift commit is 4ec6832 and `git diff --name-only 4ec6832 ff4d04f -- '*.swift'` -> no output, so nothing Swift moved after the merge; the run compiled exactly the Swift on the merged head. AND the merged head's own linux-core: run 35446203967 on ff4d04f -> success, `TESTS linux=1611/76 ios=skipped failed=0 skipped=0`, `gh pr checks 121` -> core pass 2m16s, pins-source-only pass 53s.
  The map caption picks the new chip words up with no edit of its own: "Preview build: two fixed drives, SF Peninsula selected. ..." / "... Los Angeles selected. ...". Attribution and the disclaimer are untouched on every surface.
- 2026-09-19T16:54:35Z PRE-REVIEW FIX RULED by agent/claude-opus-5 (owner), before any code, closing the read-only verifier's findings before the review is bought (CLAUDE.md, the author rule). The verifier is `.artifacts/signoffs/copy-four-mutant-pass.md` (agent/claude-fable-5-1, build e800d99): FOUR mutants survive with `linux-core` green, `ios-compile` green and `ops/lib/check-safety-disclaimer` exit 0 - M1a (the LA card copies the Skyline URL), M1c (the button opens Skyline while the card copies LA), M3a (a number enters `home.timing`), M3b (the card's sentence made identical to the home sentence). I WITHDRAW R1's sentence "No structural check under `ops/` is written for the same reason (`ops/` is outside `touches:`) and none is needed": `ops/` being outside `touches:` is a scope fact, not an argument that none is needed, and four one-token edits shipping through every gate is the definition of the gap. The rulings below are the whole of this branch's remaining work.
  F0 TOUCHES WIDENED, deliberately and minimally: `ops/lib/` is added to this task's `touches:` in the SAME commit as this entry, because the fix requires a new file at `ops/lib/check-drive-copy` and the pre-commit hook reads the `touches:` of the task file being committed (T-0160). Nothing else under `ops/` is edited: `ops/lib/check-safety-disclaimer` and its lib and table are not touched (P-SAFE-03 is not this task's pin), `ops/lib/mutate-population-allowlist.json` is not touched (no module is added - see F2), and `pins/PINS.yaml` is not touched (see F4).
  F1 THE CLASS, NAMED ONCE. `FeatureScenicHome` is an Apple-only target with NO TEST BUNDLE and there is no Apple toolchain on this box (R0). Everything in it is therefore unobserved by every gate: `linux-core` cannot import it, `ios-compile` only type-checks it, and `check-safety-disclaimer` counts a different population (the acknowledgement's identifiers and the one door out). Two shapes close a defect in that target: MOVE the property into a Linux target that has tests, or, where the property IS the Apple-only call site, a whitelist-anchored structural check over `apps/ios` in the P-SAFE-03 style. M3a/M3b take the first (F2), M1a/M1c the second (F3). Nothing is closed by prose.
  F2 THE WORDS MOVE TO `Sources/Handoff`, which CORRECTS R3's location ruling. R3 put both sentences in the feature target (`DriveFacts.timing(for:)` and `HandoffFailureCard.timingNote`); M3a typed "about 3 hours" into the first and M3b made the second equal to the first, and nothing could see either. `HandoffDrive` now owns three shipping symbols - `realTimePromise` ("Apple Maps gives you the real time when it opens."), `timingSentence` (per drive, the HOME surface) and `failureTimingSentence` (the card) - and `DriveFacts` and `HandoffFailureCard` RENDER those: `DriveFacts.timing(for:)` and `HandoffFailureCard.timingNote` are gone, so no second literal exists in the Apple-only target to drift from. `home.timing` and `home.error.timing` are unchanged, as R3 required; `HandoffFailureCard.clipboardText` still pastes the HOME sentence (`drive.timingSentence`), as R3 required. No module is added - the sentences land on the existing, already-allowlisted `HandoffDrive.swift` - so `check-mutate-population.py` stays at "0 added by this branch" and R1's P-PROC-06 reasoning still holds.
  `Tests/HandoffTests/HandoffDriveTimingSentenceTests.swift` binds them, over `HandoffDrive.allCases` in every test (never a drive the test chooses - that blindness is what M1a exploited). THE EXACT PREDICATES, ruled here so the reviewer can check them against the file: (a) `drive.timingSentence` contains no `Character.isNumber`; (b) `drive.failureTimingSentence` contains no `Character.isNumber`; (c) `drive.failureTimingSentence != drive.timingSentence` AND `!= other.timingSentence` for every other case (M3b in both directions); (d) "does not promise a time" is decided as `drive.timingSentence.contains(HandoffDrive.realTimePromise)` AND `!drive.failureTimingSentence.contains(HandoffDrive.realTimePromise)` - the predicate is over a SHIPPING IDENTIFIER, the named clause both home sentences are built from, not over a phrase typed into the test; (e) the paste composed the way the card composes it (`clipboardPayload(roadList:straightLine:timing: drive.timingSentence)`) has `drive.directions.url()` as its FIRST line and `drive.timingSentence` as its last, for EVERY case. What (d) cannot decide, stated so nobody reads more into it: whether some future sentence promises a time in words that do not contain that clause. That is a reading, not a predicate; the digit rule and the difference rule are what a machine can hold.
  F3 THE TWO RECEIVERS NO LINUX TEST CAN SEE -> `ops/lib/check-drive-copy` (new, 100755, its own `--prove-red` table, the shape of `ops/lib/check-safety-disclaimer`). M1a is the card's `clipboardPayload(` RECEIVER and M1c is `SkylineHandoff.directions(for:)`'s body; both are Apple-only call sites, so no Linux test can reach either - the suite calls the method on a drive the TEST chooses. Four WHITELISTS over identifiers, counts typed as literals, `//` and string literals stripped before counting, never a comment anchored (CLAUDE.md): (i) `clipboardPayload(` at `HandoffFailureCard.swift(1)` and every occurrence spelled `drive.clipboardPayload(`; (ii) `.directions` at `SkylineHandoff.swift(1)` and every occurrence spelled `drive.directions`; (iii) the drive cases `.skyline` and `.santaMonicaMountains` each ONLY at the tracked selector sites, counted on this tree and typed in - `DriveBasemap.swift(1) DriveCopy.swift(3) DriveSelector.swift(1)` - so a hard-coded case anywhere else, in the card or the handoff, is refused by name; (iv) `.timingSentence` at `DriveFacts.swift(1) HandoffFailureCard.swift(1)` and `.failureTimingSentence` at `HandoffFailureCard.swift(1)`, which is what stops F2's words walking back into the untested target as a fresh literal. Not a blacklist of spellings of the bad write: any other count in any file is refused, whatever the spelling (PR #101's B5 precedent).
  F4 THE PIN. `pins/PINS.yaml` is NOT edited by this branch: no pin exists for this class today (the only Apple-source pin is P-SAFE-03, whose statement is the disclaimer gate and whose assertion is a different script), PINS.yaml is serial-adjacent with PR #119 editing it, and inventing a pin id here would collide. RECORDED GAP: `ops/lib/check-drive-copy` ships registered by no pin, so `ops/check-pins` does not run it; it runs bare in this task's acceptance block and in the PR body. The pin entry belongs with the Apple-source pin work already filed - T-0180 (the P-SAFE-03 XCUITest half, backlog) is where the "what a source check cannot see" list for this target is owned - and the reviewer should read the missing registration as this branch's recorded gap, not as an oversight.
  F5 THE RENDERED STRINGS, the pass's third finding, ruled. "Plan an afternoon, not a commute." IS KEPT for the LA loop. It is a duration in words over a loop nobody has timed, and that is what both drivers asked for in words: DRIVER ONE (03:13) "I have 25 minutes... it is an afternoon, not a commute. But the screen never says that." - the sentence is that driver's own phrase returned to them, a statement about the KIND of outing, and it is followed in the same breath by where the number comes from. An "afternoon" is not a figure with the digits removed the way "about 3 hours" is: it does not survive being read as an ETA, which is precisely the failure "112 km" caused (DRIVER TWO guessed 90 minutes). SOFTENED: the Peninsula sentence's "this one runs further than the LA loop" is withdrawn - directly beneath a straight-line figure, "runs further" reads on screen as a DRIVING distance, and no road distance for either drive has been measured by anything in this repository (the code comment said "crow flies"; the screen does not). NEW SENTENCE, in full: "Plan a long afternoon, not a commute - the longer of the two drives. Apple Maps gives you the real time when it opens." (117 characters, 8 shorter than the sentence it replaces). The claim is now an ORDERING of the two measured straight lines (112 km of chain against 47), which is what R3 actually ruled from.
  F6 THE LA MILES LITERAL, beside the literal, as the pass requires. The LA chain is 47_445.124 m = 29.4810 mi: floor 29, round 29. THE LA LITERAL 29 CANNOT SEPARATE FLOOR FROM ROUND - its fraction is .48, so `.rounded(.down)` -> `.rounded()` leaves it green. What carries flooring is the Skyline literal (112_268.093 m = 69.7602 mi: floor 69, round 70) and the synthetic `theMilesAreFlooredAndNeverRoundedUp` (3200.7 m = 1.988 mi -> 1, not 2). R2's "literals pinned beside the km literals" must NOT be read as a floor-vs-round proof for the LA loop; it is a value proof for that chain and nothing more. No code change: the mutant is caught, twice, by the other two assertions (the pass ran M2a and it went RED).
  F7 RED FIRST, and the one deviation from a single code commit, declared. The structural check's rows are demonstrated red LOCALLY and by name (`--prove-red`, a throwaway copy of `apps/ios` per row). The new Swift suite cannot be run on this box (R0), so it is bought the way this branch bought its first RED: the mutants are ARMED in the shipping symbol on one commit, `linux-core` runs them through the PR, and the next commit disarms them. That is two code commits instead of one; CLAUDE.md's "a check that has never been seen red is untested" is the rule that decides it, and the brief's "ONE code commit" is the thing that gives way.
- 2026-09-19T17:13:00Z PRE-REVIEW FIX BUILT, RED THEN GREEN, and the WHOLE acceptance block re-run BARE on the merged head by agent/claude-opus-5 (owner). Commits 42d6901 (the rulings + touches), a89ea0e / 58a280a (RED), 766653d (GREEN), merge 86f12f5.
  RED FIRST, THROUGH CI, because R0 (no Swift toolchain on this box). a89ea0e armed M3a ("about 3 hours" in `timingSentence(.santaMonicaMountains)`) and M3b (`failureTimingSentence` returning `timingSentence`) in the SHIPPING SYMBOL. Its linux-core run 35456558369 went red at BUILD time - `error: fatalError` in 15 s with no file diagnostic (ops/test:23 greps swift test's output down to `^error:`, so a macro expansion's own message is all that reaches the log) and pins-source-only's P-SAFE-05 fell over the same package. A red that names nothing proves nothing, so 58a280a rewrote the suite in the expression shapes the existing compiling suite uses (a Bool bound in a `let` before every `#expect`, no `"literal" + value` where a `Comment` is expected, `try #require` for the optionals) with both mutants STILL ARMED.
  RED BY NAME: linux-core run 35456872113 on headSha 58a280a -> conclusion FAILURE, `TESTS linux=1646/76 ios=skipped failed=5 skipped=0`, "Test run with 298 tests failed after 0.247 seconds with 12 issues": `✘ Test "the home sentence carries no digit, for every drive" failed ... with 1 issue`, `✘ Test "the failure card's sentence carries no digit, for every drive" ... 1 issue`, `✘ Test "the card's sentence is no drive's home sentence" ... 4 issues`, `✘ Test "the promise of a real time lives on the home surface and never on the card" ... 2 issues` (named: `theCardSentenceMakesNoTimePromise(drive:) - Expectation failed: (cardPromises -> true) == false: the card promises a time from the app that refused to open: Plan a long afternoon, not a commute - the longe...`), and `✘ Suite "The timing sentences of the two drives" failed ... with 8 issues`. Four of this branch's five new tests, each red for the defect it is named for.
  THE FIFTH FAILURE IN THAT RUN WAS AN EXISTING GUARD, RED BY NAME ON THE NEW PROSE, and it is ruled rather than allow-listed: `✘ Test "every capitalised identifier in the shipping source is on the allow-list" ... 4 issues` (`HandoffSourceTests`, Suite "Handoff shipping source") - `Plan`, `Apple`, `Maps`. RULING: that list closes the set of TYPES `Sources/Handoff` names, and a capitalised word inside a `"..."` literal is DATA, not a dependency; adding three English words to a `Set<String>` called `allowedTypes` would make the list say something false. So the scan now reads each line with its string literals emptied (`HandoffSourceTests.outsideStringLiterals`), and the half that must stay true is pinned beside it - `stringLiteralsAreNotTypeReferences` requires a type named in CODE on the same line as a sentence to still be seen (`NumberFormatter`), an escaped quote inside a literal not to eat the code after it, and a trailing comment's odd quote not to eat the next line. What this gives up, stated: a type reached BY NAME from a string. That escape still spells a capitalised identifier in code (`NSClassFromString`, a `Bundle` lookup), where the scan still sees it, and every string in this module before today was a lowercase URL component (`maps.apple.com`, `waypoint`, `driving`), so nothing that was ever covered stops being covered.
  GREEN: 766653d disarms both mutants (the LA sentence is "Plan an afternoon, not a commute." again; `failureTimingSentence` is the card's own sentence) and lands the allow-list ruling. The GREEN linux-core count line for this branch is quoted in the next entry, with the ios-compile run - this entry is written as the stage lands (CLAUDE.md), not held back.
  MERGE: `git fetch origin && git merge --no-edit origin/main` -> merge commit 86f12f5, NO conflict. NOT queue-only this time, which is why the whole block was re-run after it: main brought `apps/ios/.../MapAdapter/BasemapResolver.swift` and `ScenicStyleDocument.swift` (Swift inside my check's own population), three new `ops/lib/check-map-attribution*` files, `ops/lib/check-pmtiles-provenance.py`, `ops/mutate/scenic_tags*.py` and an edited `pins/PINS.yaml`. No file this branch touches was touched by the merge.
  ACCEPTANCE, BARE, ON THE MERGED HEAD 86f12f5:
    bash ops/lib/check-drive-copy -> exit 0. "over the 21 .swift file(s) under apps/ios - clipboardPayload( at .../HandoffFailureCard.swift(1), every occurrence spelled drive.clipboardPayload(; .directions at .../SkylineHandoff.swift(1), every occurrence spelled drive.directions; the drive cases .skyline and .santaMonicaMountains each at .../DriveBasemap.swift(1) .../DriveCopy.swift(3) .../DriveSelector.swift(1) and nowhere else; .timingSentence at .../DriveFacts.swift(1) .../HandoffFailureCard.swift(1), .failureTimingSentence at .../HandoffFailureCard.swift(1)." The population includes the two MapAdapter files main just added; neither names a drive case, a payload or a directions builder.
    bash ops/lib/check-drive-copy --prove-red -> exit 0, "prove-red: 7/7 mutations refused by name": M1a the card copies a hard-coded drive (1 yes), M1c the handoff opens a hard-coded drive (1 yes), the card reads a hard-coded case beside the selected drive (1 yes), a second paste built beside the card's (1 yes), the failure card grows its own timing literal again (1 yes), the home screen stops rendering the shipping sentence (1 yes), an emptied feature tree (1 yes). Each row mutates a throwaway copy of the whole of apps/ios and requires a non-zero exit NAMING its reason; the table refuses to run at all against a tree that is already red.
    bash ops/lib/check-safety-disclaimer -> exit 0, the tracked set UNCHANGED by these edits and identical to R5's pre-edit quote: SkylineHandoff.open( once (GatedHandoffButton.swift line 87) dominated by the guard at line 82; GatedHandoffButton( once in ScenicHomeScreen.swift with no `: true`; the write once at line 138 inside the accept closure opening at 137; onBlocked() at line 83 before the return at line 84; by occurrence isSafetyDisclaimerAcknowledged GatedHandoffButton.swift(2) ScenicHomeScreen.swift(4), the key ScenicHomeScreen.swift(1). The product invariant holds: the safety disclaimer still gates the handoff.
    bash ops/lib/check-line-cap -> exit 0, "P-SRC-02: 92 Swift files tracked (Sources=29, Tests=42, apps/ios=21), none over 300 lines" (91 -> 92: HandoffDriveTimingSentenceTests.swift).
    bash ops/lib/check-exec-bits -> exit 0, "P-OPS-01: 84 files, 23 required present, all modes correct" (79 -> 84: main's three check-map-attribution files and check-pmtiles-provenance.py, plus this branch's ops/lib/check-drive-copy). `git ls-files -s ops/lib/check-drive-copy` -> 100755, committed with `git update-index --chmod=+x` because core.filemode is false here.
    python ops/lib/check-mutate-population.py -> exit 0, "P-PROC-06: 74 modules, 24 covered by 11 populations, 27 allowlisted, 0 added by this branch" and "every added module is covered or allowlisted; the floor of 23 holds". The new file is a bash CHECK under ops/lib/, not a numeric module under Sources/ or services/etl/etl/, so P-PROC-06 does not range over it; its mutation population is its own --prove-red table.
    bash ops/queue-check -> exit 0, "QUEUE OK (213 tasks)".
    wc -l on every touched file, re-measured on the merged head: 170 Sources/Handoff/HandoffDrive.swift, 101 Sources/Handoff/StraightLineDistance.swift, 84 Tests/HandoffTests/HandoffDriveClipboardPayloadTests.swift, 100 Tests/HandoffTests/HandoffDriveTimingSentenceTests.swift, 270 Tests/HandoffTests/HandoffSourceTests.swift, 156 Tests/HandoffTests/StraightLineDistanceTests.swift, 190 Tests/HandoffTests/SantaMonicaMountainsChainTests.swift, 86 apps/ios/.../DriveCopy.swift, 67 apps/ios/.../DriveFacts.swift, 158 apps/ios/.../HandoffFailureCard.swift, 92 apps/ios/.../SkylineHandoff.swift, 291 ops/lib/check-drive-copy. ScenicHomeScreen.swift: still NOT TOUCHED.
    F4 RE-VERIFIED ON THE MERGED HEAD: `grep -n "check-drive-copy\|clipboardPayload\|timingSentence" pins/PINS.yaml` -> no match. No pin registers this check even after main's PINS.yaml edit landed; the gap stands as F4 recorded it, and PINS.yaml is untouched by this branch.
  THE RENDERED STRINGS THAT CHANGED SINCE ff4d04f's transcription - the owner reads these:
    home.timing, SF PENINSULA, was "Plan a long afternoon, not a commute - this one runs further than the LA loop. Apple Maps gives you the real time when it opens." and is now "Plan a long afternoon, not a commute - the longer of the two drives. Apple Maps gives you the real time when it opens." (F5).
    home.timing LOS ANGELES: "Plan an afternoon, not a commute. Apple Maps gives you the real time when it opens." UNCHANGED (F5 keeps it).
    home.error.timing: "Apple Maps did not open, so nothing here can promise a time." UNCHANGED - now `HandoffDrive.failureTimingSentence`, rendered by the card, with a test that it is no drive's home sentence.
    home.distance, the chip words, the button label and the four-line payload order are all UNCHANGED; the payload's fourth line for the Peninsula drive carries the new sentence above.
